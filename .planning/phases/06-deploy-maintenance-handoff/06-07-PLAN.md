---
phase: 06-deploy-maintenance-handoff
plan: 07
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - astro.config.mjs
  - public/robots.txt
  - .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md
autonomous: false
requirements:
  - FOUND-04
tags: [phase-6, wave-3, domain, dns, https, vercel, cloudflare-registrar, d-08, site-flip, foun-04]

user_setup:
  - service: cloudflare-registrar
    why: "Domain registration for caleblim.com (or D-08 fallback) — only place Caleb has account-owner access to register the domain"
    env_vars: []
    dashboard_config:
      - task: "Open Cloudflare Registrar at dash.cloudflare.com/?to=/:account/registrar and search availability for caleblim.com"
        location: "https://dash.cloudflare.com/?to=/:account/registrar"
      - task: "If caleblim.com is available — register it. Else walk fallback chain: caleblim.co → caleb.work → caleblimkr.com. Surface as decision checkpoint before locking."
        location: "Cloudflare Registrar UI"
      - task: "After registration, DNS zone defaults to Cloudflare nameservers — Vercel A record + www CNAME get added in the DNS panel"
        location: "Cloudflare dashboard → DNS → Records for <registered-domain>"
  - service: vercel
    why: "Domain attach + HTTPS auto-provisioning — Caleb's existing Vercel account owns the project"
    env_vars: []
    dashboard_config:
      - task: "Open Vercel project settings at vercel.com/c-lb/caleb-lim-portfolio/settings/domains → Add Domain → enter the registered domain → Vercel shows required DNS records"
        location: "https://vercel.com/c-lb/caleb-lim-portfolio/settings/domains"
      - task: "Wait for Vercel domain status to flip from Invalid Configuration → Valid Configuration → SSL provisioned (~1-5min after DNS propagates)"
        location: "Vercel dashboard"

must_haves:
  truths:
    - "Domain availability for caleblim.com is checked at Cloudflare Registrar; if available, registered. If not, fallback chain walked (caleblim.co → caleb.work → caleblimkr.com) and the choice locked via blocking decision checkpoint with Caleb"
    - "Registered domain has a Cloudflare DNS A record for the apex (`@` → Vercel-supplied IP from `76.76.21.x` pool) set to **DNS only (grey cloud, NOT Proxied)** to avoid the Vercel auto-HTTPS redirect-loop landmine documented in RESEARCH §Pitfall 1"
    - "Registered domain has a Cloudflare DNS CNAME record for `www` → `cname.vercel-dns.com`, also DNS only (grey cloud)"
    - "Vercel dashboard shows the registered domain status = Valid Configuration with SSL provisioned (Let's Encrypt cert auto-provisioned within 1-5min of DNS verification per RESEARCH §Pitfall 4)"
    - "`dig +short <registered-domain> @1.1.1.1` returns the Vercel A IP (verified via Cloudflare's 1.1.1.1 resolver — Cloudflare-internal records are NOT special-cased by 1.1.1.1)"
    - "`curl -vI https://<registered-domain> 2>&1 | grep -E 'subject:|issuer:|HTTP/'` returns: HTTP/2 200, subject CN matches the domain, issuer matches Let's Encrypt (per RESEARCH §Vercel domain wiring step 7)"
    - "astro.config.mjs `site:` is flipped from `'https://caleb-lim-portfolio.vercel.app'` (Plan 06-02 Wave 1) to `'https://<registered-domain>'` (e.g. `'https://caleblim.com'`)"
    - "public/robots.txt Sitemap line host is flipped in lockstep with astro.config.mjs:site"
    - "After flip + rebuild + redeploy, dist/sitemap-0.xml contains `<loc>https://<registered-domain>/...</loc>` entries (not the Vercel default subdomain)"
    - "06-VERIFICATION.md Domain Availability Check Log section is populated with the actual outcome (domain registered + date) and the Critical-Path Walk header is updated to point at the custom domain"
    - "Phase 1-5 gates 1-25 still GREEN; Phase 6 Gates 26a, 26b, 26c, 27 still GREEN (the flip is a string-rename, not a config rewrite — no gate regression expected)"
    - "FOUND-04 traceability row in REQUIREMENTS.md gains a completion annotation (`Complete (Plan 06-07, 2026-MM-DD)`) following the FOUND-02 / FOUND-03 closure pattern"
  artifacts:
    - path: "astro.config.mjs"
      provides: "site: flipped to https://<registered-domain> (the actual outcome of the D-08 chain)"
      contains: "site:"
    - path: "public/robots.txt"
      provides: "Sitemap line host flipped in lockstep with astro.config.mjs:site"
      contains: "Sitemap: https://"
    - path: ".planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md"
      provides: "Domain Availability Check Log row(s) filled in; SC1 cert chain + DNS resolution rows filled in"
  key_links:
    - from: "Cloudflare DNS A record (apex)"
      to: "Vercel Edge CDN"
      via: "76.76.21.x Anycast pool IP; DNS only proxy mode"
      pattern: "DNS only"
    - from: "Vercel custom domain"
      to: "Let's Encrypt SSL cert"
      via: "Vercel auto-provisioning post-DNS-verify"
      pattern: "Let's Encrypt"
    - from: "astro.config.mjs site flip"
      to: "Astro.site in Base.astro (Plan 06-03 meta block)"
      via: "Astro injects updated site as Astro.site at build; OG/canonical/sitemap URLs auto-rewrite"
      pattern: "site:\\s*'https://"
---

<objective>
Wave 3 — register the domain, wire DNS at Cloudflare, attach the domain in Vercel, wait for HTTPS + Let's Encrypt SSL, then flip `astro.config.mjs:site` + `public/robots.txt` Sitemap line from the Vercel default subdomain (Wave 1 placeholder) to the custom domain. Closes FOUND-04.

Purpose: Closes the only outstanding v1 requirement (FOUND-04 — deployed to caleblim.com on Vercel; Cloudflare Registrar). The technical surface area is small — register, set DNS, attach, flip 2 strings. The risk-bearing parts are external: domain availability (D-08 fallback chain), DNS propagation latency (RESEARCH §Pitfall 4), and the Cloudflare-proxy redirect-loop landmine (RESEARCH §Pitfall 1).

The plan includes:
- A blocking **decision checkpoint** before registration locks in (D-08 fallback chain navigation if caleblim.com is taken)
- A blocking **human-verify checkpoint** at the Cloudflare DNS step (proxy mode must be DNS-only, grey cloud — Caleb confirms before walking away)
- A blocking **human-verify checkpoint** at Vercel "Valid Configuration" + SSL state
- An automated final flip (astro.config.mjs:site + robots.txt) + rebuild + redeploy + automated cert-chain + DNS verification

Output: domain registered + wired, custom domain serving HTTPS, 2 string flips committed, FOUND-04 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md
@.planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md
@.planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md
@.planning/REQUIREMENTS.md
@astro.config.mjs
@public/robots.txt

<interfaces>
<!-- Vercel domain-wire procedure (RESEARCH §Code Examples §Vercel domain wiring via dashboard): -->
<!--   1. Cloudflare Registrar: register caleblim.com (or D-08 fallback). At-cost ~$10.46/yr. -->
<!--   2. Vercel dashboard → Project caleb-lim-portfolio → Settings → Domains → Add Domain → enter <domain>. -->
<!--   3. Vercel shows "Invalid Configuration" + required DNS records: -->
<!--      - Apex: A record `@` → 76.76.21.x (or the IP Vercel shows — Anycast pool, may differ at task-time) -->
<!--      - www: CNAME `www` → cname.vercel-dns.com -->
<!--   4. Cloudflare dashboard → <domain> → DNS → Records → Add both records. -->
<!--      CRITICAL: Proxy status = "DNS only" (grey cloud), NOT Proxied (orange). -->
<!--   5. Wait for DNS propagation. Verify via dig +short <domain> @1.1.1.1 returning the Vercel IP. -->
<!--   6. Vercel dashboard → Domain status flips to "Valid Configuration" → SSL auto-provisions in 1-5 min. -->
<!--   7. Verify cert chain via curl -vI https://<domain> 2>&1 | grep -E 'SSL|TLS|issuer'. -->

<!-- D-08 fallback chain (CONTEXT.md): -->
<!--   1. caleblim.com (primary — Cloudflare Registrar at-cost ~$10.46/yr) -->
<!--   2. caleblim.co (fallback 1) -->
<!--   3. caleb.work (fallback 2) -->
<!--   4. caleblimkr.com (fallback 3 — middle-name variant) -->
<!-- Walk the chain. Surface as decision checkpoint before locking. -->

<!-- Anti-patterns (RESEARCH): -->
<!--   - DO NOT set the apex A record to "Proxied" (orange cloud) — causes redirect loops / 525 SSL errors per Pitfall 1. -->
<!--   - DO NOT flip astro.config.mjs:site BEFORE the domain is verified live — Astro.site rewrites sitemap + OG URLs at build, so they'd point at a dead domain. Verify cert chain first, then flip + rebuild + redeploy. -->

<!-- 2-step flip pattern (RESEARCH §Pitfall 2): -->
<!--   - Plan 06-02 set: site: 'https://caleb-lim-portfolio.vercel.app'  + robots.txt Sitemap host = same -->
<!--   - This plan flips BOTH to https://<registered-domain> in lockstep -->
<!--   - One commit covers both flips so the rebuild captures the change atomically -->

<!-- FOUND-04 closure annotation pattern (REQUIREMENTS.md FOUND-02 row line 133): -->
<!--   | FOUND-02 | Phase 5 | Complete (Plan 05-09 Lighthouse re-audit) | -->
<!-- Apply same to FOUND-04 after this plan: -->
<!--   | FOUND-04 | Phase 6 | Complete (Plan 06-07, 2026-MM-DD) | -->

<!-- Vercel A-record IP A1 [ASSUMED, per RESEARCH §Assumptions Log] is from the 76.76.21.x pool; -->
<!--   verify by reading the actual IP Vercel surfaces in the dashboard at task-time. -->
<!--   The exact IP may differ (Anycast pool). Use whatever Vercel shows. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Domain availability check + D-08 fallback navigation</name>
  <files></files>
  <action>Orchestrator runs availability checks against Cloudflare Registrar dashboard for caleblim.com and the D-08 fallbacks (caleblim.co, caleb.work, caleblimkr.com), surfaces the result table to Caleb, and waits for Caleb's choice via resume-signal.</action>
  <verify><human-check>Caleb selects option-a..option-e via resume-signal; the choice is recorded in 06-VERIFICATION.md Domain Availability Check Log (filled in Task 6).</human-check></verify>
  <read_first>
    - .planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md D-08 (fallback chain order + Vercel Domains alternative path)
    - .planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md §Code Examples §caleblim.com availability check
  </read_first>
  <decision>
    Which domain to register? Walk the D-08 chain in order and stop at the first available.
  </decision>
  <context>
    D-08 locks Cloudflare Registrar as the default and `caleblim.com` as the primary choice. Availability is unknown until checked. The fallback chain has 3 entries plus an alternative-vendor escape hatch (Vercel Domains, single-vendor DNS). This checkpoint determines what gets registered before any money is spent.

    Pre-checkpoint actions for the executor:
    1. Open Cloudflare Registrar at https://dash.cloudflare.com/?to=/:account/registrar (signed in as Caleb).
    2. Use the dashboard search to check availability of each domain in the D-08 order:
       - caleblim.com (primary; ~$10.46/yr at-cost per RESEARCH §Vercel domain wiring)
       - caleblim.co
       - caleb.work
       - caleblimkr.com
    3. Note the availability + listed price of each (Cloudflare's at-cost pricing means no surprises, but record what the dashboard shows).
    4. If a CLI alternative is available: `whois caleblim.com | head -10` returns NXDOMAIN-style output if available, or registration data if taken. macOS `whois` is BSD-flavored and reliable for `.com`/`.co`/`.work`.
    5. Surface the result table to Caleb in the checkpoint message.
  </context>
  <options>
    <option id="option-a">
      <name>Register caleblim.com (primary)</name>
      <pros>D-08 default; matches PROJECT.md original plan; matches the existing Vercel subdomain slug `caleb-lim-portfolio`. Recruiter-recognizable as a name domain.</pros>
      <cons>None if available.</cons>
    </option>
    <option id="option-b">
      <name>Register caleblim.co (fallback 1 — .co TLD)</name>
      <pros>If caleblim.com is taken. .co reads as Columbian ccTLD but is widely used as "company" in tech. Still names-Caleb-clean.</pros>
      <cons>Slightly higher cost (~$25-30/yr at-cost typical for .co at Cloudflare Registrar). Some recruiters might typo to .com.</cons>
    </option>
    <option id="option-c">
      <name>Register caleb.work (fallback 2 — .work TLD)</name>
      <pros>Newer-gTLD; intent-signal ("work") is portfolio-aligned. Cloudflare at-cost ~$8-12/yr typical.</pros>
      <cons>Two-string-namespace might confuse non-tech recruiters. Less brand-immediate than caleblim.*</cons>
    </option>
    <option id="option-d">
      <name>Register caleblimkr.com (fallback 3 — middle-name variant)</name>
      <pros>If all the above are taken. .com TLD restored. Adds "kr" suffix per Caleb's middle name.</pros>
      <cons>Less clean than just-the-name. Reads slightly tech-handle-y.</cons>
    </option>
    <option id="option-e">
      <name>Switch path — Vercel Domains (single-vendor; D-08 alternative)</name>
      <pros>Slightly more expensive (~$15/yr for .com) but eliminates the Cloudflare→Vercel DNS-pointer step. Vercel dashboard auto-configures DNS records on registration. Useful if Caleb prefers single-vendor.</pros>
      <cons>Locks the registrar into Vercel — migrating away later is non-trivial. Cloudflare Registrar is the documented default per PROJECT.md.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (caleblim.com), option-b (caleblim.co), option-c (caleb.work), option-d (caleblimkr.com), or option-e (Vercel Domains path). The selected domain is what Task 2-5 wire up.</resume-signal>
  <done>
    Caleb's domain choice is locked. Executor proceeds to register it via the chosen registrar's UI. The remainder of the plan uses `<registered-domain>` as a placeholder for whichever domain was selected.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Caleb registers the selected domain at Cloudflare Registrar (or Vercel Domains if option-e)</name>
  <files></files>
  <action>Orchestrator reads the steps verbatim to Caleb (or leaves in the checkpoint message), then waits for "registered <domain>" via resume-signal. No CLI/API path — Cloudflare registrar transactions require dashboard session + payment method.</action>
  <verify><human-check>Caleb confirms registration via resume-signal; orchestrator captures the date + registrar account for 06-VERIFICATION.md Domain Availability Check Log fill in Task 6.</human-check></verify>
  <what-needed>
    Domain registration is an external-service transaction that requires Caleb's signed-in dashboard session + payment method on file. Claude cannot perform this step via CLI/API in this environment (Cloudflare's registrar-domain-register API requires an API token scoped to registrar — not configured in this repo per RESEARCH §Environment Availability "whois ?" entry, and the dashboard flow is the canonical path per Vercel + Cloudflare migration guides).

    Steps for Caleb (executor reads these to Caleb verbatim if Caleb is present at the checkpoint; otherwise leaves the steps in the resume signal):
    1. Open the chosen registrar's UI (Cloudflare Registrar at https://dash.cloudflare.com/?to=/:account/registrar OR Vercel Domains at vercel.com).
    2. Search for the domain selected in Task 1.
    3. Click Register / Add Domain. Confirm at-cost pricing (no upsells). Select 1-year registration.
    4. Auto-renewal: Caleb's choice — recommend ON (avoids accidental expiry).
    5. Privacy: ON by default at Cloudflare Registrar (free); ON at Vercel Domains (paid; check the cost).
    6. Complete the purchase. The domain's DNS zone defaults to Cloudflare nameservers (if Cloudflare Registrar) or Vercel-managed DNS (if Vercel Domains).
    7. Confirm registration success — Caleb sees the domain in the registrar's dashboard.
  </what-needed>
  <resume-signal>Type "registered" plus the full domain name (e.g. "registered caleblim.com") once Caleb sees the domain in the registrar dashboard. If registration failed for any reason (payment declined, domain became unavailable between Task 1 check and Task 2 purchase, etc.), describe the failure and the executor will re-loop through Task 1's fallback chain.</resume-signal>
  <done>
    Domain is registered to Caleb's account. Registration date + registrar account is captured in 06-VERIFICATION.md Domain Availability Check Log (executor fills the row in Task 7).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Caleb adds Vercel DNS records at Cloudflare with DNS-only proxy mode</name>
  <files></files>
  <action>Orchestrator reads Vercel's required DNS records (apex A + www CNAME from the Vercel dashboard) to Caleb, watches Caleb add them in Cloudflare DNS panel with DNS-only proxy mode (grey cloud — RESEARCH §Pitfall 1), and waits for "dns set" + dig output via resume-signal.</action>
  <verify><human-check>Caleb returns "dns set" plus `dig +short <domain> @1.1.1.1` output via resume-signal; orchestrator confirms the resolved IP matches the Vercel-supplied IP.</human-check></verify>
  <what-built>
    Domain is registered (Task 2). Vercel dashboard needs the apex A record + www CNAME pointed at it before SSL can provision. The CRITICAL pitfall per RESEARCH §Pitfall 1: if the apex A record is set to Cloudflare "Proxied" (orange cloud), Vercel's auto-HTTPS termination collides with Cloudflare's SSL termination → redirect loops or 525 errors. The apex A record MUST be set to "DNS only" (grey cloud).

    Pre-checkpoint actions for the executor:
    1. Open Vercel dashboard at https://vercel.com/c-lb/caleb-lim-portfolio/settings/domains (signed in as Caleb).
    2. Click "Add Domain". Enter the registered domain. Vercel shows "Invalid Configuration" + the required DNS records:
       - Apex: A record `@` → an IP from the `76.76.21.x` Anycast pool. **Record the exact IP Vercel shows** (it may not be `76.76.21.21`; the Anycast pool varies per RESEARCH §Assumptions Log A1).
       - www subdomain: CNAME `www` → `cname.vercel-dns.com`.
    3. Open Cloudflare dashboard → Domains → <registered-domain> → DNS → Records.
    4. Click "Add record" twice — one for the A record, one for the CNAME. Use the EXACT values Vercel surfaced in step 2.
    5. **For BOTH records, set Proxy status to "DNS only" (grey cloud icon)** — per RESEARCH §Pitfall 1, NOT Proxied. The toggle is in the proxy-status column of the record row; default is orange (Proxied); switch to grey before saving.
    6. Save both records.
  </what-built>
  <how-to-verify>
    Caleb confirms three things visually:
    1. Cloudflare DNS panel shows: A record `@` → <IP from Vercel> with proxy status **DNS only (grey cloud)**.
    2. Cloudflare DNS panel shows: CNAME `www` → `cname.vercel-dns.com` with proxy status **DNS only (grey cloud)**.
    3. Both records show their TTL (typically Auto).

    Wall-clock waiting: DNS propagation can take 5min – 48hr (RESEARCH §Pitfall 4). Run from a terminal: `dig +short <registered-domain> @1.1.1.1` and `dig +short <registered-domain> @8.8.8.8`. Both should return the Vercel A IP within 5-15min on Cloudflare-Registrar zones (Cloudflare's own anycast resolver is fastest to update for Cloudflare-registered domains). If after 30min the dig still returns NXDOMAIN, re-check the records in Cloudflare dashboard (records sometimes save with a typo'd subdomain; the apex must be `@`, not blank).

    Common typos to catch in this checkpoint:
    - A record name field is blank or `<domain>.com` instead of `@` (Cloudflare interprets `@` as apex; some users type the domain name)
    - CNAME name is `www.<domain>.com` instead of `www`
    - Proxy left orange (the cause of RESEARCH §Pitfall 1's redirect loop)
  </how-to-verify>
  <resume-signal>Type "dns set" once both records are visible in Cloudflare DNS with grey-cloud proxy status. Include the `dig +short <domain> @1.1.1.1` output (the resolved A IP) so executor can verify it matches what Vercel expects. If something looks wrong (orange cloud, typo'd record, dig still returning NXDOMAIN after 30min) describe and the executor loops on diagnosis.</resume-signal>
  <done>
    Both DNS records are live with grey-cloud proxy. dig confirms propagation. Executor proceeds to Task 4 (Vercel "Valid Configuration" wait).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify Vercel "Valid Configuration" + SSL provisioned</name>
  <files></files>
  <action>Orchestrator waits for Vercel domain status to flip Invalid → Valid → SSL Active (1-15min total); on Caleb's "https green" signal, runs `curl -vI https://<domain> 2>&1 | grep -E 'subject:|issuer:|HTTP/'` to verify the Let's Encrypt cert chain.</action>
  <verify><human-check>Caleb returns "https green" via resume-signal with the curl-grep output included; orchestrator confirms subject CN matches the domain and issuer is Let's Encrypt.</human-check></verify>
  <what-built>
    DNS is propagated (Task 3). Vercel's polling loop should detect the records within ~60s of propagation. The domain status in Vercel flips from "Invalid Configuration" → "Valid Configuration" → SSL provisions Let's Encrypt cert (1-5min per RESEARCH §Pitfall 4 + A5). This checkpoint is the wait + visual confirmation.

    Pre-checkpoint actions for the executor:
    1. Refresh the Vercel domain settings page at https://vercel.com/c-lb/caleb-lim-portfolio/settings/domains.
    2. Watch the domain's status indicator. Initially "Invalid Configuration" (yellow/red). After DNS propagates (~1-15min total), it flips to "Valid Configuration" (green checkmark).
    3. Once "Valid Configuration", Vercel auto-triggers Let's Encrypt cert provisioning. UI usually shows "Provisioning certificate..." for ~1-5min, then "SSL: Active" or similar.
  </what-built>
  <how-to-verify>
    Caleb confirms in the Vercel dashboard:
    1. Domain status = green/checkmark "Valid Configuration"
    2. SSL status = Active / Provisioned (no "Provisioning..." pending, no error)
    3. Both apex (`<domain>`) and www (`www.<domain>`) show the same green state — Vercel handles both as the same project, www typically auto-redirects to apex (or vice versa — Vercel's default).

    Then executor runs from terminal: `curl -vI https://<registered-domain> 2>&1 | grep -E 'subject:|issuer:|HTTP/'`. Expected output:
    - `HTTP/2 200` (or HTTP/2 301 if www-redirect — depends on Vercel default)
    - `subject: CN=<registered-domain>` (or matches some Vercel managed cert SAN list)
    - `issuer: ... CN=R3 ... O=Let's Encrypt` (or current Let's Encrypt issuer name; check `curl -vI` output for the actual issuer string and verify it contains "Let's Encrypt")

    The cert chain MUST verify cleanly — `curl` exit code 0 on the https request. If it returns SSL errors (525, 526, "self-signed certificate" warnings), something's wrong — check Cloudflare proxy status (must be grey) and Vercel SSL state.

    Note: If you happen to be using a `?` browser/network with strict SSL filtering, the curl above is the canonical check. Don't rely on `https://<domain>` rendering in a browser as the SSL check — browsers cache SSL errors and may show stale states.
  </how-to-verify>
  <resume-signal>Type "https green" plus the `curl -vI` output (just the 3 grep'd lines — subject, issuer, HTTP status) once the Vercel domain shows "Valid Configuration" + Active SSL and curl confirms the cert chain. If SSL provisioning gets stuck >30min, describe + executor diagnoses (most common cause: Cloudflare proxy still orange — back to Task 3 to fix).</resume-signal>
  <done>
    Custom domain serves HTTPS with valid Let's Encrypt cert. Executor proceeds to Task 5 (the 2-step flip).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Flip astro.config.mjs:site + public/robots.txt Sitemap line to the custom domain</name>
  <files>astro.config.mjs, public/robots.txt</files>
  <read_first>
    - astro.config.mjs (post-Plan-06-02 state; the `site: 'https://caleb-lim-portfolio.vercel.app'` line is the target of the flip)
    - public/robots.txt (post-Plan-06-02 state; the `Sitemap: https://caleb-lim-portfolio.vercel.app/sitemap-index.xml` line is the target of the flip)
    - .planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md §Pitfall 2 (the 2-step flip is the whole point of this task)
    - .planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md §public/robots.txt — Wave 2 amendment ("swap the host segment to https://caleblim.com (or D-08 fallback) after DNS verifies")
  </read_first>
  <behavior>
    - astro.config.mjs `site:` value is now `'https://<registered-domain>'` (e.g. `'https://caleblim.com'`).
    - astro.config.mjs comment above `site:` is updated — the prior Wave 1 comment said "Vercel default subdomain"; replace with a 1-line comment naming the flipped state + the Plan 06-07 reference: `// Phase 6 Wave 2 (Plan 06-07) — flipped to custom domain on <date>. Was https://caleb-lim-portfolio.vercel.app (Wave 1 placeholder).`
    - public/robots.txt Sitemap host segment is flipped in lockstep: `Sitemap: https://<registered-domain>/sitemap-index.xml`.
    - `npm run build` succeeds. `dist/sitemap-0.xml` now contains `<loc>https://<registered-domain>/...</loc>` entries (NOT the Vercel default subdomain).
    - `dist/index.html` shows `og:image` content attribute pointing at `https://<registered-domain>/og.png` (absolute URL with the flipped host).
    - `dist/robots.txt` Sitemap line matches astro.config.mjs:site.
    - Gates 1-25 + 26a + 26b + 26c + 27 still GREEN.
  </behavior>
  <action>
    Two file edits, single commit.

    Edit 1 — `astro.config.mjs`: locate the `site: 'https://caleb-lim-portfolio.vercel.app'` line and the 2-line comment above it (Plan 06-02 set both). Replace the URL with the custom-domain URL (per Task 1's selected option). Update the comment: instead of "Wave 1 — Vercel default subdomain; Wave 2 flips...", write a single-line comment naming the actual flipped state + the date + the prior state (preserves audit trail per the same pattern as Plan 06-06's CLAUDE.md amendments).

    Edit 2 — `public/robots.txt`: replace `https://caleb-lim-portfolio.vercel.app` with `https://<registered-domain>` on the `Sitemap:` line. The other 3 lines (User-agent, Allow, blank) are unchanged.

    Commit both edits as a single commit named `feat(phase-6/06-07): flip site URL to <registered-domain>`. This atomic commit triggers a Vercel webhook → build → deploy with the new URLs baked in.

    After commit, push to main. Vercel deploys within ~30s (per Plan 05-02's Vercel push-trigger verification). Wait for the deploy to complete (Vercel dashboard shows the new commit's deploy as "Ready").

    Then verify end-to-end:
    - `curl -s https://<registered-domain>/sitemap-index.xml | head -20` shows the sitemap-index points at `sitemap-0.xml`.
    - `curl -s https://<registered-domain>/sitemap-0.xml | head -20` shows `<loc>https://<registered-domain>/...</loc>` URLs (NOT the Vercel default subdomain).
    - `curl -s https://<registered-domain>/robots.txt` shows the flipped Sitemap line.
    - `curl -s https://<registered-domain>/ | grep -F 'og:image'` shows the og:image content pointing at `https://<registered-domain>/og.png`.

    Run `bash scripts/verify-build.sh` locally to confirm Gates 1-27 stay GREEN.
  </action>
  <verify>
    <automated>grep -F "site: 'https://" astro.config.mjs | grep -v 'caleb-lim-portfolio.vercel.app' &amp;&amp; grep -F 'Sitemap: https://' public/robots.txt | grep -v 'caleb-lim-portfolio.vercel.app' &amp;&amp; npm run build &amp;&amp; ! grep -q 'caleb-lim-portfolio.vercel.app' dist/sitemap-0.xml &amp;&amp; ! grep -q 'caleb-lim-portfolio.vercel.app' dist/robots.txt &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/06-07-t5.log &amp;&amp; grep -F 'OK: Gate 25' /tmp/06-07-t5.log &amp;&amp; grep -F 'OK: Gate 26b' /tmp/06-07-t5.log &amp;&amp; grep -F 'OK: Gate 26c' /tmp/06-07-t5.log &amp;&amp; grep -F 'OK: Gate 27' /tmp/06-07-t5.log</automated>
  </verify>
  <acceptance_criteria>
    - `astro.config.mjs` `site:` value does NOT contain `caleb-lim-portfolio.vercel.app` substring (i.e. the flip happened).
    - `astro.config.mjs` `site:` value uses the chosen domain from Task 1.
    - `public/robots.txt` `Sitemap:` line host matches `astro.config.mjs:site`.
    - `npm run build` exits 0.
    - `dist/sitemap-0.xml` contains the custom-domain host in `<loc>` entries (NOT the Vercel default subdomain).
    - `dist/robots.txt` matches the source robots.txt (Astro copies verbatim).
    - Gates 1-27 all GREEN.
    - Commit is pushed to main; Vercel auto-deploys; `curl -s https://<domain>/sitemap-index.xml` returns 200 with the flipped URL.
  </acceptance_criteria>
  <done>
    2-step site flip is complete. Custom domain is live with correct sitemap, robots, OG URLs. Build pipeline rebuilt cleanly. Gates green. Commits cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 6: Record outcome in 06-VERIFICATION.md + amend REQUIREMENTS.md FOUND-04 closure</name>
  <files>.planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md (Wave 0 scaffold from Plan 06-01 — Domain Availability Check Log section has empty rows pre-seeded with caleblim.com + the 3 fallbacks)
    - .planning/REQUIREMENTS.md FOUND-02 / FOUND-03 closure rows (lines 133-134 — `Complete (Plan XX-YY ...)` annotation pattern)
  </read_first>
  <behavior>
    - 06-VERIFICATION.md Domain Availability Check Log table has actual outcome rows filled: which domain was checked, which was available, which was registered, on what date.
    - 06-VERIFICATION.md SC1 sign-off row in the Phase Exit SC sign-off table is updated with: ✅ <date>, Evidence: "DNS verified via dig; SSL chain verified via curl -vI; custom domain serving HTTPS — see Domain Availability Check Log + Cert-Chain Verification rows below."
    - 06-VERIFICATION.md gets a new sub-section "Cert-Chain Verification" with the `curl -vI` output (subject, issuer, HTTP status) recorded as a code block.
    - REQUIREMENTS.md FOUND-04 row line 135 traceability column changes from `Pending` to `Complete (Plan 06-07, <date>)` matching FOUND-02 / FOUND-03's annotation style.
    - REQUIREMENTS.md FOUND-04 inline description (line 48) is unchanged — the row is amended on closure, not the requirement itself.
  </behavior>
  <action>
    Edit 1 — `.planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md`:

    Fill the Domain Availability Check Log section. For each row in the table (caleblim.com primary + 3 fallbacks), record what was found during Task 1 (Available / Taken) and which was Registered (Yes for whichever Task 2 picked). Include a Notes column with any per-row remark (pricing, why fallback skipped, etc.).

    Update the SC1 row in the SC sign-off table at the bottom: change Sign-off column from `TBD` to `✅ <today's date>`. Change Evidence column from `TBD` to: `DNS verified via dig +short <domain> @1.1.1.1; SSL chain verified via curl -vI; Vercel dashboard shows "Valid Configuration"; see Domain Availability Check Log + Cert-Chain Verification.`

    Add a new sub-section before SC sign-off (after the existing Lighthouse Scores section), titled `## Cert-Chain Verification`. Inside, paste the `curl -vI` output (just the 3 grep'd lines — subject, issuer, HTTP/2 status) into a fenced code block. Header note: `Verified <date> against https://<registered-domain> from local terminal (executor's machine). Vercel dashboard separately confirmed SSL = Active.`

    Edit 2 — `.planning/REQUIREMENTS.md`:

    Find the FOUND-04 row in the Traceability table (line 135 per current file state). Change `Pending` to `Complete (Plan 06-07, <today's date>)` matching the FOUND-02 closure annotation style (line 133: `Complete (Plan 05-09 Lighthouse re-audit)`).

    Optional: if FOUND-04's description line (line 48) names the domain target with phrases like "subject to availability check at deploy time", consider whether to add an inline amendment annotation naming the actually-registered domain. Per the FOUND-03 amendment pattern (line 47, the 2026-05-18 amendment), inline annotations preserve audit trail; if the registered domain is the primary caleblim.com, no annotation needed; if a fallback was registered (option-b through option-e from Task 1), add a `(Registered: <actual-domain>, 2026-MM-DD per D-08 fallback)` annotation at the end of FOUND-04's prose. This makes future grep-for-the-actual-domain reliable.

    Run `bash scripts/verify-build.sh` — unchanged-pass state.
  </action>
  <verify>
    <automated>grep -F 'Domain Availability Check Log' .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md &amp;&amp; grep -F 'Cert-Chain Verification' .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md &amp;&amp; grep -E '^\| FOUND-04 \| Phase 6 \| Complete' .planning/REQUIREMENTS.md &amp;&amp; ! grep -E '^\| FOUND-04 \| Phase 6 \| Pending' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - 06-VERIFICATION.md Domain Availability Check Log table is filled with 4 outcome rows (caleblim.com + 3 fallbacks) showing Available/Taken status and which was Registered.
    - 06-VERIFICATION.md has a new "Cert-Chain Verification" subsection containing the curl -vI output (subject + issuer + HTTP status).
    - 06-VERIFICATION.md SC1 sign-off row shows ✅ <date> in the Sign-off column and named evidence in the Evidence column.
    - REQUIREMENTS.md FOUND-04 row in the Traceability table reads `Complete (Plan 06-07, <date>)`, NOT `Pending`.
    - `bash scripts/verify-build.sh` keeps all gates GREEN.
  </acceptance_criteria>
  <done>
    Domain outcome recorded in the canonical phase-exit doc. FOUND-04 traceability is closed in REQUIREMENTS.md. Phase 6 is one Wave 3 plan away from milestone v1.0. Commits cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare Registrar account → DNS zone | Caleb's account is the source of truth for domain ownership; MFA-protected at Cloudflare. |
| Cloudflare DNS records → Vercel CDN | Apex A + www CNAME map our domain to Vercel's Edge. DNS-only proxy mode (grey cloud) ensures Vercel's SSL stack is the only TLS terminator. |
| Vercel SSL provisioning → Let's Encrypt | Vercel auto-provisions and auto-renews. No manual cert management. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Spoofing | Domain hijack via Cloudflare Registrar account takeover | mitigate | Caleb's Cloudflare account MUST have MFA enabled. Surface this as a recommendation during the Task 2 checkpoint if not already set. Domain auto-renewal ON to prevent accidental expiry-then-squat. |
| T-06-18 | Tampering | DNS record tampering | mitigate | Cloudflare DNSSEC enabled by default on Cloudflare-registered domains. No additional action required, but Caleb can verify in the Cloudflare DNS panel that DNSSEC shows "Active" post-registration. |
| T-06-19 | Information Disclosure | WHOIS privacy | accept | Cloudflare Registrar provides WHOIS privacy free by default. Domain owner contact is masked. |
| T-06-20 | Denial of Service | DNS propagation delays | accept | DNS propagation is asynchronous; mitigation is the wait + dig verification at Task 3. No additional defense possible — this is the nature of DNS. |
| T-06-21 | Elevation of Privilege | Vercel deployment hooks executed by GitHub push | accept | Vercel's webhook contract is OAuth-bound to Caleb's GitHub identity. Anyone pushing to main is implicitly authorized to deploy. This is the FOUND-04 maintenance pitfall the dry run validates (Wave 3) — pushing to main IS the deploy authorization, intentional. |

ASVS Level 1 covers HTTPS cert chain verification (curl -vI in Task 4); DNS hardening (DNSSEC + DNS-only proxy mode at Task 3); and authentication for Cloudflare + Vercel dashboards (MFA recommendation in T-06-17 mitigation).
</threat_model>

<verification>
- `dig +short <registered-domain> @1.1.1.1` returns the Vercel A IP.
- `dig +short www.<registered-domain> @1.1.1.1` returns either a CNAME or the resolved A from the CNAME.
- `curl -vI https://<registered-domain> 2>&1 | grep -E 'subject:|issuer:|HTTP/'` returns HTTP/2 200, subject CN matches the domain, issuer matches Let's Encrypt.
- Vercel dashboard shows "Valid Configuration" + Active SSL for the custom domain.
- `npm run build && bash scripts/verify-build.sh` keeps Gates 1-27 GREEN.
- `cat dist/sitemap-0.xml | head -10` shows `<loc>https://<registered-domain>/...</loc>` entries (no Vercel default subdomain references).
- `cat dist/robots.txt` shows the flipped Sitemap line.
- After commit + push, `curl -s https://<registered-domain>/` returns 200 with the full site rendered.
- 06-VERIFICATION.md Domain Availability Check Log + Cert-Chain Verification sections are filled.
- REQUIREMENTS.md FOUND-04 traceability row says `Complete (Plan 06-07, <date>)`.
</verification>

<success_criteria>
FOUND-04 closed. Site is live on a custom domain with valid HTTPS. SC1 (HTTPS cert chain + DNS resolution) closed. 2-step site flip atomically rebuilds OG URLs, canonical links, and sitemap entries to point at the custom domain. Wave 3 (Plan 06-08) is the last plan before milestone v1.0 ships.
</success_criteria>

<output>
Create `.planning/phases/06-deploy-maintenance-handoff/06-07-SUMMARY.md`. Note:
- Which domain was registered (Task 1 outcome) and which fallback (if any) was used.
- The actual Vercel A IP — record for future plan reference (A1 [ASSUMED] is now resolved to a concrete value).
- DNS propagation duration (wall-clock from Task 3 set-records to Task 4 Vercel "Valid Configuration").
- SSL provisioning duration (Vercel "Valid Configuration" to "Active SSL").
- Any pitfalls encountered (proxy-mode toggle catches, dig delays, etc.).
- Vercel SSL cert details (subject CN, issuer, expiry — Let's Encrypt 90-day rotation, Vercel auto-renews).
- Pointer to Wave 3 Plan 06-08 for the Caleb dry run + README population + cross-browser smoke + phase-exit Lighthouse re-audit.
</output>
