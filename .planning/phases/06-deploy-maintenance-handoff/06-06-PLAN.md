---
phase: 06-deploy-maintenance-handoff
plan: 06
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements: []
tags: [phase-6, wave-1, slice-e, doc-rot, d-07, amendment, vercel]

must_haves:
  truths:
    - "CLAUDE.md line 12 has a Vercel amendment annotation following the 2026-05-18 FOUND-03 amendment pattern: `Deployed to **Cloudflare Pages** (free tier, unlimited bandwidth)` is replaced with `Deployed to **Vercel Hobby** (free tier; D-13 Phase 5 amendment 2026-05-18 — was Cloudflare Pages)`"
    - "CLAUDE.md line ~75 (the inline shell comment `# Deploy via Cloudflare Pages: ...`) has the host name replaced with Vercel; the build command + output dir parts of the comment stay intact (Vercel accepts the same `npm run build` + `dist/` shape)"
    - "CLAUDE.md Hosting table at lines 60-64 has a 1-line callout prepended above it: `> Amendment 2026-05-20 (FOUND-04 D-07): Vercel selected over Cloudflare Pages per Phase 5 D-13. Table below preserved as original research; current choice is Vercel.`"
    - "The original Hosting table rows (Cloudflare Pages, Netlify, Vercel) are PRESERVED — no row deleted, no rationale rewritten (PATTERNS.md §CLAUDE.md: preserve audit trail)"
    - ".planning/PROJECT.md Constraints section gains a parallel amendment annotation pointing at D-07 and the prior Cloudflare Pages choice"
    - "Both files retain their full prior text — amendments are additive (inline notes + a callout), not replacements (per PATTERNS.md §Shared Patterns §Vercel-amendment doc style)"
    - "Phase 1-5 gates 1-25 still GREEN; Phase 6 gates from other Wave 1 plans (26b, 26c, 27) unaffected"
    - "No code files touched — this plan is documentation-only"
  artifacts:
    - path: "CLAUDE.md"
      provides: "D-07 doc-rot fix; Cloudflare Pages references annotated as superseded by Vercel; full audit trail preserved"
    - path: ".planning/PROJECT.md"
      provides: "Parallel Constraints-section amendment; same audit-trail-preserving pattern"
  key_links:
    - from: "D-07 (CONTEXT.md, 2026-05-20)"
      to: "ROADMAP.md §Phase 6 + REQUIREMENTS.md FOUND-04 (already amended 2026-05-20 per CONTEXT commit)"
      via: "doc-amendment cascade — CLAUDE.md + PROJECT.md were missed in the original CONTEXT commit"
      pattern: "Vercel"
    - from: "Amendment pattern"
      to: "2026-05-18 FOUND-03 amendment in REQUIREMENTS.md line 47"
      via: "inline annotation + date + decision-ref (per PATTERNS.md §Shared Patterns §Vercel-amendment doc style)"
      pattern: "Amendment.*2026-05-\\d{2}"
---

<objective>
Wave 1 Slice E — close the D-07 doc-rot cascade. CONTEXT commit (2026-05-20) amended ROADMAP §Phase 6 + REQUIREMENTS FOUND-04, but CLAUDE.md and PROJECT.md still reference Cloudflare Pages as the deploy target. RESEARCH §Runtime State Inventory caught this as outstanding doc-rot.

Purpose: Closes a documentation-correctness debt that propagates noise into every future planning session (PROJECT.md is the canonical project doc; CLAUDE.md is the agent's project preamble). Pattern matches the 2026-05-18 FOUND-03 amendment in REQUIREMENTS.md line 47 — inline annotation, never deletion. The full prior text stays so the audit trail is preserved.

Output: 2 doc files modified, ~6-10 lines of inline annotations added total. No code touched. No new gates needed (this is doc-state, not code-state; sufficient verification is grep).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md
@.planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md
@.planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- CLAUDE.md line 12 (PATTERNS.md §CLAUDE.md verbatim — note the bold inline reference): -->
<!-- - **Tech stack**: **Astro** with content collections (markdown + Zod schema), build-time PDF rasterization via `pdfjs-dist` + `@napi-rs/canvas`, motion via `motion` (formerly framer-motion) v12 and selective GSAP. Deployed to **Cloudflare Pages** (free tier, unlimited bandwidth). Domain via **Cloudflare Registrar** (~$10/yr). Decided after research surfaced a Framer-vs-Astro fork; owner is comfortable enough with markdown + git that no-code platform's main value disappeared, and Astro avoids the platform-lock-in pitfall. -->

<!-- Target line 12 (PATTERNS.md §CLAUDE.md pattern to apply #1 — surgical edit to the host segment only): -->
<!-- - **Tech stack**: **Astro** with content collections (markdown + Zod schema), build-time PDF rasterization via `pdfjs-dist` + `@napi-rs/canvas`, motion via `motion` (formerly framer-motion) v12 and selective GSAP. Deployed to **Vercel Hobby** (free tier; D-13 Phase 5 amendment 2026-05-18 — was Cloudflare Pages). Domain via **Cloudflare Registrar** (~$10/yr). Decided after research surfaced a Framer-vs-Astro fork; owner is comfortable enough with markdown + git that no-code platform's main value disappeared, and Astro avoids the platform-lock-in pitfall. -->

<!-- CLAUDE.md Hosting table lines 60-64 (PATTERNS.md §CLAUDE.md current code at lines 59-75 — note the 3 rows: Cloudflare Pages, Netlify, Vercel): -->
<!--   ### Hosting -->
<!--   | Choice | Cost | Why | -->
<!--   |--------|------|-----| -->
<!--   | **Cloudflare Pages** | Free | ...rationale... | -->
<!--   | Netlify | Free (100 GB) | ...rationale... | -->
<!--   | Vercel | Free for hobby | Avoid for this — Hobby's commercial-use restriction is a grey area... | -->

<!-- Target: insert a 1-line callout ABOVE the table (PATTERNS.md §CLAUDE.md pattern to apply #3): -->
<!--   > Amendment 2026-05-20 (FOUND-04 D-07): Vercel selected over Cloudflare Pages per Phase 5 D-13. Table below preserved as original research; current choice is Vercel. -->
<!--   -->
<!--   ### Hosting -->
<!--   | Choice | Cost | Why | -->
<!--   ...table unchanged... -->

<!-- CLAUDE.md line ~75 (the inline shell comment in the install instructions, PATTERNS.md §CLAUDE.md pattern to apply #2): -->
<!--   # Deploy via Cloudflare Pages: connect git repo, set build command to `npm run build`, output dir to `dist/`. -->
<!-- Target: -->
<!--   # Deploy via Vercel: connect git repo, set build command to `npm run build`, output dir to `dist/`. -->
<!-- (Astro framework preset auto-detected on Vercel; same build command, same output dir.) -->

<!-- .planning/PROJECT.md Constraints section (PATTERNS.md §.planning/PROJECT.md — identical inline-amendment style): -->
<!--   PROJECT.md content is mirrored in CLAUDE.md's <!-- GSD:project-start --> block. Look for the same "Cloudflare Pages" string in PROJECT.md's Constraints section and apply the same inline annotation. -->

<!-- Reference pattern — 2026-05-18 FOUND-03 amendment in REQUIREMENTS.md line 47: -->
<!--   FOUND-03: ... Honors prefers-reduced-motion everywhere ... **Exception (amended 2026-05-18 during /gsd-discuss-phase 5):** brief user-initiated feedback motion ... is classified as essential interaction feedback and remains active under prefers-reduced-motion. All other motion still disables ... -->
<!-- (Bold "Exception (amended YYYY-MM-DD during /gsd-discuss-phase X):" header + explanatory clause.) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend CLAUDE.md — line 12, line ~75, and Hosting table callout</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (read in full — small file; verify exact line numbers haven't drifted from PATTERNS.md capture)
    - .planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md §CLAUDE.md (3 surgical edits specified with verbatim before/after)
    - .planning/REQUIREMENTS.md FOUND-03 line 47 (canonical amendment pattern source — read the existing 2026-05-18 amendment to match its tone)
  </read_first>
  <behavior>
    - CLAUDE.md line 12 has the inline annotation: `**Vercel Hobby** (free tier; D-13 Phase 5 amendment 2026-05-18 — was Cloudflare Pages)` replacing the `**Cloudflare Pages** (free tier, unlimited bandwidth)` substring. The surrounding sentence structure (everything before and after the host clause) is unchanged.
    - The "Domain via **Cloudflare Registrar**" clause on the SAME line is unchanged — Cloudflare Registrar is the planned domain registrar per D-08; it's the **Pages** product that's stale, not Cloudflare overall.
    - CLAUDE.md line ~75 (inline shell comment) has `Cloudflare Pages` swapped to `Vercel`. The rest of that comment (build command `npm run build`, output dir `dist/`) is unchanged.
    - The Hosting table at lines 60-64 has a 1-line `> Amendment 2026-05-20 (FOUND-04 D-07): ...` callout inserted IMMEDIATELY ABOVE the `### Hosting` heading. The table itself is preserved verbatim (3 rows: Cloudflare Pages, Netlify, Vercel — all stay). This preserves the audit trail.
    - No bullet/section is deleted. No row of any table is deleted. No rationale paragraph is rewritten.
    - All edits land within the `<!-- GSD:project-start -->` ... `<!-- GSD:project-end -->` block and the `<!-- GSD:stack-start -->` ... `<!-- GSD:stack-end -->` block respectively. These delimiters MUST stay intact (they're parsed by the GSD claude.md regenerator).
    - The file remains valid markdown.
  </behavior>
  <action>
    Three surgical edits to CLAUDE.md, single commit.

    Edit 1 — Line 12 host clause: locate the substring `Deployed to **Cloudflare Pages** (free tier, unlimited bandwidth)`. Replace with `Deployed to **Vercel Hobby** (free tier; D-13 Phase 5 amendment 2026-05-18 — was Cloudflare Pages)`. Keep everything before "Deployed to" and everything after the closing `)` untouched. The surrounding tech-stack paragraph keeps its full rationale.

    Edit 2 — Inline shell-comment near line 75 (inside the `<!-- GSD:stack-start -->` block, in the install instructions). Locate the line `# Deploy via Cloudflare Pages: connect git repo, set build command to \`npm run build\`, output dir to \`dist/\`.` Replace `Cloudflare Pages` with `Vercel`. Result: `# Deploy via Vercel: connect git repo, set build command to \`npm run build\`, output dir to \`dist/\`.`. Astro framework preset is auto-detected on Vercel, so the build command + output dir parts of the comment remain accurate.

    Edit 3 — Insert the Hosting-table callout ABOVE the `### Hosting` heading (~line 59). The callout is a single line: `> Amendment 2026-05-20 (FOUND-04 D-07): Vercel selected over Cloudflare Pages per Phase 5 D-13. Table below preserved as original research; current choice is Vercel.`. Put a blank line before the callout (to separate it from the preceding section), then the callout on its own line, then a blank line, then the existing `### Hosting` heading. Do NOT modify any of the 3 table rows — all preserved verbatim per PATTERNS.md §CLAUDE.md "preserves audit trail."

    Do NOT touch the `<!-- GSD:project-start -->`, `<!-- GSD:project-end -->`, `<!-- GSD:stack-start -->`, `<!-- GSD:stack-end -->`, or any other GSD delimiter comment. These are parsed by the regenerator and breaking them silently desyncs claude-md regeneration.

    Verify the file is still valid markdown by running `npm run build` (the build script doesn't parse CLAUDE.md but it does parse the rest of the project — a build success confirms no accidental Astro-component-file collateral damage). Then run `bash scripts/verify-build.sh` — all gates unchanged.

    Sanity-check the resulting state with `grep -c 'Vercel' CLAUDE.md` (expect ≥ 3 — original Vercel row in the Hosting table + Edit 1 + Edit 2 + Edit 3 callout, so ≥ 4). And `grep -c 'Cloudflare Pages' CLAUDE.md` — the substring still appears in the original Hosting table row, in the callout naming what was superseded, and in the Edit 1 amendment trailing parenthetical — so expect ≥ 3 (the count goes UP because we ADD references, not down because we delete).
  </action>
  <verify>
    <automated>grep -F 'Vercel Hobby' CLAUDE.md &amp;&amp; grep -F 'Amendment 2026-05-20 (FOUND-04 D-07)' CLAUDE.md &amp;&amp; grep -F 'Deploy via Vercel:' CLAUDE.md &amp;&amp; grep -F 'D-13 Phase 5 amendment 2026-05-18 — was Cloudflare Pages' CLAUDE.md &amp;&amp; grep -F 'GSD:project-start' CLAUDE.md &amp;&amp; grep -F 'GSD:project-end' CLAUDE.md &amp;&amp; grep -F 'GSD:stack-start' CLAUDE.md &amp;&amp; grep -F 'GSD:stack-end' CLAUDE.md &amp;&amp; npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | grep -F 'OK: Gate 25'</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains the string `**Vercel Hobby** (free tier; D-13 Phase 5 amendment 2026-05-18 — was Cloudflare Pages)`.
    - CLAUDE.md contains the string `# Deploy via Vercel:` (the amended shell comment).
    - CLAUDE.md contains the callout `> Amendment 2026-05-20 (FOUND-04 D-07): Vercel selected over Cloudflare Pages per Phase 5 D-13. Table below preserved as original research; current choice is Vercel.`
    - All 4 GSD delimiter comments (`GSD:project-start/-end`, `GSD:stack-start/-end`) still present.
    - The Hosting table still has 3 rows (Cloudflare Pages, Netlify, Vercel) — no deletions.
    - `npm run build && bash scripts/verify-build.sh` unchanged-pass state.
  </acceptance_criteria>
  <done>
    CLAUDE.md is doc-rot-clean for D-07. Two inline annotations + one callout. Audit trail preserved. GSD delimiters intact. Commits cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Amend .planning/PROJECT.md Constraints section with the same inline pattern</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (read in full — particularly the Constraints section; this is mirrored into CLAUDE.md's GSD:project block but is the canonical source)
    - .planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md §.planning/PROJECT.md (identical inline-amendment style)
    - CLAUDE.md (post-Task-1 state — the amended line 12 wording is the pattern to mirror in PROJECT.md so the two stay in sync next time CLAUDE.md is regenerated from PROJECT.md)
  </read_first>
  <behavior>
    - PROJECT.md's Constraints section has the same surgical inline amendment as CLAUDE.md line 12 — Cloudflare Pages reference annotated as "D-13 Phase 5 amendment 2026-05-18 — was Cloudflare Pages" with Vercel Hobby as the current choice.
    - Any other PROJECT.md references to "Cloudflare Pages" (search the full file) get the same annotation pattern.
    - The full original prose is preserved; the amendment is additive.
    - PROJECT.md's other sections (Core Value, Decisions, etc.) are unchanged.
    - The file remains valid markdown.
  </behavior>
  <action>
    Open `.planning/PROJECT.md`. Locate the Constraints section (look for the heading `### Constraints` or `## Constraints` depending on the file's heading hierarchy).

    Inside Constraints, find the bullet that mentions "Cloudflare Pages" (per RESEARCH §Runtime State Inventory and PATTERNS.md §.planning/PROJECT.md, the bullet is mirrored from what CLAUDE.md line 12 says). Apply the same surgical inline edit as Task 1 Edit 1:

    - Replace `Deployed to **Cloudflare Pages** (free tier, unlimited bandwidth)` with `Deployed to **Vercel Hobby** (free tier; D-13 Phase 5 amendment 2026-05-18 — was Cloudflare Pages)`.

    Then `grep -n 'Cloudflare Pages' .planning/PROJECT.md` for any other occurrences. For each remaining hit, decide whether it's:
    - **Load-bearing prose** (a sentence whose meaning would change if rewritten without the amendment) → add an inline `(was Cloudflare Pages; superseded by Vercel per D-07 2026-05-20)` after the relevant clause, preserving the original sentence.
    - **Historical-mention prose** (a research-rationale paragraph or "we chose X over Y" sentence where the entire sentence is about the prior decision) → leave as-is. The 2026-05-18 D-13 + 2026-05-20 D-07 amendment cascade is documented in REQUIREMENTS.md FOUND-04 and ROADMAP §Phase 6; PROJECT.md doesn't need to re-litigate it in every reference.

    Per PATTERNS.md §Shared Patterns §Vercel-amendment doc style: never delete prior text. Append inline annotation. The audit trail is the point.

    If PROJECT.md mentions "Cloudflare Registrar" (the domain registrar) — that's NOT stale. D-08 keeps Cloudflare Registrar as the default. Do not touch Cloudflare Registrar references. Only Cloudflare Pages (the host product) is amended.

    After editing, run `grep -c 'D-13 Phase 5 amendment\|D-07' .planning/PROJECT.md` — expect ≥ 1 (the amendment annotation lands at least once).

    Run `npm run build && bash scripts/verify-build.sh` — both unchanged (PROJECT.md isn't built; the script doesn't parse it). Sanity-check that PROJECT.md is still valid markdown by opening it in a renderer (or just visual-inspection in an editor).
  </action>
  <verify>
    <automated>grep -F 'Vercel Hobby' .planning/PROJECT.md &amp;&amp; grep -F 'D-13 Phase 5 amendment 2026-05-18' .planning/PROJECT.md &amp;&amp; ! grep -F 'Deployed to **Cloudflare Pages** (free tier, unlimited bandwidth)' .planning/PROJECT.md &amp;&amp; grep -F 'Cloudflare Registrar' .planning/PROJECT.md &amp;&amp; npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | grep -F 'OK: Gate 25'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains the string `**Vercel Hobby** (free tier; D-13 Phase 5 amendment 2026-05-18 — was Cloudflare Pages)` (mirroring CLAUDE.md line 12 post-Task-1).
    - The pre-amendment substring `Deployed to **Cloudflare Pages** (free tier, unlimited bandwidth)` no longer appears (it's been replaced with the amended form).
    - The string `Cloudflare Registrar` still appears (domain registrar is NOT amended — D-08 keeps it).
    - Any OTHER `Cloudflare Pages` references in PROJECT.md either (a) carry an inline `(was Cloudflare Pages; superseded by Vercel per D-07 2026-05-20)` annotation if load-bearing, or (b) are historical-mention prose left as-is.
    - File renders as valid markdown (no broken headings, no stranded list items).
    - `npm run build && bash scripts/verify-build.sh` unchanged-pass state.
  </acceptance_criteria>
  <done>
    PROJECT.md Constraints section is doc-rot-clean for D-07. Annotation style matches CLAUDE.md. Audit trail preserved. Commits cleanly. CLAUDE.md regenerator (when it next runs) will pull the amended PROJECT.md text into the GSD:project block, keeping the two in sync.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc files → human readers + GSD agents | CLAUDE.md is read by Claude on every Claude Code session; PROJECT.md is read by GSD workflows. Doc-rot here propagates into plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Information Disclosure | CLAUDE.md / PROJECT.md public-repo visibility | accept | Both files are committed to a public GitHub repo as part of the portfolio. They name vendor choices (Vercel, Cloudflare Registrar) and budget constraints (~$10/yr domain). None of this is sensitive — vendor names + free-tier choices are part of an open-source portfolio's normal surface area. |
| T-06-16 | Tampering | Doc-rot regression — future plan picks up the wrong vendor | mitigate | Inline amendment pattern preserves the audit trail (matches the 2026-05-18 FOUND-03 amendment that has now sat in REQUIREMENTS.md for 2 days without ambiguity). Future agents reading the file see both the original Cloudflare Pages choice + the Vercel amendment side-by-side. |

ASVS Level 1 informational. Documentation edits only. No code, no auth, no input handling, no network endpoints.
</threat_model>

<verification>
- `grep -c 'D-13 Phase 5 amendment 2026-05-18' CLAUDE.md .planning/PROJECT.md` — at least 1 hit per file.
- `grep -c 'Vercel Hobby' CLAUDE.md .planning/PROJECT.md` — at least 1 hit per file.
- `grep -F 'Amendment 2026-05-20 (FOUND-04 D-07)' CLAUDE.md` — exactly 1 hit (the Hosting-table callout).
- All 4 GSD delimiter comments in CLAUDE.md present.
- `npm run build && bash scripts/verify-build.sh` unchanged.
</verification>

<success_criteria>
D-07 doc-rot cascade closed. CLAUDE.md and PROJECT.md correctly reflect Vercel as the current host while preserving the original Cloudflare Pages research rationale. Future plans no longer cite stale infrastructure.
</success_criteria>

<output>
Create `.planning/phases/06-deploy-maintenance-handoff/06-06-SUMMARY.md`. Note:
- Exact line counts of "Cloudflare Pages" in each file before vs after (expected: count INCREASES because amendment annotations add inline references to the prior choice — preserving audit trail).
- Whether PROJECT.md had any "load-bearing" Cloudflare Pages references beyond the Constraints bullet that required additional annotation.
- Confirm GSD delimiter comments in CLAUDE.md survived intact (these are parsed by the regenerator).
- Note that the next time CLAUDE.md is regenerated from PROJECT.md (via `gsd-regenerate-claude-md` or similar), the amendments in PROJECT.md will propagate automatically.
</output>
</content>
