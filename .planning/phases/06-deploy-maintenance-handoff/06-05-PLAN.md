---
phase: 06-deploy-maintenance-handoff
plan: 05
type: execute
wave: 2
depends_on: [06-01, 06-04]
files_modified:
  - src/components/StatusPill.astro
  - src/pages/[category].astro
  - src/pages/[category]/[slug].astro
autonomous: true
requirements: []
tags: [phase-6, wave-2, slice-d, reduced-motion, d-06, d-08, foun-03, surgical]

must_haves:
  truths:
    - "src/components/StatusPill.astro:82-85 — the 4-line `@media (prefers-reduced-motion: reduce) { .pill { transition: none; } .pill:hover { transform: none; } }` block is deleted; the closing brace of `@keyframes pulse` (line 81) and the `/* D-04 StatusPill mobile shrink */` comment (line 86) remain intact"
    - "src/pages/[category].astro:140-142 — the 3-line `@media (prefers-reduced-motion: reduce) { .b-cat-back { transition: none; } }` block is deleted; the preceding comment `/* Reduced-motion (D-13) */` at line 139 is also deleted (it loses its referent); the closing brace of `.b-cat-meta strong` at line 138 stays; line 144 `@media (max-width: 900px)` stays"
    - "src/pages/[category]/[slug].astro:332-334 — the 3-line `@media (prefers-reduced-motion: reduce) { .pager-link { transition: none; } }` block is deleted; line 331 closing brace of `.pager-link:focus-visible` stays; line 336 `@media (max-width: 900px)` stays"
    - "After deletion, the StatusPill hover-scale (D-08 #12), back-pill color transition (D-08 #16), and pager-link color transition (D-08 #18) all FIRE under prefers-reduced-motion: reduce — this is the D-08 surgical-policy-compliant behavior per Plan 05-06's architecture (RESEARCH §Pattern 4)"
    - "Gate 27 (transition:none absent inside prefers-reduced-motion blocks across all three D-06 files) flips GREEN"
    - "Phase 1-5 gates 1-25 still GREEN. Specifically Gate 23 (topbar collapse), Gate 24 (gallery hero rendering) unchanged"
    - "No NEW reduced-motion block is added — the fix is pure deletion, not replacement (the absence of override IS the D-08 surgical-policy mechanism; per Plan 05-06 SUMMARY)"
    - "The detail-page hero `<Image>` widths prop addition (Plan 06-04) is NOT touched by this plan — different lines, no overlap (Plan 06-04 touches line ~102; this plan touches line 332-334)"
  artifacts:
    - path: "src/components/StatusPill.astro"
      provides: "Reduced-motion block at lines 82-85 removed; D-08 #12 hover-scale now fires"
    - path: "src/pages/[category].astro"
      provides: "Reduced-motion block at lines 139-142 (block + preceding comment) removed; D-08 #16 back-pill color fires"
    - path: "src/pages/[category]/[slug].astro"
      provides: "Reduced-motion block at lines 332-334 removed; D-08 #18 pager-link color fires"
  key_links:
    - from: "Plan 05-06 surgical reduced-motion policy"
      to: "the three exempt motions D-08 #12, #16, #18"
      via: "absence of override (per Plan 05-06 SUMMARY pattern)"
      pattern: "transition:\\s*none"
    - from: "Gate 27 in scripts/verify-build.sh"
      to: "the three files' @media blocks"
      via: "perl multi-line regex (Plan 06-01 Task 2 wired this)"
      pattern: "prefers-reduced-motion.*transition.*none"
---

<objective>
Wave 2 Slice D — surgical removal of the three per-source `transition: none` reduced-motion blocks that contradict D-08's exempt classifications #12 (StatusPill hover), #16 (back-pill color), #18 (pager-link color). Each block is 3-4 lines; total deletion is ~10 lines across 3 files.

Purpose: Closes SC6 (folded Phase 5 carryover — D-06 stragglers). Plan 05-06 made the global `*` reduced-motion clamp surgical, but three per-source mini-hammers in StatusPill.astro / [category].astro / [slug].astro remained as Out-of-Scope findings (Plan 05-06 SUMMARY §Out-of-Scope Findings) because they sat outside that plan's authorized files_modified. Each block suppresses an EXEMPT D-08 motion (color or transform-only, no spatial motion) — under D-08's surgical policy, the override should not exist. Per RESEARCH §Pattern 4 the fix is pure deletion: no replacement rule, no new selector. The absence of override IS the policy.

Output: 3 files, ~10 lines deleted total. Gate 27 flips GREEN. No new functionality added; the existing exempt motions resume their intended behavior under reduced-motion (which is the D-08 contract per Plan 05-06's SUMMARY §Decisions Made).

Real-device reduced-motion walk verification (iPhone 15 / iOS 26.4.2) is folded into Wave 3 phase-exit verification (06-08); this plan only ships the deletions and locks them via Gate 27.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md
@.planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md
@.planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md
@.planning/phases/05-mobile-performance-accessibility/05-06-SUMMARY.md
@src/components/StatusPill.astro
@src/pages/[category].astro
@src/pages/[category]/[slug].astro

<interfaces>
<!-- D-06 straggler #1 — src/components/StatusPill.astro:82-85 (PATTERNS.md §StatusPill.astro:82-85, verbatim): -->
<!--   @media (prefers-reduced-motion: reduce) { -->
<!--     .pill { transition: none; } -->
<!--     .pill:hover { transform: none; } -->
<!--   } -->
<!-- Surrounding context: -->
<!--   line 81: } (closing @keyframes pulse) -->
<!--   line 82: @media (prefers-reduced-motion: reduce) {     ← delete -->
<!--   line 83:   .pill { transition: none; }                ← delete -->
<!--   line 84:   .pill:hover { transform: none; }           ← delete -->
<!--   line 85: }                                            ← delete -->
<!--   line 86: /* D-04 StatusPill mobile shrink */          ← keep -->

<!-- D-06 straggler #2 — src/pages/[category].astro:139-142 (PATTERNS.md §[category].astro:140-142): -->
<!--   line 138: } (closing .b-cat-meta strong)               ← keep -->
<!--   line 139: /* Reduced-motion (D-13) */                  ← delete (loses referent) -->
<!--   line 140: @media (prefers-reduced-motion: reduce) {    ← delete -->
<!--   line 141:   .b-cat-back { transition: none; }          ← delete -->
<!--   line 142: }                                            ← delete -->
<!--   line 143: (blank line, may or may not exist)           ← keep if present -->
<!--   line 144: @media (max-width: 900px) { ... }            ← keep -->

<!-- D-06 straggler #3 — src/pages/[category]/[slug].astro:332-334 (PATTERNS.md §[slug].astro:332-334): -->
<!--   line 331: } (closing .pager-link:focus-visible)         ← keep -->
<!--   line 332: @media (prefers-reduced-motion: reduce) {     ← delete -->
<!--   line 333:   .pager-link { transition: none; }           ← delete -->
<!--   line 334: }                                             ← delete -->
<!--   line 335: (blank line, may or may not exist)            ← keep if present -->
<!--   line 336: @media (max-width: 900px) { ... }             ← keep -->

<!-- D-08 exempt classifications (from FOUND-03 amendment 2026-05-18 + Plan 05-06 SUMMARY motion-by-motion table): -->
<!--   #12 StatusPill hover-scale → user-initiated feedback motion, exempt -->
<!--   #16 back-pill color → color-only transition (no spatial motion), exempt -->
<!--   #18 pager-link color → color-only, exempt -->

<!-- Why removal doesn't regress Phase 5 a11y (RESEARCH §Pattern 4): -->
<!--   These blocks suppress color/transform-only motion; D-08 explicitly classifies them as essential interaction feedback. -->
<!--   No scroll-driven animation, no autoplay, no decorative rotation — those are JS-gated by separate suppressors that this plan does not touch. -->
<!--   Plan 05-06's two per-source `animation: none` disables (b-card, b-bio entrance shakes) remain intact. -->
<!--   The carousel slide-transition guard at index.astro:204-206 remains intact. -->
<!--   The .skip link guard at Base.astro:153-155 remains intact. -->

<!-- Plan 06-04 overlap check: Plan 06-04 touches [slug].astro line 102; this plan touches [slug].astro line 332-334. No line conflict. Same-wave plans with overlapping files but non-overlapping lines are allowed; commits land sequentially within the wave but the two plans can be drafted and reviewed in parallel. -->
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete the three D-06 transition:none stragglers in lockstep</name>
  <files>src/components/StatusPill.astro, src/pages/[category].astro, src/pages/[category]/[slug].astro</files>
  <read_first>
    - src/components/StatusPill.astro (lines 78-90 — verify the @media block is at lines 82-85 per PATTERNS.md; if drift, locate the SECOND `prefers-reduced-motion` block in the file by grep)
    - src/pages/[category].astro (lines 135-148 — verify lines 139-142 per PATTERNS.md; if drift, locate by grep)
    - src/pages/[category]/[slug].astro (lines 328-340 — verify lines 332-334 per PATTERNS.md; if drift, locate by grep)
    - .planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md §StatusPill.astro:82-85 + §[category].astro:140-142 + §[slug].astro:332-334 (exact deletion specs)
    - .planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md §Pattern 4 (D-06 straggler verified locations + why-removing-them-doesn't-regress rationale)
    - .planning/phases/05-mobile-performance-accessibility/05-06-SUMMARY.md §Out-of-Scope Findings (the three blocks were deferred to a future plan; this plan is that future plan)
  </read_first>
  <behavior>
    - StatusPill.astro: the 4-line `@media (prefers-reduced-motion: reduce) { .pill { transition: none; } .pill:hover { transform: none; } }` block at lines 82-85 is gone. Line 81 (closing brace of @keyframes pulse) and line 86 (`/* D-04 StatusPill mobile shrink */` comment) are intact.
    - [category].astro: lines 139-142 are gone (the preceding `/* Reduced-motion (D-13) */` comment AND the @media block, since the comment loses its referent and would dangle). Line 138 closing brace of `.b-cat-meta strong` stays. Line 144 `@media (max-width: 900px)` stays.
    - [slug].astro: lines 332-334 (@media block) are gone. Line 331 closing brace of `.pager-link:focus-visible` stays. Line 336 `@media (max-width: 900px)` stays.
    - No NEW reduced-motion override is added in any of the three files. The fix is pure deletion (per RESEARCH §Pattern 4 — "the removal IS the fix").
    - `bash scripts/verify-build.sh` reports `OK: Gate 27` for ALL THREE files.
    - Gates 1-25 still GREEN. Gate 23 (topbar collapse) and Gate 24 (gallery hero) specifically remain GREEN (those gates verify markup, not motion suppression).
    - `npm run build` succeeds. Visual smoke at `npm run preview` shows the site still renders.
  </behavior>
  <action>
    Three surgical deletions, one per file. Do them in this order, committing all three in a single commit (the three deletions are causally coupled — Gate 27 flips GREEN only when all three are removed).

    File 1 — `src/components/StatusPill.astro`:
    Locate the `@media (prefers-reduced-motion: reduce)` block at lines 82-85 (search the file for `prefers-reduced-motion` to confirm position; PATTERNS.md captures it as the SECOND prefers-reduced-motion block in the file). Delete lines 82-85 inclusive (4 lines: the `@media` opener, the `.pill { transition: none; }` declaration, the `.pill:hover { transform: none; }` declaration, the closing brace). Line 81 (closing `}` of `@keyframes pulse`) and line 86 (the `/* D-04 StatusPill mobile shrink */` comment) MUST remain.

    File 2 — `src/pages/[category].astro`:
    Locate the same kind of block at lines 139-142 (preceded by a `/* Reduced-motion (D-13) */` comment at line 139). Per PATTERNS.md §[category].astro the comment ALSO must go — it would dangle and confuse future readers. Delete lines 139-142 inclusive (4 lines: the comment, the `@media` opener, the `.b-cat-back { transition: none; }` declaration, the closing brace). Line 138 (closing brace of `.b-cat-meta strong`) and line 144 (`@media (max-width: 900px) {...}`) MUST remain.

    File 3 — `src/pages/[category]/[slug].astro`:
    Locate the block at lines 332-334. There is no preceding comment on this one (PATTERNS.md §[slug].astro:332-334). Delete lines 332-334 inclusive (3 lines: the `@media` opener, the `.pager-link { transition: none; }` declaration, the closing brace). Line 331 (closing brace of `.pager-link:focus-visible`) and line 336 (`@media (max-width: 900px) {...}`) MUST remain.

    Do NOT add any replacement rules. Do NOT add a comment in any of the three files saying "removed for D-06" — Plan 05-06's SUMMARY pattern is "the absence of override IS the policy". Plan 06-05's SUMMARY records the deletion; the source files just have the override gone.

    Verify the surrounding context survived each edit by reading 5 lines above and 5 lines below each deletion. The braces should match — if any unclosed/unmatched brace appears, the edit went wrong.

    After all three deletions, run `npm run build` — must succeed. Run `bash scripts/verify-build.sh 2>&1 | tee /tmp/06-05-t1.log`. Expect `OK: Gate 27` (the perl multi-line slurp in Gate 27 will check all three files; if any file still has the offending block, that file's line in the log will show FAIL). Gates 1-25 must remain GREEN.

    Visual smoke: run `npm run preview` and walk through `/`, `/design`, `/design/design-real-piece`, `/about` — confirm the site still renders. Reduced-motion verification (toggling iOS Reduce Motion and walking the three exempt motions) is Wave 3 phase-exit territory.

    Plan 06-04 overlap: Plan 06-04 also touches `[slug].astro` but at line 102 (the detail-hero `<Image>`). This plan touches line 332-334 (the pager-link). The two edits do not conflict on lines. Same-wave but commits land sequentially — whichever plan executes second will see the other's edit in the file, and that's fine. If both commits land cleanly the file ends with both edits.
  </action>
  <verify>
    <automated>! perl -0777 -ne 'exit 1 if m{\@media\s*\(\s*prefers-reduced-motion:\s*reduce\s*\)\s*\{[^}]*transition:\s*none}s' src/components/StatusPill.astro &amp;&amp; ! perl -0777 -ne 'exit 1 if m{\@media\s*\(\s*prefers-reduced-motion:\s*reduce\s*\)\s*\{[^}]*transition:\s*none}s' src/pages/\[category\].astro &amp;&amp; ! perl -0777 -ne 'exit 1 if m{\@media\s*\(\s*prefers-reduced-motion:\s*reduce\s*\)\s*\{[^}]*transition:\s*none}s' src/pages/\[category\]/\[slug\].astro &amp;&amp; npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/06-05-t1.log &amp;&amp; grep -F 'OK: Gate 27' /tmp/06-05-t1.log &amp;&amp; grep -F 'OK: Gate 23' /tmp/06-05-t1.log &amp;&amp; grep -F 'OK: Gate 24' /tmp/06-05-t1.log &amp;&amp; grep -F 'OK: Gate 25' /tmp/06-05-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `perl -0777 -ne 'exit 1 if m{\@media\s*\(\s*prefers-reduced-motion:\s*reduce\s*\)\s*\{[^}]*transition:\s*none}s' "$f"` exits 0 (i.e. no match) for each of the three files.
    - The `/* Reduced-motion (D-13) */` comment in `[category].astro` (line 139 pre-edit) is also gone (it loses its referent).
    - The `/* D-04 StatusPill mobile shrink */` comment in `StatusPill.astro` (line 86 pre-edit) IS preserved (still in the file).
    - `npm run build` exits 0.
    - `bash scripts/verify-build.sh` reports `OK: Gate 27`. Gates 1-25 stay GREEN.
    - No new `@media` block added in any of the three files (verify by counting `@media` occurrences before vs after the edit — should be exactly one fewer per file).
    - Visual smoke at `npm run preview` shows the site renders.
  </acceptance_criteria>
  <done>
    Three D-06 stragglers deleted. Gate 27 GREEN. D-08 exempt motions (#12 StatusPill hover, #16 back-pill color, #18 pager-link color) now fire under prefers-reduced-motion: reduce per the surgical policy. Wave 3 phase-exit walks the real-device reduced-motion check. Commits cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS scoped styles → browser motion behavior | Browser respects prefers-reduced-motion media query; no JS or third-party involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Information Disclosure | Reduced-motion user signal exposed via @media | accept | Reduced-motion is a public user preference exposed by every browser to every site by design. Knowing a user has it ON allows the site to honor the preference — that's the entire point of the media query. |
| T-06-14 | Tampering | Surgical deletion regressing Phase 5 a11y | mitigate | Gate 27 locks the absence post-deletion. Wave 3 phase-exit reduced-motion walk on real iPhone 15 verifies the three previously-suppressed exempt motions now fire as designed (per Plan 05-06 SUMMARY's motion-by-motion table — these three motions sit at "exempt fires" lines #12, #16, #18 of the post-deletion table). |

ASVS Level 1 informational. CSS-only edit. No new dependencies, no auth, no input handling.
</threat_model>

<verification>
- All three perl checks return exit 0 (no transition:none inside prefers-reduced-motion block in any file).
- `npm run build && bash scripts/verify-build.sh` shows Gate 27 GREEN + all other gates GREEN.
- `grep -c 'prefers-reduced-motion' src/components/StatusPill.astro src/pages/\[category\].astro src/pages/\[category\]/\[slug\].astro` — each count should be exactly one less than before (or zero, depending on whether each file had OTHER `prefers-reduced-motion` references).
- `npm run preview` visual smoke — site renders, no broken styling, hover-scale on StatusPill, color transitions on back-pill and pager visually still work.
</verification>

<success_criteria>
SC6 (folded Phase 5 carryover — D-06 stragglers) is closed at the source level. Gate 27 GREEN. Wave 3 phase-exit real-device walk (iPhone 15 reduce-motion ON) verifies the runtime behavior matches D-08's exempt-fires contract.
</success_criteria>

<output>
Create `.planning/phases/06-deploy-maintenance-handoff/06-05-SUMMARY.md`. Note:
- Exact line-number deltas (pre-edit vs post-edit) for each of the three files.
- Confirm the preserved comment in StatusPill.astro line 86 (`/* D-04 StatusPill mobile shrink */`) and the deleted comment in [category].astro line 139 (`/* Reduced-motion (D-13) */`).
- Plan 06-04 overlap on [slug].astro: confirm the two edits coexist cleanly (line 102 widths prop + line 332-334 deletion don't conflict).
- Pointer to Wave 3 (Plan 06-08) for the real-device reduce-motion walk that closes SC6 phenomenologically.
</output>
</content>
