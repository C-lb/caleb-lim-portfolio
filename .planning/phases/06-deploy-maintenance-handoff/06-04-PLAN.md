---
phase: 06-deploy-maintenance-handoff
plan: 04
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - src/pages/[category]/[slug].astro
autonomous: true
requirements: []
tags: [phase-6, wave-1, slice-c, lcp, image, d-05, foun-02, perf]

must_haves:
  truths:
    - "src/pages/[category]/[slug].astro:97-103 detail-hero `<Image>` invocation gains exactly one new prop: `widths={[480, 768, 960, 1280]}`, inserted between the existing `priority` (line 101) and `sizes` (line 102) props"
    - "The src, alt, class, priority, sizes props at the detail hero invocation are unchanged (RESEARCH §Pattern 3: D-05's actual root cause is the missing widths array, NOT priority or sizes which were already shipped in Plan 05-04)"
    - "No format prop is added — Astro 5's default format is webp and the source asset is already webp (RESEARCH §Pattern 3 recommendation; reconsider only if Lighthouse re-audit still shows the hero as LCP bottleneck after this fix)"
    - "After npm run build, `dist/design/design-real-piece/index.html` shows the hero `<img>` carries `srcset=` (multiple resolution variants), NOT just a single source (the broken state confirmed by RESEARCH §Pattern 3 dist/ inspection 2026-05-20)"
    - "The emitted srcset contains at least 4 image variants — one for each width in [480, 768, 960, 1280]"
    - "The paginated PDF block below the hero (if present on this slug) remains lazy-loaded — no spillover of priority/widths to those images"
    - "The pager-link block at lines 332-334 (D-06 straggler) is NOT touched by this plan — Plan 06-05 owns it"
    - "Phase 1-5 gates 1-25 still GREEN; specifically Gate 10 (paginated slides), Gate 11 (full-PDF link), Gate 22 (pager parity) unchanged"
    - "Lighthouse re-audit on the deployed Vercel URL after this commit shows detail-page LCP drops from 3121ms (Plan 05-08 baseline) to under 2000ms — verification deferred to Wave 3 phase-exit Lighthouse run, but the build-side fix lands here"
  artifacts:
    - path: "src/pages/[category]/[slug].astro"
      provides: "Detail-page hero <Image> with widths array added at line ~102"
      contains: "widths={[480, 768, 960, 1280]}"
  key_links:
    - from: "detail-hero <Image> widths prop"
      to: "Astro <Image> srcset generator"
      via: "Astro 5.18 <Image> emits srcset when widths is supplied"
      pattern: "widths=\\{\\[480"
    - from: "GalleryA12.astro:23-30 canonical pattern"
      to: "[slug].astro:97-103 mirrored pattern"
      via: "prop order src → alt → class → priority → widths → sizes (PATTERNS.md §Shared Patterns)"
      pattern: "priority\\s+widths"
---

<objective>
Wave 1 Slice C — close the detail-page LCP regression by adding the `widths={[...]}` array to the existing `<Image>` invocation. Single-line surgical edit.

Purpose: Closes SC5 (folded Phase 5 carryover — detail-page LCP <2000ms). Phase 5 Plan 05-04 added `priority` + `sizes` to this same `<Image>` but Plan 05-08's Lighthouse audit measured 3121ms LCP on the detail page (vs splash's 1671ms). RESEARCH §Pattern 3 verified the root cause via direct dist/ inspection 2026-05-20: Astro emits a single 1280×1600 native-resolution `<img>` with NO `srcset` because `widths` was never provided. `priority` schedules the load early; `sizes` tells the browser the display size; `widths` is what generates the responsive variants. The three are complementary; all three are required.

Output: ONE prop added to ONE `<Image>` invocation. Build emits srcset for the hero. Lighthouse re-audit (Wave 3) confirms <2000ms LCP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md
@.planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md
@.planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md
@.planning/phases/05-mobile-performance-accessibility/05-04-PLAN.md
@.planning/phases/05-mobile-performance-accessibility/05-04-SUMMARY.md
@src/components/GalleryA12.astro
@src/pages/[category]/[slug].astro

<interfaces>
<!-- Current detail-hero invocation [slug].astro:97-103 (PATTERNS.md §[slug].astro lines 165-173, verbatim): -->
<!--     <Image -->
<!--       src={hero} -->
<!--       alt={title} -->
<!--       class="detail-hero" -->
<!--       priority -->
<!--       sizes="(max-width: 960px) 100vw, 960px" -->
<!--     /> -->

<!-- Target invocation (PATTERNS.md §[slug].astro lines 178-186 — insert ONE prop between priority and sizes): -->
<!--     <Image -->
<!--       src={hero} -->
<!--       alt={title} -->
<!--       class="detail-hero" -->
<!--       priority -->
<!--       widths={[480, 768, 960, 1280]} -->
<!--       sizes="(max-width: 960px) 100vw, 960px" -->
<!--     /> -->

<!-- Canonical analog GalleryA12.astro:23-30 (PATTERNS.md §Shared Patterns — prop order src → alt → class → priority → widths → sizes): -->
<!--   <Image -->
<!--     src={piece.data.hero} -->
<!--     alt={piece.data.title} -->
<!--     class="cover" -->
<!--     priority={slot === 1} -->
<!--     widths={[280, 560]} -->
<!--     sizes="(max-width: 900px) 50vw, 240px" -->
<!--   /> -->

<!-- widths rationale (RESEARCH §Pattern 3): -->
<!--   - CSS .detail caps at max-width: 960px -->
<!--   - Source asset is 1280×1600 (the cap; Astro skips variants larger than source) -->
<!--   - Mobile <540 → 480w variant -->
<!--   - Mid-range mobile → 768w -->
<!--   - Tablet / 1× desktop → 960w -->
<!--   - 2× DPR desktop → 1280w (cap) -->

<!-- DO NOT touch: -->
<!--   - paginated PDF slide block (lines ~120-150 per Plan 05-04 comments) — stays lazy per FOUND-02 -->
<!--   - pager-link block (lines 332-334) — D-06 straggler removed by Plan 06-05, not this plan -->
<!--   - any CSS in this file — purely a markup-prop addition -->
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add widths={[480, 768, 960, 1280]} to the detail-page hero `<Image>`</name>
  <files>src/pages/[category]/[slug].astro</files>
  <read_first>
    - src/pages/[category]/[slug].astro (lines 95-105 — verify the detail-hero `<Image>` is at lines 97-103 and the current 6-line prop layout matches PATTERNS.md §[slug].astro analog; if Phase 5 work drifted line numbers, locate the FIRST `<Image>` element whose `class="detail-hero"` and operate on it)
    - .planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md §[slug].astro detail hero (target shape verbatim — the ONE-prop addition between priority and sizes)
    - .planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md §Pattern 3 (widths rationale + the broken-srcset diagnosis)
    - .planning/phases/05-mobile-performance-accessibility/05-04-PLAN.md Task 3 + GalleryA12.astro:23-30 (canonical prop-order analog)
  </read_first>
  <behavior>
    - After editing, the detail-hero `<Image>` has exactly 6 props in this order: `src={hero}`, `alt={title}`, `class="detail-hero"`, `priority`, `widths={[480, 768, 960, 1280]}`, `sizes="(max-width: 960px) 100vw, 960px"`.
    - The widths array contains exactly 4 numeric entries: 480, 768, 960, 1280.
    - `npm run build` produces `dist/design/design-real-piece/index.html` whose detail-hero `<img>` carries a non-empty `srcset` attribute with at least 4 variants.
    - The emitted srcset references the same image at different widths (verifiable by `grep -c '\\.webp' dist/design/design-real-piece/index.html` ≥ 4 — one per width entry).
    - The `loading="lazy"` lower-priority images for the paginated PDF block (if this slug has one) remain unchanged.
    - Gates 1-25 stay GREEN. Specifically Gate 10 (paginated slides count), Gate 11 (full-PDF link), Gate 22 (pager parity) unchanged.
  </behavior>
  <action>
    Open `src/pages/[category]/[slug].astro`. Locate the detail-hero `<Image>` — it's the first `<Image>` element in the file (NOT the paginated PDF slides further down), styled with `class="detail-hero"`. Per PATTERNS.md it sits at lines 97-103 of the current file; if line numbers have drifted, search for `class="detail-hero"` to land on it directly.

    Add ONE line between the existing `priority` (currently line ~101) and the existing `sizes="(max-width: 960px) 100vw, 960px"` (currently line ~102):

    Insert: `      widths={[480, 768, 960, 1280]}`

    Use the same 6-space indentation as the surrounding props (match GalleryA12.astro:23-30 indentation if in doubt). Do NOT touch the surrounding props. Do NOT add `format="webp"` — Astro 5's default is webp, the source asset is already webp, and adding the prop would be redundant (RESEARCH §Pattern 3 explicitly recommends against it for the first pass).

    Resulting invocation should be 7 lines (open tag + 6 props + close tag) matching PATTERNS.md §[slug].astro target shape verbatim.

    Do NOT modify anywhere else in the file:
    - The paginated PDF slide block (further down the file) stays lazy per FOUND-02
    - The pager-link block at lines 332-334 (D-06 straggler) is Plan 06-05's territory — leave it alone in this plan
    - All CSS in the `<style>` block is untouched
    - The frontmatter is untouched

    After editing run `npm run build`. The build must complete. Spot-check the output: `grep -A2 'detail-hero' dist/design/design-real-piece/index.html | head -10` should show the `<img>` carrying both `srcset="..."` and `sizes="..."` attributes. The srcset value should list multiple URLs with `_astro/<hash>.webp` paths.

    Then run `bash scripts/verify-build.sh`. Confirm Gates 1-25 + (whichever Phase 6 gates have landed at this point) stay GREEN. Specifically check `OK: Gate 10` (paginated slides), `OK: Gate 11` (full-PDF link), `OK: Gate 22` (pager parity). No regressions.

    Lighthouse re-audit confirmation is deferred to Wave 3's phase-exit run — running it now against the Vercel default subdomain pre-domain-flip is fine but not required by this plan's verify. The build-side fix lands here; the measurement happens after Wave 2 + 3 produce the production-on-custom-domain URL.
  </action>
  <verify>
    <automated>grep -E 'widths=\{\[480,?\s*768,?\s*960,?\s*1280\]\}' src/pages/\[category\]/\[slug\].astro &amp;&amp; grep -B5 -A5 'class="detail-hero"' src/pages/\[category\]/\[slug\].astro | grep -F 'priority' &amp;&amp; grep -B5 -A5 'class="detail-hero"' src/pages/\[category\]/\[slug\].astro | grep -F 'sizes="(max-width: 960px) 100vw, 960px"' &amp;&amp; npm run build &amp;&amp; grep -A2 'detail-hero' dist/design/design-real-piece/index.html | grep -q 'srcset=' &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/06-04-t1.log &amp;&amp; grep -F 'OK: Gate 10' /tmp/06-04-t1.log &amp;&amp; grep -F 'OK: Gate 11' /tmp/06-04-t1.log &amp;&amp; grep -F 'OK: Gate 22' /tmp/06-04-t1.log &amp;&amp; grep -F 'OK: Gate 25' /tmp/06-04-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `widths={[480, 768, 960, 1280]}` literal appears in `src/pages/[category]/[slug].astro` (whitespace-insensitive match acceptable, but the 4 numeric values are non-negotiable).
    - The line is inserted BETWEEN `priority` and `sizes` props of the detail-hero `<Image>` (verifiable by reading lines 95-105 of the modified file).
    - All other props on that `<Image>` (src, alt, class, priority, sizes) are unchanged.
    - `npm run build` exits 0.
    - `dist/design/design-real-piece/index.html` shows the detail-hero `<img>` carrying a non-empty `srcset=` attribute (this is the load-bearing fix — if srcset is missing, the widths prop wasn't picked up).
    - Gates 1-25 stay OK; Gates 10, 11, 22 specifically remain GREEN (no regression to paginated PDFs, full-PDF link, or pager).
    - No edits anywhere else in the file.
  </acceptance_criteria>
  <done>
    Detail-page hero `<Image>` now has the full canonical 6-prop set matching GalleryA12.astro pattern (src, alt, class, priority, widths, sizes). Build emits srcset for the hero. Lighthouse re-audit (Wave 3) measures the LCP drop. Commits cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Astro <Image> build pipeline → emitted srcset | Build-time only; no runtime input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Denial of Service | Browser downloading multiple variants | accept | Browsers select ONE variant from srcset per the viewport. The total bytes-per-page-load go DOWN, not up — mobile downloads the 480w or 768w instead of the 1280w. The build does produce more files (4 variants per source), but that's sharp's job and Plan 02-01 already amortized this. |
| T-06-12 | Tampering | widths array literal | accept | Source-controlled. Pure number array. No injection surface. |

ASVS Level 1 informational. Single-prop addition. No new dependencies, no auth, no input handling.
</threat_model>

<verification>
- Modified line is visible via `grep -B2 -A5 'class="detail-hero"' src/pages/[category]/[slug].astro` — shows the canonical 6-prop layout.
- `npm run build && bash scripts/verify-build.sh` keeps all Phase 1-5 gates GREEN.
- `grep -A2 'detail-hero' dist/design/design-real-piece/index.html | grep srcset` confirms srcset is emitted with multiple variants.
- `ls dist/_astro/ | grep -c '\.webp$'` confirms more webp variants exist after this build than before (the new sharp-generated variants land here).
- Wave 3 phase-exit Lighthouse audit measures the actual LCP drop on the production custom domain.
</verification>

<success_criteria>
SC5 (folded Phase 5 carryover — detail-page LCP <2000ms) is set up to close. Build-side fix is in. Wave 3's Lighthouse re-audit confirms the runtime improvement. The 3121ms → <2000ms transition closes the only Phase 5 D-15 carryover concern.
</success_criteria>

<output>
Create `.planning/phases/06-deploy-maintenance-handoff/06-04-SUMMARY.md`. Note:
- Pre-fix vs post-fix `<img>` HTML — paste the relevant `dist/design/design-real-piece/index.html` snippet showing srcset went from absent to populated.
- Variant count emitted under `dist/_astro/` (verify Astro generated 4 widths × however many DPR multipliers; usually 4 entries total since the cap is at source 1280px).
- Confirm paginated PDF slides (if this slug has any) remain lazy — no spillover of priority semantics.
- Pointer to Wave 3 Lighthouse re-audit as the final SC5 closure step.
</output>
</content>
