---
phase: 06-deploy-maintenance-handoff
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md
  - scripts/verify-build.sh
  - README.md
autonomous: true
requirements: []
tags: [phase-6, wave-0, verification-scaffolding, gates, nyquist]

must_haves:
  truths:
    - "06-VERIFICATION.md exists at .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md and mirrors the structure of 05-VERIFICATION.md (Real-Device Test Rig, SC sign-off table for SC1-SC6, Critical-Path Walk template, Reduced-Motion Walk template, Lighthouse anchors, Cross-Browser Matrix template, Domain Availability Check Log)"
    - "scripts/verify-build.sh has a new 'Phase 6 gates' banner after the existing 'Phase 5 gates' block, and contains Gate 26a (public/og.png present + 1200x630 PNG), Gate 26b (dist/sitemap-index.xml + dist/sitemap-0.xml present post-build), Gate 26c (public/robots.txt present + references Sitemap: line), and Gate 27 (transition:none absent inside prefers-reduced-motion blocks across the three D-06 files)"
    - "All Phase 1-5 gates (1-25) remain GREEN after the new gates are added (no regression introduced by editing the script)"
    - "Gates 26a, 26b, 26c, 27 emit FAIL (RED) on the current pre-Wave-1 state - this is expected and documents the work the Wave 1 plans will satisfy"
    - "README.md exists at repo root as a scaffold with five numbered placeholders (`<!-- 01-open-github-dev.png -->` through `<!-- 06-piece-live-on-prod.png -->`) for the SC3 walkthrough screenshots Wave 3 will fill in"
    - "Cross-browser matrix template inside 06-VERIFICATION.md has a 5x4 table (routes: /, /design, /design/<slug>, /about, /<bad-404> x browsers: iPhone Safari, Android Chrome, desktop Safari, desktop Firefox) with empty PASS/FAIL cells"
    - "Domain Availability Check Log section in 06-VERIFICATION.md has empty rows for caleblim.com primary + the three D-08 fallbacks (caleblim.co, caleb.work, caleblimkr.com)"
  artifacts:
    - path: ".planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md"
      provides: "Phase 6 verification template - SC1-SC6 sign-off table, cross-browser matrix, domain log, walks"
    - path: "scripts/verify-build.sh"
      provides: "Gates 26a/26b/26c/27 appended after Phase 5 block"
      contains: "Phase 6 gates"
    - path: "README.md"
      provides: "Repo-root README scaffold with screenshot placeholders for Wave 3"
  key_links:
    - from: "Gate 26b in scripts/verify-build.sh"
      to: "dist/sitemap-index.xml and dist/sitemap-0.xml"
      via: "test -f $DIST/sitemap-index.xml etc."
      pattern: "Gate 26b"
    - from: "Gate 27 in scripts/verify-build.sh"
      to: "the three D-06 files (StatusPill.astro, [category].astro, [slug].astro)"
      via: "perl -0777 -ne 'exit 1 if m{...transition:\\s*none}'"
      pattern: "prefers-reduced-motion.*transition"
    - from: "06-VERIFICATION.md SC sign-off table"
      to: "SC1-SC6 from ROADMAP Phase 6"
      via: "table rows naming each SC with sign-off date + evidence column"
      pattern: "SC1.*HTTPS"
---

<objective>
Wave 0 — scaffold all verification artefacts BEFORE Wave 1 ships any deliverables. Establish the RED signals that Wave 1 plans will turn GREEN.

Purpose: Phase 6 has six SCs and 11 decisions. Without scaffolding the verification harness first, Wave 1 plans ship blind. This plan creates 06-VERIFICATION.md (the SC sign-off doc), extends scripts/verify-build.sh with Gates 26a/b/c + 27 (regression locks for the SEO bundle + D-06 stragglers), and lays a README.md skeleton at repo root for the Wave 3 walkthrough.

Output: Three files created/modified. New gates are RED post-Wave-0 (no og.png yet, no sitemap yet, no robots.txt yet, transition:none blocks still present) — this is the correct pre-Wave-1 state and documents what each downstream plan must produce.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md
@.planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md
@.planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md
@.planning/phases/06-deploy-maintenance-handoff/06-VALIDATION.md
@.planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md
@scripts/verify-build.sh

<interfaces>
<!-- Gate header convention extracted from scripts/verify-build.sh Gate 23/25 (PATTERNS.md §verify-build.sh gate header style): -->
<!--   # Gate NN (Phase X): <one-sentence "what" with SC/D-ref anchor>. -->
<!--   # <2-4 lines of "why" / rationale / what other gate it complements>. -->
<!--   # Expected RED until Plan XX-YY lands — that is correct. -->

<!-- Phase-banner convention (mirrors verify-build.sh lines 537-538 / 848-849): -->
<!--   echo -->
<!--   echo "Phase 6 gates" -->
<!--   echo "=============" -->

<!-- Gate 17 file-existence pattern (verify-build.sh:502-517): -->
<!--   if [[ ! -f "$DIST/404.html" ]]; then -->
<!--     echo "  FAIL: ..."; fail=1 -->
<!--   else -->
<!--     if ! grep -q ... ; then echo "  FAIL: ..."; fail=1; fi -->
<!--     ...success branch... -->
<!--   fi -->

<!-- Gate 25 grep-with-trailing-||true pattern (verify-build.sh:934-949): -->
<!--   gate25_hits=$(grep -rnE 'pattern' file/path 2>/dev/null || true) -->
<!--   if [[ -n "$gate25_hits" ]]; then ... fail=1 ... else echo "  OK: ..."; fi -->

<!-- 05-VERIFICATION.md structure to mirror (verbatim section headers): -->
<!--   ## Real-Device Test Rig -->
<!--   ## Vercel Project Bootstrap -->
<!--   ## Critical-Path Walk -->
<!--   ## Lighthouse Scores -->
<!--   ## Reduced-Motion Walk -->
<!--   ## Phase Exit Sign-Off (with SC sign-off table at the bottom) -->

<!-- Astro DIST env var convention (verify-build.sh likely defines DIST near top): -->
<!--   DIST="${DIST:-dist}"  (or similar - read the script to confirm) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 06-VERIFICATION.md from 05-VERIFICATION.md template</name>
  <files>.planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md (template source - lines 1-246; mirror its section structure exactly)
    - .planning/ROADMAP.md (Phase 6 SCs - 6 success criteria including folded SC5 detail-LCP + SC6 D-06 stragglers)
    - .planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md (D-01..D-11 + canonical_refs)
    - .planning/phases/06-deploy-maintenance-handoff/06-VALIDATION.md (sampling rate + manual checkpoints)
  </read_first>
  <action>
    Create `.planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md` with frontmatter `phase: 6`, `status: scaffolded`, `recorded_at: TBD`, `notes: Wave 0 scaffold; filled by Wave 1 (Gates 26a/b/c + LCP), Wave 2 (domain + HTTPS), Wave 3 (dry run + cross-browser + Lighthouse + reduced-motion).`

    Mirror 05-VERIFICATION.md's section ordering exactly. Required sections (each as empty / TBD scaffold ready for Wave 1-3 to fill):

    1. **Real-Device Test Rig** table — same fields as 05-VERIFICATION (iPhone model, iOS version, Network, Browser, Test date, Tester). Pre-fill iPhone 15 / iOS 26.4.2 / Caleb Lim per D-09 carryover from Phase 5 rig.
    2. **Vercel Production Bootstrap** — Production URL row (https://caleb-lim-portfolio.vercel.app), Custom-domain URL row (TBD until Wave 2 lands), Domain status row (TBD), First production-on-custom-domain commit row (TBD).
    3. **Domain Availability Check Log** — table with columns `Date | Domain | Available? | Registered? | Notes`; four rows pre-seeded for caleblim.com (primary per D-08), caleblim.co (fallback 1), caleb.work (fallback 2), caleblimkr.com (fallback 3). All cells empty; Wave 2 plan fills in.
    4. **Critical-Path Walk** — same 11-step table 05-VERIFICATION used (load splash, tap design, tap tile, scroll detail, tap back-pill, tap topbar envelope/LinkedIn/resume, tap OPEN-TO-ROLES, brand link → /marketing, repeat 2-4 on /marketing, reduced-motion repeat). PASS/FAIL column empty. Heading says "To be walked on iPhone 15 / iOS 26.4.2 against the custom domain (or Vercel default subdomain pre-Wave-2)."
    5. **Cross-Browser Matrix (D-09)** — 5×4 table. Rows = routes `/`, `/design`, `/design/<slug>` (note: pick `/design/design-real-piece` as the slug per Phase 5 baseline), `/about`, `/<bad-404>` (404 page). Columns = `iPhone Safari (iPhone 15 / iOS 26.4.2)`, `Android Chrome (device or BrowserStack — record source)`, `Desktop Safari (Mac)`, `Desktop Firefox (Mac)`. Each cell empty placeholder for PASS/FAIL + 1-line note. Header note: "Each cell: load route, confirm no console errors, exercise primary interaction (gallery click, detail open, scroll, back), then mark PASS/FAIL. ~5min × 20 cells ≈ 100min total."
    6. **Lighthouse Scores** — 5-row table matching 05-VERIFICATION (`/`, `/design`, `/marketing`, `/about`, `/design/design-real-piece`). Columns: `Perf | A11y | LCP (ms) | Pass? | Summary file`. All cells TBD. Below table: a note "Target after Wave 1 Slice C (D-05 detail-LCP fix): /design/design-real-piece LCP < 2000ms (was 3121ms in Phase 5 baseline)."
    7. **Reduced-Motion Walk (D-06)** — 9-step table mirroring 05-VERIFICATION's reduced-motion walk PLUS three new rows specifically for the D-06 exempt motions that should now fire after straggler removal: StatusPill hover-scale (D-08 #12), back-pill color transition (D-08 #16), pager-link color transition (D-08 #18). PASS/FAIL column empty.
    8. **OG / SEO Sign-Off** — table with rows: og.png exists (Gate 26a), og.png 1200×630, og.png visual approval by Caleb, LinkedIn Post Inspector preview, Twitter Card Validator (or opengraph.xyz fallback per A4), sitemap-index.xml at custom domain returns 200, robots.txt at custom domain references sitemap. All cells TBD; evidence column per row.
    9. **Caleb-Adds-a-Piece Dry Run (D-01, D-02)** — checklist scaffold for SC3 covering the 5-8 numbered screenshot steps Wave 3 will capture: (1) open github.dev → (2) create folder under src/content/pieces/ → (3) add index.md with frontmatter → (4) upload hero image → (5) commit + push → (6) Vercel auto-deploys → (7) verify piece on prod URL. Each row has a screenshot path placeholder `docs/contributing/0X-<name>.png` and a PASS/FAIL column. Date row TBD.
    10. **Phase Exit Sign-Off** — checkbox list matching 05-VERIFICATION style (all verify-build.sh gates 1-27 GREEN; Lighthouse thresholds met on prod custom domain; real-iPhone walk recorded; reduced-motion walk recorded; cross-browser matrix complete; OG card + sitemap + robots verified; Caleb dry run captured + README populated). Below: **SC sign-off table** with rows SC1 (HTTPS / DNS / domain), SC2 (cross-browser end-to-end), SC3 (Caleb dry run + README), SC4 (OG card + sitemap + robots), SC5 (detail-LCP <2000ms), SC6 (D-06 stragglers removed + D-08 motion-by-motion). All cells TBD with `Sign-off | Evidence` columns.

    Final line: `**Phase 6 verdict: TBD** (Wave 0 scaffold complete; Wave 1-3 fills in).`

    Do NOT pre-fill SC sign-off cells, Lighthouse numbers, walk results, or domain registration outcomes — those are Wave 2/3 outputs. Wave 0's job is structure, not data.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md &amp;&amp; grep -F 'Real-Device Test Rig' .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md &amp;&amp; grep -F 'Domain Availability Check Log' .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md &amp;&amp; grep -F 'Cross-Browser Matrix' .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md &amp;&amp; grep -F 'Reduced-Motion Walk' .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md &amp;&amp; grep -F 'OG / SEO Sign-Off' .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md &amp;&amp; grep -F 'Caleb-Adds-a-Piece Dry Run' .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md &amp;&amp; grep -cE '^\| SC[1-6] ' .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md | grep -qE '^[6-9]$|^[1-9][0-9]+$'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the specified path with valid YAML frontmatter (`phase: 6`, `status: scaffolded`).
    - All 10 required sections present as headed scaffolds with empty cells (no pre-filled outcomes).
    - SC sign-off table at the bottom has 6 rows (SC1 through SC6) with empty Sign-off + Evidence columns.
    - Cross-Browser Matrix has 5 route rows × 4 browser columns = 20 cells.
    - Domain Availability Check Log has 4 rows pre-seeded with the D-08 chain.
    - Phase 6 verdict line is `TBD`, not `COMPLETE`.
  </acceptance_criteria>
  <done>
    06-VERIFICATION.md is the canonical phase-exit doc, structured to mirror 05-VERIFICATION.md, with all SC sign-off rows and walk scaffolds ready for Wave 1-3 to fill. No data pre-filled. Commits cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend scripts/verify-build.sh with Phase 6 gates (26a, 26b, 26c, 27)</name>
  <files>scripts/verify-build.sh</files>
  <read_first>
    - scripts/verify-build.sh (read in full to confirm DIST variable name, fail variable name, banner-emit convention, and current trailing line)
    - .planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md §verify-build.sh — gate header structure, Gate 25 lines 934-949 (closest grep-against-repo gate), Gate 17 lines 502-517 (closest file-existence gate), banner pattern at lines 537-538 / 848-849
    - .planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md §Wave 0 Gaps + §Pattern 4 (straggler removal — Gate 27 must regression-lock these locations)
  </read_first>
  <action>
    Open `scripts/verify-build.sh` and append a new "Phase 6 gates" block AFTER the existing "Phase 5 gates" block (after Gate 25's closing OK/FAIL branch around line 949). Mirror the banner-emit convention from earlier phase banners (`echo` + `echo "Phase 6 gates"` + `echo "============="`).

    Then append four gates in this order, using the conventions from PATTERNS.md §verify-build.sh gate header style (multi-line `#` header naming Gate ID + SC/D-ref + "Expected RED until Phase 6 Plan 06-NN lands" rationale):

    **Gate 26a — `public/og.png` present + 1200x630 PNG (SC4 / D-03 / FOUND-04):**
    Use the file-existence + dimension-check approach from PATTERNS.md §Pattern 26a. Check `[[ ! -f "public/og.png" ]]` → FAIL. Else use `file public/og.png | grep -oE '[0-9]+ x [0-9]+' | head -1` to extract dims, expect `1200 x 630`. Emit OK or FAIL with `fail=1` on failure.

    **Gate 26b — sitemap-index.xml + sitemap-0.xml in $DIST (SC4 / D-04):**
    Use a `for` loop over `$DIST/sitemap-index.xml` and `$DIST/sitemap-0.xml`. Track a local `gate26b_fail=0`; on any missing file, echo `FAIL: Gate 26b — <path> missing (@astrojs/sitemap not emitting; SC4 / D-04 — expected RED pre-Wave 1)` and set `gate26b_fail=1`. After the loop, if `gate26b_fail=0`, echo OK; else escalate to outer `fail=1`. Use the same `DIST` variable the rest of the script uses (verify by reading the top of the script for the env-var assignment).

    **Gate 26c — `public/robots.txt` present + contains `Sitemap:` line (SC4 / D-04):**
    Two checks: file existence, then `grep -q '^Sitemap:.*sitemap-index\.xml' public/robots.txt`. On either failure, FAIL + `fail=1`. Otherwise OK.

    **Gate 27 — zero `transition: none` inside `@media (prefers-reduced-motion: reduce)` blocks across the three D-06 files (SC6 / D-06 / D-08):**
    Use the multi-line perl slurp approach from PATTERNS.md §Pattern 27. For each of `src/components/StatusPill.astro`, `src/pages/[category].astro`, `src/pages/[category]/[slug].astro` (escape the brackets in bash: `src/pages/\[category\].astro` etc.), run `perl -0777 -ne 'exit 1 if m{\@media\s*\(\s*prefers-reduced-motion:\s*reduce\s*\)\s*\{[^}]*transition:\s*none}s' "$src_file"`. Track `gate27_fail=0`. perl exit 0 = no match = OK; exit non-zero = match present = FAIL with explicit D-08 violation message naming #12/#16/#18. After the loop, if `gate27_fail != 0` escalate to outer `fail=1`.

    All four gate headers MUST follow PATTERNS.md §verify-build.sh gate header style exactly:
    - Line 1: `# Gate NN (Phase 6): <one-sentence what with SC/D-ref>.`
    - Lines 2-4: rationale + which Wave NN plan satisfies it
    - Last header line: `# Expected RED until Phase 6 Plan 06-NN lands — that is correct.`

    Specifically:
    - Gate 26a expected RED until Plan 06-03 (OG card + meta slice)
    - Gate 26b expected RED until Plan 06-02 (sitemap slice)
    - Gate 26c expected RED until Plan 06-02 (sitemap slice)
    - Gate 27 expected RED until Plan 06-05 (D-06 straggler removal slice)

    Do NOT modify Phase 1-5 gates. Do NOT touch the script's shebang, set -e/pipefail, DIST assignment, or fail-initial-state at the top. Only APPEND after the Phase 5 block.

    Verify the script still exits non-zero (current state has at least 4 new RED gates plus any existing RED state). Run `bash scripts/verify-build.sh; echo "exit=$?"` — expect non-zero exit because Gates 26a/b/c/27 will FAIL on the pre-Wave-1 codebase (no og.png, no sitemap, no robots, transition:none still present in all 3 files). This is the CORRECT pre-Wave-1 state. All Gates 1-25 must remain OK in the output.
  </action>
  <verify>
    <automated>bash -n scripts/verify-build.sh &amp;&amp; grep -c 'Gate 26a' scripts/verify-build.sh | grep -qE '^[1-9]' &amp;&amp; grep -c 'Gate 26b' scripts/verify-build.sh | grep -qE '^[1-9]' &amp;&amp; grep -c 'Gate 26c' scripts/verify-build.sh | grep -qE '^[1-9]' &amp;&amp; grep -c 'Gate 27' scripts/verify-build.sh | grep -qE '^[1-9]' &amp;&amp; grep -F 'Phase 6 gates' scripts/verify-build.sh &amp;&amp; npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/06-01-t2.log; grep -F 'OK: Gate 25' /tmp/06-01-t2.log &amp;&amp; grep -E 'FAIL: Gate 26[abc]|FAIL: Gate 27' /tmp/06-01-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/verify-build.sh` parses without syntax error.
    - Each Gate 26a, 26b, 26c, 27 appears in the script and has its own multi-line `#`-prefixed header following the PATTERNS.md template.
    - `npm run build && bash scripts/verify-build.sh` keeps Gates 1-25 OK (no Phase 1-5 regression).
    - On the pre-Wave-1 codebase, Gates 26a/26b/26c/27 all emit `FAIL:` lines (this is the expected RED scaffold state).
    - No edits to the script before the "Phase 5 gates" block.
    - The "Phase 6 gates" banner echoes its header before the new gates run.
  </acceptance_criteria>
  <done>
    scripts/verify-build.sh now has 4 new gates appended after Phase 5 block. Phase 1-5 gates still GREEN. Phase 6 gates RED on pre-Wave-1 codebase. Script is syntactically valid. Commits cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Scaffold README.md at repo root with screenshot placeholders</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md D-01 + D-02 (walkthrough format and depth)
    - .planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md §Open Questions #3 (screenshot path recommendation: docs/contributing/)
    - .planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md §README.md (structure to mirror)
  </read_first>
  <action>
    Create `README.md` at repo root (NOT inside `.planning/` — this is the file github.com renders on the repo home). Structure per PATTERNS.md §README.md:

    1. **H1 title**: `# Caleb Lim — Portfolio`
    2. **1-sentence pitch** under title: `Cross-functional generalist for brand, marketing, financial models, and graphic design roles. Live at <production URL> — see live link below.`
    3. **Live URL section** (`## Live`): a one-liner placeholder `<!-- TBD: replace with https://caleblim.com (or D-08 fallback) after Wave 2 -->` and a fallback line referencing the current Vercel default subdomain `Pre-domain-flip: https://caleb-lim-portfolio.vercel.app`.
    4. **Adding a piece section** (`## Adding a piece`) — the SC3 walkthrough. Per D-01 + D-02 it's the markdown-frontmatter + image-hero flow only. Pre-seed 6 numbered subsections (h3) with screenshot embed placeholders following the relative-path convention from PATTERNS.md (`docs/contributing/0X-<name>.png`):
       - Step 1 — open github.dev: `<!-- placeholder: docs/contributing/01-open-github-dev.png — captured Wave 3 -->` `![Step 1 — open github.dev](docs/contributing/01-open-github-dev.png)`
       - Step 2 — create the piece folder under `src/content/pieces/`: placeholder + embed for `02-create-piece-folder.png`
       - Step 3 — add `index.md` with frontmatter (title, category, role, outcome, context, hero): placeholder + embed for `03-add-frontmatter.png`
       - Step 4 — drop hero image into the folder: placeholder + embed for `04-add-hero-image.png`
       - Step 5 — commit + push (source-control panel): placeholder + embed for `05-commit-and-push.png` — per RESEARCH §Pitfall 3 this is the most-likely-to-trip-up step; flag in the prose
       - Step 6 — Vercel auto-deploys, piece is live: placeholder + embed for `06-piece-live-on-prod.png`
       Each step has a 1-2 sentence prose lead-in describing what Caleb does (the screenshots illustrate the prose). Leave the prose generic for now — Wave 3 will tighten it after the actual dry run.
    5. **Adding a paginated-PDF piece** (`## Adding a paginated-PDF piece`) — per D-02 this is ONE short paragraph or a single `See [CONTRIBUTING.md] for the multi-page-deck flow (rasterized at build time via the same Astro pipeline).` line. Do not screenshot-walk it. One-liner only.
    6. **Tech stack** (`## Tech`) — single line: `Astro 5 + Vercel + Cloudflare Registrar. See [.planning/PROJECT.md](.planning/PROJECT.md) for the full architecture rationale.`
    7. **License** (`## License`) — `Personal portfolio. Code is not licensed for reuse; content is © Caleb Lim.`

    Use github-flavored markdown (heading levels, image embed syntax `![alt](relative/path.png)`). Do NOT put actual screenshot files in `docs/contributing/` yet — they're captured during Wave 3's actual dry run. The README references them so the embeds will render once the binaries land.

    Header section should warn future plans: include an HTML comment near the top after the title: `<!-- Wave 0 scaffold: screenshot embeds reference docs/contributing/0X-*.png which Wave 3 (Plan 06-08) captures during the Caleb-adds-a-piece dry run. Until then the embed icons render as broken-image placeholders on github.com — expected. -->`.
  </action>
  <verify>
    <automated>test -f README.md &amp;&amp; grep -F 'Caleb Lim' README.md &amp;&amp; grep -F 'Adding a piece' README.md &amp;&amp; grep -c 'docs/contributing/0' README.md | grep -qE '^[6-9]$|^[1-9][0-9]+$' &amp;&amp; grep -F '01-open-github-dev.png' README.md &amp;&amp; grep -F '06-piece-live-on-prod.png' README.md &amp;&amp; grep -F 'Wave 0 scaffold' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md at repo root (`/Users/caleb/projects/personal-website/README.md`), not inside `.planning/`.
    - Contains all 6 numbered screenshot placeholders (`01-open-github-dev.png` through `06-piece-live-on-prod.png`).
    - Has the HTML comment flagging Wave 0 scaffold state.
    - Has the four sections: Live, Adding a piece (with the 6 numbered subsections), Adding a paginated-PDF piece, Tech, License.
    - Embeds use relative paths (`docs/contributing/0X-*.png`), not absolute paths.
    - No actual screenshot binaries committed yet — placeholders only.
  </acceptance_criteria>
  <done>
    README.md exists at repo root with the SC3 walkthrough scaffold and embed placeholders. Wave 3 captures the actual screenshots and tightens the prose. Renders on github.com (broken-image icons until binaries land — flagged in the HTML comment). Commits cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GSD plan artifacts → repo state | Verification scaffolds influence Wave 1+ work; if the gates are mis-coded they could mask a missing artefact downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `scripts/verify-build.sh` Gate 27 perl regex | mitigate | The perl multi-line regex is conservative (uses `[^}]*` inside the `@media` block). Acceptance criteria require it to FAIL on the current pre-Wave-1 codebase, proving it actually fires on a real positive case. If it OK'd on the pre-Wave-1 codebase, Gate 27 would be silently broken and Plan 06-05 deletion wouldn't be regression-locked. |
| T-06-02 | Information Disclosure | README.md at repo root | accept | No PII, no secrets, no env vars referenced. Public repo; recruiters seeing the README is the intended use case. |
| T-06-03 | Denial of Service | Gate 26b sitemap presence check | accept | Gate fails the build until sitemap emits; this is intentional (forces Wave 1 to deliver). No external dependency. |

ASVS Level 1 informational — no auth, no input handling, no network calls introduced by this plan.
</threat_model>

<verification>
- `bash -n scripts/verify-build.sh` parses without syntax error.
- `npm run build && bash scripts/verify-build.sh 2>&1` keeps Gates 1-25 OK; Gates 26a, 26b, 26c, 27 emit FAIL (RED scaffold state).
- `test -f .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md && test -f README.md` both return 0.
- 06-VERIFICATION.md contains the 10 required section headers (Real-Device Test Rig, Vercel Production Bootstrap, Domain Availability Check Log, Critical-Path Walk, Cross-Browser Matrix, Lighthouse Scores, Reduced-Motion Walk, OG / SEO Sign-Off, Caleb-Adds-a-Piece Dry Run, Phase Exit Sign-Off).
- README.md references all 6 numbered screenshots under `docs/contributing/`.
</verification>

<success_criteria>
Wave 0 verification harness is in place. Wave 1 plans have RED targets to flip GREEN (Gates 26a/b/c/27). Wave 2 has a domain-log to fill in. Wave 3 has a README to populate and an SC sign-off table to complete.

No SC closure happens in Wave 0; this plan only scaffolds the means of closure.
</success_criteria>

<output>
Create `.planning/phases/06-deploy-maintenance-handoff/06-01-SUMMARY.md` per the template. Note in the summary:
- Pre-Wave-1 state of Phase 6 gates (which are RED and why)
- Confirm Gates 1-25 still GREEN (no regression)
- 06-VERIFICATION.md row count for each table (cross-browser 5×4, domain log 4 rows, SC table 6 rows)
- README placeholder count and the Wave 3 hand-off (which Plan captures the screenshots)
</output>
</content>
</invoke>