---
phase: 06-deploy-maintenance-handoff
plan: 08
type: execute
wave: 4
depends_on: [06-04, 06-05, 06-06, 06-07]
files_modified:
  - docs/contributing/01-open-github-dev.png
  - docs/contributing/02-create-piece-folder.png
  - docs/contributing/03-add-frontmatter.png
  - docs/contributing/04-add-hero-image.png
  - docs/contributing/05-commit-and-push.png
  - docs/contributing/06-piece-live-on-prod.png
  - README.md
  - .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md
  - src/content/pieces/<test-piece-slug>/index.md
  - src/content/pieces/<test-piece-slug>/<hero-asset>
autonomous: false
requirements: []
tags: [phase-6, wave-4, dry-run, readme, cross-browser, lighthouse, reduced-motion, phase-exit, sc2, sc3, sc5, sc6]

must_haves:
  truths:
    - "Caleb personally performs the github.dev dry run: opens repo at github.dev → creates a new folder under src/content/pieces/ → authors index.md with valid frontmatter (title, category, role, outcome, context, hero asset reference) → uploads a hero image (JPG or PNG, per D-02 image-hero-only flow) → commits + pushes to main → watches Vercel auto-deploy → confirms the new piece is live at https://<registered-domain>/<category>/<slug> (SC3 closure)"
    - "Six numbered screenshots are captured DURING the dry run (the dry run IS the documentation source per D-01): 01-open-github-dev.png, 02-create-piece-folder.png, 03-add-frontmatter.png, 04-add-hero-image.png, 05-commit-and-push.png, 06-piece-live-on-prod.png. Each is committed under docs/contributing/"
    - "README.md (scaffold from Plan 06-01) is populated with the actual screenshot embeds at the right positions; the prose is tightened against what actually happened during the dry run (e.g. step 5's source-control-panel commit gotcha per RESEARCH §Pitfall 3 is described accurately)"
    - "Cross-browser matrix is walked (per D-09): 5 routes × 4 browsers = 20 cells. Each cell records PASS/FAIL + 1-line note in 06-VERIFICATION.md Cross-Browser Matrix section. Routes: / + /design + /design/<slug> + /about + /<bad-404>. Browsers: iPhone Safari (iPhone 15 / iOS 26.4.2), Android Chrome (Caleb's device or BrowserStack — record source), desktop Safari (Mac), desktop Firefox (Mac). (SC2 closure)"
    - "Lighthouse re-audit on the custom domain via bash scripts/lighthouse-audit.sh https://<registered-domain> — all 5 routes audited; results recorded in 06-VERIFICATION.md Lighthouse Scores section. Targets: Perf ≥80 (Phase 5 floor of 85 carried), A11y ≥95 every route. New target: /design/<slug> LCP <2000ms (Plan 06-04 D-05 fix verified runtime). (SC5 closure)"
    - "Real-iPhone reduced-motion walk on iPhone 15 / iOS 26.4.2 with Settings → Accessibility → Motion → Reduce Motion = ON: walks the 3 D-06-restored exempt motions (StatusPill hover #12, back-pill color #16, pager-link color #18) and confirms they fire as designed. Plus the existing 9 Plan 05-08 motions remain correctly classified. Recorded in 06-VERIFICATION.md Reduced-Motion Walk. (SC6 closure)"
    - "LinkedIn Post Inspector + opengraph.xyz preview captured for the custom-domain URL — OG card renders correctly per D-03 brand spec. Recorded in 06-VERIFICATION.md OG / SEO Sign-Off section."
    - "06-VERIFICATION.md SC sign-off table has all 6 rows (SC1 through SC6) marked ✅ <date> with evidence — Phase 6 verdict line at the bottom changes from `TBD` to `COMPLETE`"
    - "STATE.md is updated: status → 'milestone complete (v1.0)' or equivalent; current_position → 'Phase 6 complete; milestone v1.0 shipped'; progress.completed_phases → 6 / 6"
    - "ROADMAP.md Phase 6 checkbox flips from `[ ]` to `[x]`; per-plan checkboxes (06-01 through 06-08) all flipped to `[x]`"
    - "Phase 1-5 gates 1-25 still GREEN; Phase 6 gates 26a/26b/26c/27 GREEN; npm run build succeeds against the custom domain config"
  artifacts:
    - path: "docs/contributing/"
      provides: "Six numbered PNG screenshots from the actual dry run"
      contains: "01-open-github-dev.png through 06-piece-live-on-prod.png"
    - path: "README.md"
      provides: "Populated walkthrough with embedded screenshots; SC3 documentation closure"
    - path: ".planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md"
      provides: "All 10 sections filled — SC sign-off table fully populated; phase verdict flips to COMPLETE"
    - path: ".planning/STATE.md"
      provides: "Milestone v1.0 marker; phase progress 6/6"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 + per-plan checkboxes flipped to [x]"
  key_links:
    - from: "Caleb's dry-run commit on main"
      to: "Vercel webhook → build → custom-domain deploy"
      via: "GitHub push → Vercel auto-deploy pipeline (verified Plan 05-02 push-trigger)"
      pattern: "vercel-deployment-trigger"
    - from: "Six docs/contributing/0X-*.png binaries"
      to: "README.md embeds"
      via: "relative-path markdown embeds; github.com renders them on the repo home"
      pattern: "docs/contributing/0"
    - from: "06-VERIFICATION.md SC sign-off table"
      to: "STATE.md milestone closure marker"
      via: "All 6 SCs ✅ → STATE.md flips to milestone complete"
      pattern: "Phase 6 verdict.*COMPLETE"
---

<objective>
Wave 4 — phase-exit verification. Caleb performs the github.dev dry run end-to-end; orchestrator captures numbered screenshots; populated README.md ships; cross-browser matrix walked; Lighthouse re-audit on the custom domain validates SC5 LCP fix; reduced-motion walk validates SC6 D-06 closure; all SC rows in 06-VERIFICATION.md signed off; STATE.md + ROADMAP.md flipped to milestone v1.0 complete.

Purpose: Closes SC2 (cross-browser end-to-end), SC3 (Caleb-runs-it dry run + README), SC5 phenomenologically (Lighthouse measurement of D-05 fix), SC6 phenomenologically (real-iPhone reduced-motion walk of D-06 fix). Closes the milestone.

This is a multi-checkpoint plan: the Caleb dry run is a human-driven workflow with the orchestrator alongside Caleb capturing screenshots in real-time. Cross-browser is a manual ~100min walk. Lighthouse is a single ~7min script run. Reduced-motion walk is a ~10min iPhone session. Each section gates the next.

Output: ~6 PNG binaries committed under docs/contributing/, README.md populated, 06-VERIFICATION.md fully signed off, one new test piece living in src/content/pieces/ (Caleb's dry-run artifact — committed to main and live on prod), STATE.md + ROADMAP.md updated. Milestone v1.0 ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md
@.planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md
@.planning/phases/06-deploy-maintenance-handoff/06-PATTERNS.md
@.planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md
@.planning/phases/05-mobile-performance-accessibility/05-08-PLAN.md
@.planning/STATE.md
@.planning/ROADMAP.md
@README.md
@scripts/lighthouse-audit.sh

<interfaces>
<!-- Dry-run flow (D-01 + D-02 + RESEARCH §Pattern 5 + Pitfall 3): -->
<!--   1. Caleb opens github.dev for the repo (URL: github.dev/C-lb/caleb-lim-portfolio OR github.com/C-lb/caleb-lim-portfolio with `.` pressed) -->
<!--   2. Caleb creates a NEW folder under src/content/pieces/ named after a slug (e.g. design/test-dry-run/) -->
<!--   3. Caleb authors index.md with frontmatter matching src/content.config.ts schema (Zod-validated): -->
<!--      title: string -->
<!--      category: "design" | "finance" | "personal" | "marketing" -->
<!--      role: string -->
<!--      outcome: string -->
<!--      context: string -->
<!--      hero: relative image path (per D-02 image-hero only — NOT a PDF) -->
<!--      Plus body content (markdown) -->
<!--   4. Caleb drags a JPG/PNG hero asset into the folder (github.dev supports drag-and-drop per RESEARCH §A7) -->
<!--   5. Source-control panel shows changes — Caleb types a commit message, clicks Commit & Push (per RESEARCH §Pitfall 3 — most likely to trip up non-dev) -->
<!--   6. Vercel webhook fires → build → deploy (~30s per Plan 05-02 verification) -->
<!--   7. Caleb visits https://<registered-domain>/<category>/<slug> — piece is live -->

<!-- Cross-browser matrix walk (per D-09 + 06-VERIFICATION.md Wave 0 scaffold): -->
<!--   Routes: / + /design + /design/<slug> + /about + /<bad-404> = 5 -->
<!--   Browsers: iPhone Safari + Android Chrome + desktop Safari + desktop Firefox = 4 -->
<!--   Total cells: 20 -->
<!--   Per cell: load route, confirm no console errors, exercise primary interaction (gallery click, detail open, scroll, back), mark PASS/FAIL + 1-line note -->
<!--   ~5min per cell × 20 = ~100min total -->

<!-- Android Chrome source decision (per RESEARCH §Open Questions #4): -->
<!--   1. First choice: Caleb has a personal Android device (zero-cost, real) -->
<!--   2. Fallback: BrowserStack free trial (web-based, one session covers the walk) -->
<!--   3. Last resort: Chrome DevTools Android emulation (DOES NOT satisfy SC2 real-device per Phase 5 D-15 — flag in 06-VERIFICATION if used) -->

<!-- Lighthouse re-audit (Plan 06-04 SC5 closure): -->
<!--   bash scripts/lighthouse-audit.sh https://<registered-domain> -->
<!--   Targets per route: Perf ≥80, A11y ≥95 (Phase 5 floor carried) -->
<!--   New target: /design/<slug> LCP <2000ms (D-05 fix, was 3121ms in Plan 05-08 baseline) -->
<!--   Recorded in 06-VERIFICATION.md Lighthouse Scores section -->

<!-- Reduced-motion walk (SC6 closure, D-06 stragglers): -->
<!--   iPhone 15 / iOS 26.4.2 / Settings → Accessibility → Motion → Reduce Motion ON -->
<!--   Walk the 3 NEW-exempt motions (D-06 restored): -->
<!--     #12 StatusPill hover — should FIRE (hover-scale visible) -->
<!--     #16 back-pill color — should FIRE (color transition visible) -->
<!--     #18 pager-link color — should FIRE (color transition visible) -->
<!--   Plus re-walk the Plan 05-08 9-motion checklist — confirm no regression -->

<!-- Phase-exit Lighthouse JSON output goes into a NEW directory under .planning/phases/06-deploy-maintenance-handoff/lighthouse/ -->
<!--   (mirrors Phase 5's .planning/phases/05-mobile-performance-accessibility/lighthouse/) -->
<!--   Per-route: <route-slug>-summary.json -->
-->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Caleb performs the github.dev dry run — orchestrator captures screenshots in lockstep</name>
  <action>Orchestrator sits alongside Caleb (in chat), reads each numbered step, prompts Caleb to capture a screenshot at each step via macOS Cmd-Shift-4 + Space + click, names the target filename docs/contributing/0N-*.png, and waits for "dry run complete" via resume-signal. After Caleb's signal: confirms the 6 screenshots exist, verifies live URL via `curl -I https://<registered-domain>/<category>/<slug>` returns 200, stages the screenshot binaries for commit.</action>
  <verify><human-check>Caleb returns "dry run complete" plus the live piece URL via resume-signal; orchestrator independently curl-verifies the live URL + confirms all 6 PNG files exist under docs/contributing/.</human-check></verify>
  <files>docs/contributing/01-open-github-dev.png, docs/contributing/02-create-piece-folder.png, docs/contributing/03-add-frontmatter.png, docs/contributing/04-add-hero-image.png, docs/contributing/05-commit-and-push.png, docs/contributing/06-piece-live-on-prod.png, src/content/pieces/&lt;test-piece-slug&gt;/index.md, src/content/pieces/&lt;test-piece-slug&gt;/&lt;hero-asset&gt;</files>
  <what-needed>
    SC3 is intrinsically a human task — D-01 names it "Caleb adds a piece via github.dev" and D-02 scopes it to a markdown frontmatter + image hero only (no PDF rasterization flow). Per D-01, "the dry run IS the documentation source" — screenshots are captured DURING the actual run, not staged after.

    Orchestrator (Claude Code) sits alongside Caleb (in chat) and captures each screenshot at the right moment. Caleb runs the keyboard; orchestrator says "take the screenshot now" at each numbered step, prompts Caleb to save it to `docs/contributing/0N-<name>.png`, and continues. Recommended capture tool on macOS: Cmd-Shift-4 then Space then click the github.dev window for a clean window capture; or Cmd-Shift-3 for full-screen. Caleb saves each to the right filename per RESEARCH §Recommended Project Structure.

    Test piece content suggestion (Caleb's choice — could be a real Phase 5 deferred piece or a throwaway "test piece" he later deletes; per D-02 markdown+image only):
    - slug suggestion: `design/test-dry-run` or `design/<actual-piece-name>` — Caleb picks
    - hero asset: any JPG or PNG under 1MB (the build pipeline handles webp conversion via sharp; PNG/JPG input is fine)
    - frontmatter: minimal valid set (title, category=design, role="Test", outcome="Test", context="Dry run validation", hero=./hero.jpg)

    The piece commit lands on main and goes live — it's not a throwaway in a branch. If Caleb wants to remove it after the dry run, he can do so as a follow-up commit (also a valid dry-run extension: "Caleb deletes a piece via github.dev"). But the canonical dry run is the ADD flow.
  </what-needed>
  <resume-signal>
    Type "dry run complete" plus the URL of the live piece (e.g. https://caleblim.com/design/test-dry-run) once Caleb sees the new piece on the production custom domain. Include the 6 screenshot filenames as confirmation (or describe any deviations, e.g. "drag-and-drop didn't work — used Upload button — added screenshot 04b").

    If anything in the flow trips up (and per RESEARCH §Pitfall 3 + A7 the most likely failure modes are: github.dev commit-but-not-push, drag-and-drop binary upload, or a Zod schema validation failure on the frontmatter), describe + orchestrator helps Caleb resolve + adds a footnote to README.md explaining the gotcha.

    After Caleb signals complete, executor: (a) confirms all 6 screenshots are at `docs/contributing/0N-*.png` (or whichever path was actually used — if Caleb saved to a different convention, executor moves them); (b) executor independently visits the live URL and `curl -I https://<registered-domain>/<category>/<slug>` returns 200; (c) executor stages the docs/contributing/ binaries + Caleb's piece additions for commit (Caleb already committed the piece via github.dev; executor only commits the screenshot binaries which github.dev didn't touch).
  </resume-signal>
  <done>
    Dry run is done end-to-end: piece created in github.dev → committed → pushed → Vercel auto-deployed → live on custom domain. 6 numbered screenshots committed. The maintenance-pitfall closure FOUND-04 demands is now demonstrated, not just documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate README.md with actual screenshot embeds + tighten prose against what happened</name>
  <files>README.md</files>
  <read_first>
    - README.md (Wave 0 scaffold from Plan 06-01 — six numbered placeholders ready to be filled)
    - .planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md D-01 + D-02 (walkthrough format constraints)
    - .planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md §Pitfall 3 (the commit-but-not-push gotcha to call out)
    - docs/contributing/ (the 6 screenshots from Task 1 — verify all 6 exist before editing)
    - Caleb's dry-run notes (from Task 1's resume signal — any deviations to capture in the prose)
  </read_first>
  <behavior>
    - README.md's "Adding a piece" section is now load-bearing prose: each of the 6 numbered subsections has 2-3 sentences of accurate-against-the-actual-dry-run description plus the screenshot embed.
    - The HTML comment at the top of README.md ("Wave 0 scaffold: screenshot embeds reference docs/contributing/0X-*.png which Wave 3 captures") is REMOVED — Wave 3 is the present.
    - The "Live" section's URL is updated to point at the custom domain (per Task 1 outcome).
    - Step 5 (commit and push) prose explicitly calls out the github.dev source-control-panel gotcha per RESEARCH §Pitfall 3 — non-dev users miss the "Sync" / "Commit & Push" combo and assume the commit alone is sufficient.
    - The "Adding a paginated-PDF piece" one-liner (D-02 lite-touch) stays brief — single paragraph pointing at PROJECT.md or PROJECT.md/CONTRIBUTING.md for the build-pipeline depth.
    - If Caleb hit any pitfall during Task 1 (drag-and-drop fail, schema validation error, etc.) that warrants documenting, a "Gotchas" sub-section is added at the end of the Adding a piece section with the workaround.
    - All 6 image embeds render on github.com when the README is committed + pushed.
  </behavior>
  <action>
    Open README.md (post-Plan-06-01 scaffold). Three classes of edits:

    Edit class A — top-of-file HTML comment removal: delete the `<!-- Wave 0 scaffold: ... -->` comment near the top. The walkthrough is now live, not stubbed.

    Edit class B — Live section: replace `<!-- TBD: replace with https://caleblim.com (or D-08 fallback) after Wave 2 -->` placeholder with the actual custom domain URL. Replace the "Pre-domain-flip" line with a definitive URL line like `https://<registered-domain>`.

    Edit class C — Adding a piece section: for each of the 6 numbered subsections (steps 1-6), tighten the prose. The current scaffold has generic placeholders; replace with 2-3 sentences of what actually happens per the dry run. Concrete content guidelines per step:

    Step 1 — open github.dev: 1-2 sentences naming the URL form (github.com/C-lb/caleb-lim-portfolio + press `.`, OR github.dev/C-lb/caleb-lim-portfolio direct). Mention that github.dev is browser-based VS Code with full git integration. Embed `01-open-github-dev.png`.

    Step 2 — create the piece folder: 2-3 sentences naming the folder convention (`src/content/pieces/<category>/<slug>/`) and that the category MUST be one of `design`, `finance`, `personal`, `marketing` (Zod-validated at build time per Phase 1 schema). Embed `02-create-piece-folder.png`.

    Step 3 — add index.md with frontmatter: 3-4 sentences listing the required frontmatter keys (title, category, role, outcome, context, hero) and a one-line example. Mention that if any field is missing, the Vercel build will fail loudly with a Zod error (this is by design per Phase 1's "build fails on missing fields" SC). Embed `03-add-frontmatter.png`.

    Step 4 — drop hero image: 2 sentences. github.dev supports drag-and-drop for binary uploads (per RESEARCH §A7); if it doesn't work in Caleb's browser, the workaround is the github.dev Upload command (right-click the folder → Upload). Mention preferred formats: JPG or PNG; the build pipeline auto-converts to webp via sharp. Embed `04-add-hero-image.png`.

    Step 5 — commit + push: THIS IS THE CRITICAL STEP per RESEARCH §Pitfall 3. 3-4 sentences. Open the Source Control panel (icon in the left sidebar — looks like a branching diagram). The Source Control panel shows your changes. Type a commit message in the text box at top. Click the checkmark icon to commit. CRITICAL: clicking the checkmark commits LOCALLY in github.dev — you also need to "Sync Changes" or "Push" to land it on GitHub. github.dev usually combines both via a "Commit & Push" button — look for that one if visible. The badge counter on the Source Control icon drops to 0 when both commit and push succeed. Embed `05-commit-and-push.png`.

    Step 6 — Vercel auto-deploys: 2-3 sentences. Once the push lands on main, Vercel's webhook fires (verified in Plan 05-02 Task 3 — ~30s round trip). You can watch the deploy on vercel.com/c-lb/caleb-lim-portfolio (link visible) OR just wait ~1min and refresh https://<registered-domain>/<category>/<slug> — if the piece is visible, the deploy is done. Embed `06-piece-live-on-prod.png`.

    Optional Edit class D — Gotchas section: if Caleb hit any specific pitfalls during the dry run (Task 1's resume signal will name them), add a `### Gotchas` sub-section at the end of the Adding a piece block. Each gotcha is a 1-2 sentence problem + 1-sentence workaround. If the dry run went clean with no pitfalls, skip this sub-section.

    After editing, run `npm run build && bash scripts/verify-build.sh` — confirm gates 1-27 stay GREEN. (README isn't part of the build but the build should still succeed.) Open README.md in any markdown renderer (or push the commit and refresh github.com/C-lb/caleb-lim-portfolio) to verify the image embeds render correctly.
  </action>
  <verify>
    <automated>! grep -F 'Wave 0 scaffold' README.md &amp;&amp; ! grep -F 'TBD: replace with' README.md &amp;&amp; grep -F 'docs/contributing/01-open-github-dev.png' README.md &amp;&amp; grep -F 'docs/contributing/06-piece-live-on-prod.png' README.md &amp;&amp; test -f docs/contributing/01-open-github-dev.png &amp;&amp; test -f docs/contributing/06-piece-live-on-prod.png &amp;&amp; npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | grep -F 'OK: Gate 25'</automated>
  </verify>
  <acceptance_criteria>
    - README.md no longer contains the Wave 0 HTML comment or TBD placeholders.
    - All 6 image embed references match files that actually exist under `docs/contributing/`.
    - Each numbered step has at least 2 sentences of content-specific prose (not just the embed).
    - Step 5 prose explicitly mentions the github.dev source-control-panel commit + push combo (RESEARCH §Pitfall 3).
    - Live URL line is updated to the custom domain.
    - Gates 1-27 unchanged-pass.
  </acceptance_criteria>
  <done>
    README.md is now real documentation, not a scaffold. Recruiters viewing github.com/C-lb/caleb-lim-portfolio see Caleb's actual workflow with real screenshots. SC3 documentation half is closed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run phase-exit Lighthouse audit on the custom domain + record in 06-VERIFICATION.md</name>
  <files>.planning/phases/06-deploy-maintenance-handoff/lighthouse/, .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md</files>
  <read_first>
    - scripts/lighthouse-audit.sh (the harness script — confirm it accepts a base URL arg and emits per-route summary.json files; mirror Plan 05-08's invocation pattern)
    - .planning/phases/05-mobile-performance-accessibility/lighthouse/ (Plan 05's lighthouse output directory — see the summary.json format)
    - .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md §Lighthouse Scores (the table format to mirror in 06-VERIFICATION.md)
    - .planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md D-05 (LCP target <2s for detail page)
  </read_first>
  <behavior>
    - `bash scripts/lighthouse-audit.sh https://<registered-domain>` runs successfully and emits 5 summary.json files (one per route: splash, /design, /marketing, /about, /design/design-real-piece) under a new `.planning/phases/06-deploy-maintenance-handoff/lighthouse/` directory.
    - Each summary.json contains: route, perf score, a11y score, lcp_ms, recorded_at.
    - The new detail-page lcp_ms is < 2000 (down from 3121ms in Plan 05-08 baseline — D-05 fix verified).
    - All 5 routes show Perf ≥80 AND A11y ≥95 (Phase 5 thresholds carried — splash floor was actually 85 per Plan 05-09 closure but the relaxed target for the wider gallery / detail is 80).
    - 06-VERIFICATION.md Lighthouse Scores section is populated with the actual numbers + relative-path links to the summary.json files.
    - If the detail-page LCP measures ≥2000ms (Plan 06-04 fix didn't deliver fully), executor surfaces this as a partial-success state — does NOT silently close SC5 — and notes the deferred follow-up (add format="avif" per RESEARCH §Pattern 3, or downsize the hero source asset, or both). Phase ships if all OTHER thresholds met but SC5 stays open with a clearly-named follow-up.
  </behavior>
  <action>
    Run `bash scripts/lighthouse-audit.sh https://<registered-domain>`. The script (from Phase 5 Plan 05-01) audits a fixed set of routes; it may need the route list updated to use the custom domain (read the script — likely uses an URL = $1 + route convention, so the base URL flip is sufficient).

    Move the output JSON files into a new `.planning/phases/06-deploy-maintenance-handoff/lighthouse/` directory (or wherever the script's default output is — Plan 05's was `.planning/phases/05-mobile-performance-accessibility/lighthouse/`; assume the script either defaults to the current phase's directory or accepts a -o flag; if neither, just copy the files after the script runs).

    Open `.planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md` Lighthouse Scores section. For each of the 5 routes, fill in the row:
    - Route column: the URL path
    - Perf column: the score from summary.json
    - A11y column: the score from summary.json
    - LCP (ms): the lcp_ms from summary.json
    - Pass? column: `PASS` if (perf ≥80 AND a11y ≥95) for galleries/about/detail, OR (perf ≥85 AND a11y ≥95) for splash; otherwise `FAIL` or `PASS*` with a footnote
    - Summary file column: relative-path link to the summary.json file

    Then, below the table, paste each route's summary.json content as a fenced ```json``` code block (mirrors Plan 05's `## Lighthouse Scores` section).

    Sanity-check the D-05 outcome: detail-page LCP should be <2000ms. If it is — note in the table footer: "D-05 fix verified: detail-page LCP dropped from 3121ms (Plan 05-08 baseline) to <new-value>ms via Plan 06-04's widths array addition." If it isn't (still ≥2000ms), note: "D-05 fix landed responsive variants but LCP still measures <X>ms — SC5 partial; deferred follow-up per RESEARCH §Pattern 3 recommendation (try format=avif + source asset downsize)."

    Update the SC5 row in the SC sign-off table at the bottom: Sign-off ✅ <date> if all PASS; ✅ (partial — see footnote) if D-05 LCP didn't fully clear. Evidence column references the Lighthouse Scores section + summary.json files.

    Commit the lighthouse/ directory + the 06-VERIFICATION.md edits as a single commit named `feat(phase-6/06-08): record phase-exit Lighthouse audit`.
  </action>
  <verify>
    <automated>test -d .planning/phases/06-deploy-maintenance-handoff/lighthouse &amp;&amp; ls .planning/phases/06-deploy-maintenance-handoff/lighthouse/*.json | wc -l | grep -qE '^\s*[5-9]\s*$|^\s*[1-9][0-9]+\s*$' &amp;&amp; grep -F 'Lighthouse Scores' .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md &amp;&amp; grep -E 'lcp_ms":\s*[0-9]+' .planning/phases/06-deploy-maintenance-handoff/lighthouse/*.json | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/06-deploy-maintenance-handoff/lighthouse/` directory exists with at least 5 summary.json files (one per audited route).
    - 06-VERIFICATION.md Lighthouse Scores section has all 5 route rows filled with concrete numbers.
    - SC5 row in the SC sign-off table is marked ✅ <date> (with footnote if partial).
    - Each summary.json has lcp_ms, perf, a11y, route, recorded_at fields.
    - Build still passes; Gates 1-27 still GREEN.
  </acceptance_criteria>
  <done>
    Phase-exit Lighthouse audit recorded. D-05 LCP fix measured (closes SC5 phenomenologically). If partial, the follow-up path is named in the verification doc.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Cross-browser matrix walk — Caleb walks 5 routes × 4 browsers; orchestrator records results</name>
  <files>.planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md</files>
  <action>Orchestrator dictates the matrix walk per cell (5 routes × 4 browsers = 20 cells), Caleb executes on each browser, orchestrator fills 06-VERIFICATION.md Cross-Browser Matrix rows in real-time with PASS/FAIL + 1-line note per cell. Waits for "matrix complete" via resume-signal.</action>
  <verify><human-check>Caleb returns "matrix complete" via resume-signal; orchestrator confirms all 20 cells in 06-VERIFICATION.md have PASS/FAIL markings and that any FAIL cells are dispositioned (block, accept, or defer to v1.1).</human-check></verify>
  <read_first>
    - .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md Cross-Browser Matrix section (Wave 0 scaffold — 5×4 empty grid pre-seeded)
    - .planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md D-09 (cross-browser matrix definition)
    - .planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md §Open Questions #4 (Android Chrome device source decision)
    - .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md §Critical-Path Walk (the 11-step walk pattern Caleb already used for Phase 5)
  </read_first>
  <what-built>
    Phase 6 needs SC2 cross-browser end-to-end verification: each of 5 routes loaded in each of 4 browsers, with per-cell PASS/FAIL recorded.

    Pre-checkpoint setup by the orchestrator:
    1. Confirm the Vercel deploy currently live on the custom domain matches the latest main commit (visit Vercel dashboard, confirm the most recent commit equals `git log -1 --format=%H` locally).
    2. Determine Android Chrome source per D-09 + RESEARCH §Open Questions #4:
       - Ask Caleb: do you have an Android device? If yes, use it (real-device, zero-cost, fastest).
       - If no Android device: open BrowserStack free trial at browserstack.com (web-based, sign in if account exists or sign up). One session covers the walk.
       - If neither: Chrome DevTools Android emulation is the fallback BUT per RESEARCH §A7 + Plan 05-08 D-15 real-device-only rule for Android cells, this DOES NOT satisfy SC2; flag in 06-VERIFICATION as "emulated, not real device" with executor's recommendation to follow up with real-device walk before milestone closure if Caleb has access later.
    3. Open the 06-VERIFICATION.md Cross-Browser Matrix scaffold and have it ready for live-fill.

    Walk procedure (orchestrator dictates; Caleb executes; orchestrator fills the matrix in real-time):
    For each of the 20 cells:
      a. Open the route in the browser cell's column (iPhone Safari, Android Chrome, desktop Safari, desktop Firefox).
      b. Confirm the page loads (no error, no broken-image icon, no console crash — open browser devtools console on desktop browsers).
      c. Per-route critical interactions (mirrors Plan 05-08's 11-step walk):
         - `/` (splash): tap 1 discipline card → gallery loads. Tap brand → returns to splash.
         - `/design` (gallery): tap 1 tile → detail page loads with hero rendered. Tap back-pill → returns to gallery.
         - `/design/design-real-piece` (detail): scroll the page; if paginated PDF slides exist, scroll through them; tap "Open full PDF" if present; tap pager (prev/next).
         - `/about`: scroll the bio; tap mailto/LinkedIn/resume in topbar; tap OPEN-TO-ROLES island.
         - `/<bad-404>`: confirm 404 page renders with discipline links.
      d. Mark cell PASS (no broken behaviors) or FAIL (specific brokenness — describe in the cell's note).
      e. Move to next cell. ~5min per cell = ~100min total.

    The walk is parallelizable across browsers if Caleb has multiple devices side-by-side, but per cell the walk is sequential within that browser.
  </what-built>
  <how-to-verify>
    Orchestrator confirms with Caleb at end of walk:
    1. All 20 cells in 06-VERIFICATION.md Cross-Browser Matrix have a PASS or FAIL marking + 1-line note (the empty TBD placeholders are gone).
    2. The Android Chrome column source is recorded in the matrix header note (real device / BrowserStack / Chrome DevTools emulation).
    3. Any FAIL cells have an actionable note — what broke, on which interaction. (If FAILs exist, they may block Wave 4 gap-closure unless judged as known-acceptable — orchestrator and Caleb decide per FAIL.)
    4. The SC2 row in the SC sign-off table is updated: ✅ <date> if all 20 PASS or all FAILs are accepted/deferred; otherwise the SC stays open with the FAIL list documented.

    Executor saves Caleb's notes verbatim into 06-VERIFICATION.md as the walk progresses — Caleb may say "PASS, but LinkedIn icon rendered slightly small at Android Chrome 320px" and that goes into the cell note exactly as worded.
  </how-to-verify>
  <resume-signal>
    Type "matrix complete" once all 20 cells are filled. Optionally describe any FAILs that need follow-up (executor will then either add them to a gap-closure list OR — if minor and Caleb accepts them — annotate the cell note and leave SC2 closed).

    If Android Chrome cells were done via Chrome DevTools emulation (last-resort fallback per RESEARCH §Open Questions #4), flag this in the resume signal — executor adds a "deferred real-device Android walk" note to 06-VERIFICATION but does NOT block milestone closure (a single deferred-real-device-walk note is acceptable per the SC2 standard if BrowserStack/real-device wasn't available; the milestone ships and the follow-up gets added to v1.1 backlog).
  </resume-signal>
  <done>
    Cross-browser matrix is 100% filled. SC2 sign-off row marked. Any FAILs are documented + dispositioned (block or accept).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Real-iPhone reduced-motion walk validates D-06 closure</name>
  <files>.planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md</files>
  <action>Orchestrator walks Caleb through the 12-motion checklist (9 from Plan 05-08 baseline + 3 newly-restored D-06 exempt motions) on iPhone 15 with iOS Reduce Motion ON, fills 06-VERIFICATION.md Reduced-Motion Walk rows in real-time. Waits for "reduced-motion walk complete" via resume-signal.</action>
  <verify><human-check>Caleb returns "reduced-motion walk complete" plus per-motion summary via resume-signal; orchestrator confirms all 12 rows match expected D-08 classifications.</human-check></verify>
  <read_first>
    - .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md Reduced-Motion Walk section (Wave 0 scaffold — 12 motion rows including the 3 new D-06 exempt-fire rows)
    - .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md §Reduced-Motion Walk (Plan 05-08's 9-motion walk for the carryover comparison)
    - .planning/phases/05-mobile-performance-accessibility/05-06-SUMMARY.md §Motion-by-motion verification (D-08 exempt vs suppressed classifications)
  </read_first>
  <what-built>
    SC6 (folded Phase 5 carryover — D-06 stragglers) needs phenomenological verification on a real iPhone. Plan 06-05 deleted the three `transition: none` overrides at the source level; Gate 27 locks them via grep. Plan 06-08 walks the actual iPhone with iOS Reduce Motion ON and confirms the three previously-suppressed exempt motions now fire as designed.

    Walk procedure (mirror Plan 05-08's reduced-motion walk pattern):
    1. iPhone 15 / iOS 26.4.2 / Safari (stock, no content blockers — same as Plan 05-08 rig).
    2. Settings → Accessibility → Motion → Reduce Motion ON.
    3. Hard refresh the site (https://<registered-domain>/).
    4. Walk the 12 motions (9 from Plan 05-08 + 3 newly-restored from D-06):

    Re-check Plan 05-08's 9 motions (regression check — no behavior change expected):
       #1 Carousel auto-advance → SUPPRESSED (JS-gated; no change)
       #2 Carousel slide transition → SUPPRESSED (CSS guard; no change)
       #3 Card entrance shake → SUPPRESSED (Plan 05-06 surgical disable; no change)
       #4 Bio entrance shake → SUPPRESSED (Plan 05-06 surgical disable; no change)
       #5 Card hover-tilt → EXEMPT FIRES (D-08; no change — desktop verification)
       #6 Card click-shake → EXEMPT FIRES (no change)
       #7 Liquid-glass overlay fade on .b-card → EXEMPT FIRES (no change)
       #11 Lime-dot pulse on StatusPill → EXEMPT FIRES (no change)
       #14 Slow-scroll on /about?to=contact → SUPPRESSED (JS-gated; no change)

    New verification for D-06 stragglers (these MUST now fire — Plan 06-05 deleted their suppressors):
       #12 StatusPill hover-scale (on desktop, but the underlying CSS rule is also active on mobile tap; check tap-and-hold behavior on the iPhone for the underlying transition) → EXEMPT FIRES (verify on desktop alongside iPhone, since hover is desktop-primary)
       #16 Back-pill color transition (tap a tile, scroll back to gallery, observe the back-pill color shift on hover/focus) → EXEMPT FIRES (color-only — visible as a color change on the back-pill text/border)
       #18 Pager-link color transition (on a detail page, observe the prev/next pager-link hover/focus color shift) → EXEMPT FIRES (color-only)

    For each motion, mark PASS (behavior matches expected) or FAIL (behavior doesn't match — describe).
  </what-built>
  <how-to-verify>
    Orchestrator and Caleb walk the iPhone together (chat-paced). For each of the 12 motions:
    1. Trigger the motion (per the row's "Step" column in 06-VERIFICATION.md Reduced-Motion Walk).
    2. Observe whether the motion fires or is suppressed.
    3. Compare against the row's "Expected behavior" column.
    4. Mark PASS or FAIL.

    Caleb's eye is the gate. Some of these are visually subtle (color-only transitions on #16 and #18) — Caleb may need to slow-mo capture (iPhone screen recording + Files app review at 0.25× speed) to confirm. For the canonical D-06 deltas (#12, #16, #18), the SHOULD-FIRE behaviors are visible on a normal-paced walk — if Caleb can't see the motion at normal speed, it may not be firing (suppressor still present somewhere not caught by Gate 27 — surface for diagnosis).

    On any FAIL: orchestrator + Caleb diagnose. Common cause: caching (hard-refresh the page); CSS scope error (different selector in a file Plan 06-05 didn't touch). If diagnosis surfaces a new straggler, add to gap-closure list — milestone closure pauses until SC6 actually clears.

    Update the SC6 row in the SC sign-off table: ✅ <date> if all 12 motions match expected; otherwise leave SC6 open with the failing motion documented.
  </how-to-verify>
  <resume-signal>Type "reduced-motion walk complete" plus per-motion summary (e.g. "12/12 PASS" or "11/12 PASS — #16 back-pill color still suppressed somewhere, investigating"). If a regression is found, executor follows up with a diagnostic grep to locate the unsuppressed suppressor.</resume-signal>
  <done>
    Reduced-motion walk recorded in 06-VERIFICATION.md. SC6 sign-off marked. D-06 stragglers verified gone phenomenologically AND structurally (Gate 27).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: LinkedIn Post Inspector + opengraph.xyz preview captured for the OG card</name>
  <files>.planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md</files>
  <action>Orchestrator opens LinkedIn Post Inspector + opengraph.xyz against the custom-domain URL; waits for tool-rendered previews; reads the results to Caleb for visual confirmation; saves preview URLs + screenshots into 06-VERIFICATION.md OG / SEO Sign-Off section.</action>
  <verify><human-check>Caleb returns "og previews captured" via resume-signal with the preview URLs; orchestrator confirms the OG card image renders correctly per D-03 brand spec in both preview tools.</human-check></verify>
  <read_first>
    - .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md OG / SEO Sign-Off section (Wave 0 scaffold)
    - .planning/phases/06-deploy-maintenance-handoff/06-RESEARCH.md §Pitfall 5 (LinkedIn 7-day cache; force re-fetch via Post Inspector)
    - .planning/phases/06-deploy-maintenance-handoff/06-CONTEXT.md D-03 (OG card brand constraints)
  </read_first>
  <what-built>
    Plan 06-03 shipped the OG card + meta block. Plan 06-07 flipped the host to the custom domain. The OG card is now reachable at https://<registered-domain>/og.png and meta tags reference it as an absolute URL. SC4's OG-half needs phenomenological verification — how does it actually render in LinkedIn and other share previews?

    Three preview tools to check (per RESEARCH §Validation Architecture + §A4):
    1. LinkedIn Post Inspector at https://www.linkedin.com/post-inspector/
    2. opengraph.xyz at https://www.opengraph.xyz/url/<encoded-url>
    3. (Optional) Twitter Card Validator if still alive — per RESEARCH §A4 cards-dev.twitter.com may be deprecated. opengraph.xyz covers Twitter card preview as a fallback.

    Pre-checkpoint actions:
    1. Open LinkedIn Post Inspector. Paste `https://<registered-domain>`. Click Inspect.
    2. LinkedIn fetches the meta and renders a preview card. Confirm: 1200×630 image is visible, "CALEB LIM" brand mark renders, accent color visible at small thumbnail size.
    3. RESEARCH §Pitfall 5: if Post Inspector shows a stale or "no card" state, click the refresh button — LinkedIn caches for 7 days; first-time inspection forces a re-fetch.
    4. Screenshot the preview. Save as `docs/contributing/og-linkedin-preview.png` (or similar — Caleb's discretion on filename).
    5. Open opengraph.xyz at https://www.opengraph.xyz/url/https%3A%2F%2F<registered-domain>. Wait for the multi-platform preview to render (LinkedIn, Twitter, Facebook, Slack, Discord previews all on one page).
    6. Screenshot or save the URL with a 06-VERIFICATION.md note.
  </what-built>
  <how-to-verify>
    Caleb visually confirms in each tool:
    1. The image renders (not a "no image" placeholder or broken-image icon).
    2. The image is the OG card from Plan 06-03 (cream background, ink brand mark, accent visible).
    3. The card title and description match what's set in Base.astro (title varies per page; description is the static "Caleb Lim — cross-functional generalist..." string).
    4. The aspect ratio is 1200:630 (LinkedIn renders this; opengraph.xyz shows multiple preview shapes — LinkedIn-feed should be the relevant one).

    If LinkedIn Post Inspector renders correctly, paste the preview URL + screenshot into 06-VERIFICATION.md OG / SEO Sign-Off section under a "LinkedIn Post Inspector" row.

    If opengraph.xyz renders correctly, do the same under an "opengraph.xyz" row.

    Update the SC4 row in the SC sign-off table: ✅ <date>. Evidence: "OG card 1200×630 rendered correctly on LinkedIn + opengraph.xyz; sitemap-index + robots.txt verified via curl in Plan 06-07; see OG / SEO Sign-Off section."

    Note RESEARCH §Pitfall 5 deferral: LinkedIn's 7-day cache means if Caleb later shares the URL organically, the first organic share may show an OLDER cached version (e.g. if a colleague had previously shared the Vercel default subdomain URL during pre-domain Wave 1 development). The Post Inspector force-fetch is the canonical re-cache trigger — note this in 06-VERIFICATION as a Caleb-facing footnote.
  </how-to-verify>
  <resume-signal>Type "og previews captured" plus the URLs (LinkedIn Post Inspector result URL + opengraph.xyz result URL) and confirmation that the OG card image renders correctly. If anything looks wrong (e.g. og:image not picked up, wrong aspect ratio, image missing), diagnose with executor — most likely cause is a recent cache issue at the previewer (force-refresh) or the og:image URL pointing somewhere stale (re-verify Astro.site flip from Plan 06-07).</resume-signal>
  <done>
    Both preview tools render the OG card correctly. SC4 sign-off row marked. The brand-critical artifact's runtime behavior is verified.
  </done>
</task>

<task type="auto">
  <name>Task 7: Finalize 06-VERIFICATION.md sign-off + flip STATE.md + ROADMAP.md to milestone v1.0 complete</name>
  <files>.planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md (current state after Tasks 1-6 — all SC rows should be either ✅ or have a clearly-named follow-up)
    - .planning/STATE.md (current frontmatter + Current Position + Performance Metrics sections — these get updated to reflect milestone closure)
    - .planning/ROADMAP.md Phase 6 section (lines 26 and 147-162 — Phase 6 entry + Phase Details — the checkboxes flip from [ ] to [x])
    - .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md (the canonical phase-exit doc to mirror sign-off completeness)
  </read_first>
  <behavior>
    - 06-VERIFICATION.md SC sign-off table at the bottom has ✅ <date> + evidence in every row (SC1 through SC6). Any SC that wasn't fully closed (e.g. partial Lighthouse pass) carries a "✅ (partial — see footnote)" with explicit follow-up named.
    - 06-VERIFICATION.md Phase Exit Sign-Off checklist has ALL boxes ticked.
    - 06-VERIFICATION.md final line `Phase 6 verdict: COMPLETE (YYYY-MM-DD)` replaces the Wave 0 `TBD` line.
    - STATE.md frontmatter: `status: complete` or equivalent (mirror what was set after Phase 5 — read STATE.md for the exact pattern); `stopped_at: Milestone v1.0 shipped — Phase 6 complete YYYY-MM-DD; site live at https://<registered-domain> with full SEO bundle + Caleb-runs-it dry run captured`; `progress.completed_phases: 6`; `progress.completed_plans: 36` (28 prior + 8 new); `progress.percent: 100`; `last_activity: <date> -- Phase 6 SC sign-off complete; milestone v1.0 shipped`.
    - STATE.md Current Position section: `Phase: 06 — COMPLETE`; `Plan: 8 of 8`; `Status: Milestone v1.0 shipped`.
    - STATE.md Session Continuity: `Last session: <date>T<time>Z`; `Stopped at: Milestone v1.0 shipped on YYYY-MM-DD`; `Resume command: (none — milestone complete)` or `Resume command: /gsd-retrospective milestone v1.0` if the project supports per-milestone retrospectives.
    - ROADMAP.md line 26 (`- [ ] **Phase 6: Deploy & Maintenance Handoff**`): flips to `- [x] **Phase 6: Deploy & Maintenance Handoff**`.
    - ROADMAP.md Phase 6 Plans list (after line 161 `**Plans**: TBD`): replace `TBD` with `8 plans`; populate the per-plan checkbox list with all 8 plans flipped to `[x]`.
    - ROADMAP.md Progress table at line 183-190 — Phase 6 row updated: `Plans Complete: 8/8`, `Status: Complete`, `Completed: YYYY-MM-DD`.
    - No new gates added; no code touched; this task is doc-state finalization.
  </behavior>
  <action>
    Edit 1 — `.planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md`:

    Walk every SC row (SC1 through SC6) in the bottom SC sign-off table. For each, confirm Sign-off column has ✅ + date and Evidence column references the relevant filled section. If any row is still TBD, that means an upstream task didn't complete cleanly — pause this task and circle back to fix the upstream gap before proceeding.

    Walk the Phase Exit Sign-Off checklist near the top of the bottom section. Tick every box that's been satisfied. The expected list mirrors 05-VERIFICATION.md §Phase Exit Sign-Off:
    - [x] All scripts/verify-build.sh gates green (1-27)
    - [x] All Lighthouse thresholds met (Perf ≥80 every route; A11y ≥95 every route; splash LCP <2000ms; detail LCP <2000ms post-D-05 fix)
    - [x] Real-iPhone critical-path walk recorded
    - [x] Reduced-motion walk recorded (12 motions per Task 5)
    - [x] Cross-browser matrix complete (20 cells per Task 4)
    - [x] OG card LinkedIn Post Inspector + opengraph.xyz preview captured
    - [x] Caleb-adds-a-piece dry run captured (6 screenshots committed under docs/contributing/)
    - [x] README.md populated and embeds render on github.com
    - [x] Vercel custom domain verified (Plan 06-07 cert chain + Vercel "Valid Configuration")
    - [x] Domain availability check log filled (Plan 06-07)
    - [x] FOUND-04 traceability row in REQUIREMENTS.md marked Complete (Plan 06-07)

    Replace the final line `Phase 6 verdict: TBD` with `**Phase 6 verdict: COMPLETE** (YYYY-MM-DD)`.

    Edit 2 — `.planning/STATE.md`:

    Update the frontmatter: `status: complete`, `stopped_at: "Milestone v1.0 shipped — Phase 6 complete YYYY-MM-DD; site live at https://<registered-domain> with full SEO bundle + Caleb-runs-it dry run captured"`, `progress.completed_phases: 6`, `progress.completed_plans: 36` (or whatever the actual count is — sum from ROADMAP), `progress.percent: 100`, `last_activity: "YYYY-MM-DD -- Phase 6 SC sign-off complete; milestone v1.0 shipped"`.

    Update the Current Position section to reflect milestone closure. Add a `Phase 6 plan inventory:` table at the bottom of Session Continuity following the pattern Plan 05-08 used for `Phase 5 plan inventory`.

    Edit 3 — `.planning/ROADMAP.md`:

    Line 26: flip `[ ]` to `[x]` for Phase 6.

    Phase 6 detail section (lines 147-162): below the `**Plans**: TBD` (or its current content from the planner's initial write) — flip every plan checkbox from `[ ]` to `[x]`. The wave structure to mirror is:
    ```
    **Plans**: 8 plans
    Plans:
    **Wave 0**
    - [x] 06-01-PLAN.md — Verification scaffolding (06-VERIFICATION.md + Gates 26a/b/c + Gate 27 + README scaffold)
    **Wave 1** (parallel; all depends_on [06-01])
    - [x] 06-02-PLAN.md — Sitemap + robots.txt slice (closes Gate 26b + 26c)
    - [x] 06-04-PLAN.md — Detail-page LCP fix slice (D-05 widths array)
    - [x] 06-06-PLAN.md — D-07 doc-rot amendments slice (CLAUDE.md + PROJECT.md)
    **Wave 2** (sequential file-conflict-resolved deps after Wave 1)
    - [x] 06-03-PLAN.md — OG card + meta block slice (depends_on [06-01, 06-02])
    - [x] 06-05-PLAN.md — D-06 straggler removal slice (depends_on [06-01, 06-04])
    **Wave 3** (depends_on [06-02, 06-03])
    - [x] 06-07-PLAN.md — Domain registration + DNS + HTTPS + 2-step site flip (closes FOUND-04)
    **Wave 4** (depends_on [06-04, 06-05, 06-06, 06-07])
    - [x] 06-08-PLAN.md — Caleb dry run + README population + cross-browser matrix + Lighthouse re-audit + reduced-motion walk + phase-exit sign-off (closes SC2, SC3, SC5, SC6)
    ```

    Progress table at lines 183-190 — find the Phase 6 row, update Plans Complete to 8/8, Status to Complete, Completed to today's date.

    After all 3 edits, run `npm run build && bash scripts/verify-build.sh` — confirm gates 1-27 stay GREEN. Build still succeeds.

    Commit as a single commit named `feat(phase-6/06-08): close milestone v1.0 — Phase 6 complete`. Push to main; Vercel auto-deploys (mostly a no-op since this commit touches only docs).

    After push, send a final status to Caleb confirming milestone v1.0 is shipped.
  </action>
  <verify>
    <automated>grep -F 'Phase 6 verdict: COMPLETE' .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md &amp;&amp; ! grep -F 'Phase 6 verdict: TBD' .planning/phases/06-deploy-maintenance-handoff/06-VERIFICATION.md &amp;&amp; grep -E '^status:\s*complete' .planning/STATE.md &amp;&amp; grep -F '- [x] **Phase 6: Deploy & Maintenance Handoff**' .planning/ROADMAP.md &amp;&amp; grep -F '- [x] 06-08-PLAN.md' .planning/ROADMAP.md &amp;&amp; npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/06-08-t7.log &amp;&amp; grep -F 'OK: Gate 27' /tmp/06-08-t7.log &amp;&amp; grep -F 'OK: Gate 26a' /tmp/06-08-t7.log</automated>
  </verify>
  <acceptance_criteria>
    - 06-VERIFICATION.md final line says `Phase 6 verdict: COMPLETE`, not TBD.
    - STATE.md frontmatter `status: complete`, `completed_phases: 6`, `percent: 100`.
    - ROADMAP.md line 26 Phase 6 checkbox is `[x]`.
    - ROADMAP.md Phase 6 detail section lists all 8 plans with `[x]` checkboxes in the wave structure.
    - ROADMAP.md Progress table Phase 6 row shows 8/8 Complete + date.
    - All scripts/verify-build.sh gates 1-27 GREEN.
    - Build still passes; final commit lands on main; Vercel deploys cleanly.
  </acceptance_criteria>
  <done>
    Phase 6 closed. Milestone v1.0 shipped. STATE.md + ROADMAP.md reflect the new reality. Phase exit doc is fully signed off. The Caleb Lim Portfolio is live, owned by Caleb, maintainable by Caleb via github.dev, and the README documents the workflow for future self-maintenance.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| github.dev → main → Vercel auto-deploy | Caleb's GitHub identity authorizes both the in-browser commit and the Vercel deploy. |
| docs/contributing/0X-*.png binaries → public repo | Screenshots may incidentally capture browser tabs, bookmarks, email addresses in the github.dev UI. |
| 06-VERIFICATION.md OG previews | LinkedIn Post Inspector requires a LinkedIn login but doesn't expose Caleb's session to the inspected URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-22 | Information Disclosure | Screenshots may leak email addresses / unrelated browser tabs / bookmarks | mitigate | Caleb captures clean github.dev windows for each step (Cmd-Shift-4 + Space + click). Orchestrator visually reviews each screenshot before commit to ensure no incidental PII leak (other email addresses, unrelated chat windows, bookmark bars with sensitive site names). If a leak is spotted post-capture, redact before commit (Preview's annotation tool) or re-capture. |
| T-06-23 | Tampering | Caleb's dry-run commit to main triggers production deploy | accept | This is the intentional FOUND-04 closure contract — committing to main IS the production deploy authorization. The threat is the feature. The README's commit-and-push prose (Task 2 Edit C Step 5) explicitly warns Caleb that pushing to main = live deploy. |
| T-06-24 | Denial of Service | Wave 3 testing exhausts Lighthouse free-tier or BrowserStack free-tier | accept | Lighthouse runs locally (lighthouse-audit.sh uses npx lighthouse — no quota). BrowserStack free trial is one session; if exhausted, fall back to real Android (Caleb's device) or document the gap. |
| T-06-25 | Spoofing | Custom domain typosquat | accept | Out of Phase 6 scope. Cloudflare Registrar's WHOIS privacy + DNSSEC + auto-renewal close the major squat vectors at the registrar layer. |

ASVS Level 1 informational. Final phase shipping work. No new auth, no DB, no new attack surface.
</threat_model>

<verification>
- 06-VERIFICATION.md all 10 sections filled; SC sign-off table 6/6 ✅; final verdict COMPLETE.
- 6 screenshots in docs/contributing/ named 01-* through 06-*.png; total binary size <2MB (well under github.com's 100MB-per-file limit).
- README.md embeds render on github.com (visual confirmation by visiting github.com/C-lb/caleb-lim-portfolio).
- Lighthouse summary JSONs committed under .planning/phases/06-deploy-maintenance-handoff/lighthouse/.
- STATE.md status: complete, progress 100%, completed_phases 6/6.
- ROADMAP.md Phase 6 checkbox [x]; all 8 plans listed and checked.
- `npm run build && bash scripts/verify-build.sh` keeps Gates 1-27 GREEN.
- Custom-domain `curl -I https://<registered-domain>/` returns 200 OK with valid Let's Encrypt cert.
- One new piece by Caleb is live at `https://<registered-domain>/<category>/<slug>` (from the dry run).
</verification>

<success_criteria>
SC1 (HTTPS + DNS) ✅ via Plan 06-07.
SC2 (cross-browser end-to-end) ✅ via Task 4.
SC3 (Caleb dry run + README) ✅ via Tasks 1 + 2.
SC4 (OG card + sitemap + robots) ✅ via Plans 06-02 + 06-03 + Task 6.
SC5 (detail-page LCP <2s) ✅ via Plan 06-04 + Task 3 measurement.
SC6 (D-06 stragglers + D-08 motion-by-motion) ✅ via Plan 06-05 + Task 5.
FOUND-04 closed via Plan 06-07.
Milestone v1.0 shipped.
</success_criteria>

<output>
Create `.planning/phases/06-deploy-maintenance-handoff/06-08-SUMMARY.md`. Note:
- Dry-run outcome: which slug Caleb added, which hero asset, any pitfalls encountered (RESEARCH §Pitfall 3 commit-and-push, RESEARCH §A7 drag-and-drop, Zod schema validation failures).
- README.md screenshot count + total file size (for future-archive reference).
- Cross-browser matrix outcome: 20/20 PASS expected; any FAILs documented + dispositioned.
- Lighthouse final numbers for all 5 routes (perf, a11y, lcp_ms) + the D-05 LCP delta (from 3121ms baseline).
- Reduced-motion walk outcome: 12/12 motions expected to match D-08 classifications; any anomalies.
- OG previews captured: paste the LinkedIn Post Inspector + opengraph.xyz result URLs.
- STATE.md + ROADMAP.md before/after deltas.
- Final milestone v1.0 status: site URL, deploy commit hash, total Phase 6 duration (start = 2026-05-20 CONTEXT commit; end = today).
- Pointer to optional next steps: /gsd-retrospective on milestone v1.0, follow-up backlog from any partial SC closures (e.g. Phase 6 D-15 cross-browser FAIL items moved to v1.1 polish backlog).
</output>
</content>
