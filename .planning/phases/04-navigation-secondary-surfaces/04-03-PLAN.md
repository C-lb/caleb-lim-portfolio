---
phase: 04-navigation-secondary-surfaces
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/pages/about.astro
autonomous: true
requirements: [CONTACT-05]
requirements_addressed: [CONTACT-05]
tags: [navigation, about, contact-block]

must_haves:
  truths:
    - "dist/about/index.html renders a <section class=\"contact\"> below the existing resume-link paragraph, inside the <article class=\"about\"> container."
    - "The contact section contains TWO rows: an email row pointing at mailto:caleblimster@gmail.com, and a LinkedIn row pointing at https://linkedin.com/in/caleblkr."
    - "The LinkedIn anchor in the About contact block carries target=\"_blank\" rel=\"noopener noreferrer\" (Gate 20 catches misses)."
    - "Calendly is NOT present in the contact block (user decision; researcher confirmed skip)."
    - "Gate 19e (CONTACT-05) flips to GREEN; verify-build.sh now passes overall (exit 0)."
    - "The contact section uses ONLY existing Phase 3 tokens — no new CSS custom properties introduced."
  artifacts:
    - path: "src/pages/about.astro"
      provides: "Contact section appended below .resume-link with email + LinkedIn rows + scoped CSS; existing bio + resume + back-pill preserved verbatim"
      min_lines: 130
  key_links:
    - from: "contact section in about.astro"
      to: "<a href=\"mailto:caleblimster@gmail.com\">"
      via: "literal anchor inside <li>"
      pattern: "href=\"mailto:caleblimster@gmail\\.com\""
    - from: "contact section LinkedIn anchor"
      to: "rel=\"noopener noreferrer\" + target=\"_blank\""
      via: "literal attributes on the same <a> tag"
      pattern: "target=\"_blank\"[^>]*rel=\"[^\"]*noopener[^\"]*noreferrer"
---

<objective>
Append a `<section class="contact">` to `src/pages/about.astro` below the existing `.resume-link` paragraph, containing two contact rows (email + LinkedIn). Mirror the typographic vocabulary the detail page uses for Context/Role/Outcome labels (mono uppercase) so the contact block reads as the same vocabulary as the rest of the site. Skip Calendly per user lock.

Purpose: Closes CONTACT-05 — the slightly-larger contact surface for recruiters who already read the bio and want a deliberate place to act on it. Flips Gate 19e (RED at end of Plan 04-01) to GREEN; this is the final Phase 4 gate. After this plan ships, `bash scripts/verify-build.sh` returns exit 0.

Output: One commit, two tasks. Independent of Plan 04-02 — different file, different verify-build.sh section. Both Wave 2 plans can run in parallel; Plan 04-03 depends only on Plan 04-01 (which authored Gate 19e).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-navigation-secondary-surfaces/04-RESEARCH.md
@.planning/phases/04-navigation-secondary-surfaces/04-PATTERNS.md
@.planning/phases/04-navigation-secondary-surfaces/04-01-PLAN.md
@src/pages/about.astro
@src/styles/tokens.css

<interfaces>
<!-- Key constants the executor needs. Hardcode in tasks; do NOT prompt user. -->

From .planning/phases/04-navigation-secondary-surfaces/04-RESEARCH.md user_constraints:
- mailto: caleblimster@gmail.com
- LinkedIn URL: https://linkedin.com/in/caleblkr  (https:// prefix MANDATORY)
- Calendly: SKIPPED — do NOT include scheduler links in the About contact block, despite the CONTACT-05 text in REQUIREMENTS.md reading "email + LinkedIn + optional Calendly"
- Phase 3 design tokens LOCKED — use only existing custom properties

Existing about.astro structure (PATTERNS.md "src/pages/about.astro"):
- Line 8: `<article class="about">` opens
- Line 9: back-pill `<a href="/" class="b-cat-back">← splash</a>` (Phase 3 — DO NOT MODIFY)
- Line 10: `<h1>Caleb Lim</h1>`
- Lines 11–21: bio `<p>` (Phase 2 D-12/D-14 contract — DO NOT MODIFY copy)
- Lines 22–24: `<p class="resume-link">` (Phase 2 ABOUT-01 + CONTACT-01 — DO NOT MODIFY)
- Line 25: `</article>` closes
- Insertion point: after line 24, before line 25.

Existing body-anchor typography to inherit (about.astro:78–90):
```css
.about p a {
  color: var(--ink);
  text-decoration: underline;
  text-decoration-thickness: 1px;
  text-underline-offset: 0.18em;
}
.about p a:hover { color: var(--terracotta); }
.about p a:focus-visible { outline: 2px solid var(--ink); outline-offset: 2px; }
```
The new `.contact-list a` rule mirrors this verbatim with one addition: `font-family: var(--serif)` (the `<section class="contact">` is OUTSIDE `.about p` scope, so the rule must restate the inherited font).

CRO label vocabulary to copy from [category]/[slug].astro:164–172 (PATTERNS.md S4):
```css
font-family: var(--mono);
font-size: var(--fs-mono);
font-weight: 600;
letter-spacing: 0.16em;
text-transform: uppercase;
color: var(--ink);
opacity: 0.6;
```

External-link safety convention (Pattern S1 — established by Plan 04-01's LinkedIn header anchor; this is the SECOND occurrence in the codebase):
```astro
<a href="https://linkedin.com/in/caleblkr" target="_blank" rel="noopener noreferrer">…</a>
```

Tokens available (tokens.css:6–50): --paper, --ink, --terracotta, --sans, --serif, --mono, --fs-body, --fs-mono, --sp-1, --sp-2, --sp-4, --sp-5, --sp-6, --sp-8, --sp-10, --lh-bio.
Note: there is NO --sp-3 or --sp-7; the scale is intentionally gapped. The contact block uses --sp-2, --sp-4, --sp-5.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append contact section to about.astro + scoped CSS</name>
  <files>src/pages/about.astro</files>
  <read_first>
    - src/pages/about.astro (full file — own analog; insertion point is after line 24, before line 25)
    - src/pages/about.astro lines 22–24 — the existing `.resume-link` paragraph (DO NOT MODIFY)
    - src/pages/about.astro lines 78–90 — body-anchor underline pattern that `.contact-list a` mirrors
    - src/pages/about.astro lines 97–99 — existing `@media (max-width: 900px)` block (the contact block adds a SEPARATE `@media (max-width: 600px)` block per PATTERNS.md — do NOT merge)
    - src/pages/[category]/[slug].astro lines 164–172 — CRO label typography (mono, uppercase, 0.16em tracking, opacity 0.6) that `.contact-list .label` copies
    - .planning/phases/04-navigation-secondary-surfaces/04-RESEARCH.md "Pattern 4: About contact block layout" lines 308–377 — the exact JSX + CSS recipe
    - .planning/phases/04-navigation-secondary-surfaces/04-PATTERNS.md "src/pages/about.astro" lines 171–207
    - src/styles/tokens.css lines 6–50 — confirm available tokens
    - src/layouts/Base.astro (post-Plan-04-01 state) — confirm header chrome flowed in; `<main id="main">` slot wrap is now in place around the About `<article>`
  </read_first>
  <behavior>
    - The existing `<article class="about">` gains a new child after `.resume-link` (line 24): a `<section class="contact" aria-labelledby="contact-h">` containing an `<h2 id="contact-h">Get in touch</h2>` and a `<ul class="contact-list">` with two `<li>` rows.
    - Row 1: `<span class="label">email</span>` + `<a href="mailto:caleblimster@gmail.com">caleblimster@gmail.com</a>`.
    - Row 2: `<span class="label">linkedin</span>` + `<a href="https://linkedin.com/in/caleblkr" target="_blank" rel="noopener noreferrer">linkedin.com/in/caleblkr</a>`.
    - The visible mailto anchor text is `caleblimster@gmail.com` (matches the href).
    - The visible LinkedIn anchor text is `linkedin.com/in/caleblkr` (matches the href without the protocol prefix — readability convention).
    - No Calendly row, no third row, no "optional" affordance (researcher + user both flagged skip).
    - The bio paragraph (lines 11–21), the `.resume-link` paragraph (lines 22–24), the back-pill (line 9), and the `<h1>` (line 10) all stay byte-identical.
    - Existing `@media (max-width: 900px)` block at line 97 is NOT modified; the contact block adds a SEPARATE `@media (max-width: 600px)` block per PATTERNS.md note (different responsive scope; the existing 900px rule scopes `.about` padding, the new 600px rule scopes `.contact-list` grid).
    - All new CSS consumes existing tokens only — no new `--<name>:` definitions.
  </behavior>
  <action>
    Make two discrete edits to `src/pages/about.astro`:

    **(1) Insert the contact section.** Between line 24 (closing `</p>` of `.resume-link`) and line 25 (closing `</article>`), insert:

    ```astro
        {/* CONTACT-05 — slightly larger contact surface for recruiters who already read the bio.
            Calendly intentionally skipped (researcher + user lock; CONTACT-05 text reads "optional Calendly").
            External LinkedIn anchor establishes the Phase 4 SECOND target=_blank in codebase (first: header in Base.astro). */}
        <section class="contact" aria-labelledby="contact-h">
          <h2 id="contact-h">Get in touch</h2>
          <ul class="contact-list">
            <li>
              <span class="label">email</span>
              <a href="mailto:caleblimster@gmail.com">caleblimster@gmail.com</a>
            </li>
            <li>
              <span class="label">linkedin</span>
              <a href="https://linkedin.com/in/caleblkr" target="_blank" rel="noopener noreferrer">linkedin.com/in/caleblkr</a>
            </li>
          </ul>
        </section>
    ```

    **(2) Append scoped CSS for the contact block.** Inside the existing `<style>` block, AFTER the `.resume-link a` rule at lines 91–95 and BEFORE the `@media (max-width: 900px)` block at line 97, append:

    ```css
    /* Phase 4 — CONTACT-05 contact block. Mirrors CRO label typography from
       [category]/[slug].astro:164–172 (mono uppercase, 0.16em tracking, opacity 0.6).
       External-link safety convention: see Plan 04-01 / Pattern S1. No new tokens. */
    .contact {
      display: flex;
      flex-direction: column;
      gap: var(--sp-4);
      margin-top: var(--sp-5);
    }
    .contact h2 {
      font-family: var(--mono);
      font-size: var(--fs-mono);
      letter-spacing: 0.16em;
      text-transform: uppercase;
      font-weight: 600;
      color: var(--ink);
      opacity: 0.6;
      margin: 0;
    }
    .contact-list {
      display: flex;
      flex-direction: column;
      gap: var(--sp-2);
    }
    .contact-list li {
      display: grid;
      grid-template-columns: 100px 1fr;
      gap: var(--sp-4);
      align-items: baseline;
    }
    .contact-list .label {
      font-family: var(--mono);
      font-size: var(--fs-mono);
      letter-spacing: 0.16em;
      text-transform: uppercase;
      font-weight: 600;
      color: var(--ink);
      opacity: 0.6;
    }
    .contact-list a {
      font-family: var(--serif);
      font-size: var(--fs-body);
      color: var(--ink);
      text-decoration: underline;
      text-decoration-thickness: 1px;
      text-underline-offset: 0.18em;
    }
    .contact-list a:hover { color: var(--terracotta); }
    .contact-list a:focus-visible {
      outline: 2px solid var(--ink);
      outline-offset: 2px;
    }
    ```

    Then append a SEPARATE `@media (max-width: 600px)` block at the very end of the `<style>` block (after the existing `@media (max-width: 900px)` block at line 99). Do NOT merge into the 900px block — PATTERNS.md line 206 explicitly notes the two breakpoints are independent CSS scopes:

    ```css
    @media (max-width: 600px) {
      .contact-list li { grid-template-columns: 1fr; gap: var(--sp-1); }
    }
    ```

    Notes on what NOT to change:
    - DO NOT touch lines 9, 10, 11–21, 22–24 of the JSX (back-pill, h1, bio, resume-link).
    - DO NOT touch the existing `.about` block CSS, the `.b-cat-back` CSS, the `.about h1` CSS, the `.about p` CSS, the `.about p a` CSS, or the `.resume-link a` CSS — all stay byte-identical.
    - DO NOT introduce new tokens. Verify diff: `git diff src/pages/about.astro | grep -E '^\+.*--[a-z][a-z0-9-]+:' | grep -v 'var('` returns nothing.
    - DO NOT add a Calendly link, "schedule a call" button, or any third row.
    - The LinkedIn `target="_blank" rel="noopener noreferrer"` is mandatory — Gate 20 (Plan 04-01) catches mistakes.

    After editing, run:
    ```bash
    npm run build 2>&1 | tail -20
    ```

    Then sanity-grep:
    ```bash
    echo "== dist/about/index.html =="
    sed -n '/<article/,/<\/article>/p' dist/about/index.html \
      | grep -E 'class="contact"|caleblimster@gmail\.com|linkedin\.com/in/caleblkr|target="_blank"'
    echo "== Calendly check (must return nothing) =="
    grep -i 'calendly' dist/about/index.html || echo "no calendly — correct"
    ```

    Expected: contact section markup present; mailto + LinkedIn both inside `<article>`; target="_blank" + rel attributes on the LinkedIn anchor; no Calendly reference.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; sed -n '/&lt;article/,/&lt;\/article&gt;/p' dist/about/index.html | grep -qE 'href="mailto:caleblimster@gmail\.com"' &amp;&amp; sed -n '/&lt;article/,/&lt;\/article&gt;/p' dist/about/index.html | grep -qE 'href="https://linkedin\.com/in/caleblkr"' &amp;&amp; ! grep -qi calendly dist/about/index.html &amp;&amp; echo "TASK1-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with no Astro/TypeScript errors.
    - `sed -n '/<article/,/<\/article>/p' dist/about/index.html | grep -q 'class="contact"'` returns 0 (contact section inside article).
    - `sed -n '/<article/,/<\/article>/p' dist/about/index.html | grep -q 'href="mailto:caleblimster@gmail.com"'` returns 0.
    - `sed -n '/<article/,/<\/article>/p' dist/about/index.html | grep -q 'href="https://linkedin.com/in/caleblkr"'` returns 0.
    - `grep -E '<a[^>]*href="https://linkedin\.com/in/caleblkr"[^>]*target="_blank"[^>]*rel="[^"]*noopener[^"]*noreferrer[^"]*"' dist/about/index.html` returns 0 (trio on the same anchor; Gate 20 conformance) — OR the same regex with attribute order swapped.
    - `grep -i calendly dist/about/index.html` returns 1 (no Calendly — user lock).
    - `grep -c '<section class="contact"' dist/about/index.html` returns 1.
    - `grep -c 'aria-labelledby="contact-h"' dist/about/index.html` returns 1.
    - The bio paragraph still reads byte-identical (Phase 2 content contract preserved): `grep -q "I'm Caleb Lim. I work across four lanes" dist/about/index.html` returns 0.
    - The resume-link paragraph still present: `grep -q 'href="/caleb-lim-resume.pdf"' dist/about/index.html` returns 0.
    - The back-pill still present: `grep -q 'class="b-cat-back"' dist/about/index.html` returns 0.
    - No new CSS custom property defined: `git diff src/pages/about.astro | grep -E '^\+\s*--[a-z][a-z0-9-]+\s*:' | wc -l` returns 0.
    - The 900px `@media` block was NOT modified (verify `git diff src/pages/about.astro | grep -B2 '@media (max-width: 900px)'` shows no surrounding `+` or `-` lines in that block).
  </acceptance_criteria>
  <done>Contact section landed inside `<article class="about">`; mailto + LinkedIn rows render; no Calendly; existing bio + resume + back-pill verbatim; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm Gate 19e flips GREEN + full-suite verification + commit</name>
  <files>src/pages/about.astro</files>
  <read_first>
    - The modified about.astro from Task 1 (already in context)
    - scripts/verify-build.sh — confirm Gate 19e logic (added by Plan 04-01) is still in place and what it checks
    - .planning/phases/04-navigation-secondary-surfaces/04-01-PLAN.md Task 1 Gate 19e definition — the exact assertion is "dist/about/index.html contains both href=\"mailto:caleblimster@gmail.com\" AND href=\"https://linkedin.com/in/caleblkr\" inside <article> scope"
  </read_first>
  <behavior>
    - Gate 19e flips from RED to GREEN.
    - Full verify-build.sh suite returns exit 0 (ALL GREEN for Phase 4 plus all of Phases 1–3) IF Plan 04-02 has also landed; if Plan 04-02 has NOT yet landed, Gates 21a/21b/21c/22 may still show as not-yet-defined (those don't exist in the script until Plan 04-02 Task 2 lands) — in that case `rc == 0` is still expected because Phase 4's existing gates (19a–f + 20) all pass.
    - One commit lands with a conventional message tying back to CONTACT-05.
  </behavior>
  <action>
    Run the full suite:

    ```bash
    npm run build && bash scripts/verify-build.sh 2>&1 | tee /tmp/p4p3-final.log
    rc=${PIPESTATUS[1]}
    echo "verify-build.sh exit code: $rc"
    ```

    Inspect `/tmp/p4p3-final.log`:
    - Final line should read `ALL GREEN`
    - `rc == 0`
    - No `FAIL:` lines anywhere
    - Specifically: Gate 19e now shows `OK:` (look for an OK line referencing About contact / CONTACT-05)

    If Plan 04-02 has NOT yet landed (i.e., Gates 21+22 don't exist yet in the script), the result is still `ALL GREEN` — Plan 04-03 is independent of Plan 04-02 except for shared `verify-build.sh` reads.

    Site-wide external-link audit sanity check — every `target="_blank"` in the built site should be paired with the rel attributes. There should be `target="_blank"` on every page (LinkedIn in header from Plan 04-01) plus an EXTRA occurrence on `dist/about/index.html` (LinkedIn in contact block from this plan). Verify:

    ```bash
    echo "== target=\"_blank\" occurrences per page =="
    for f in $(find dist -name '*.html' -type f); do
      n=$(grep -c 'target="_blank"' "$f" || echo 0)
      echo "$f: $n"
    done | sort
    ```

    Expected: most pages = 1; `dist/about/index.html` = 2. Also verify every `target="_blank"` has the rel pair:

    ```bash
    # Gate 20 is the formal check; this is a manual cross-verification.
    grep -rE '<a [^>]*target="_blank"[^>]*>' dist --include='*.html' \
      | grep -vE 'rel="[^"]*noopener[^"]*"|rel="[^"]*noreferrer[^"]*"' \
      || echo "  all target=_blank anchors carry rel attributes — OK"
    ```

    Expected: the second grep returns the OK echo (no anchors missing the rel pair).

    Commit:

    ```bash
    git add src/pages/about.astro
    git status  # confirm only src/pages/about.astro staged
    git commit -m "feat(04-03): About contact block — email + LinkedIn

    Appends <section class='contact'> to about.astro below the
    resume-link paragraph. Two rows: mailto:caleblimster@gmail.com
    and https://linkedin.com/in/caleblkr. Calendly intentionally
    skipped per user lock (CONTACT-05 text reads 'optional Calendly').

    Mono uppercase labels mirror the CRO vocabulary on detail
    pages. Phase 3 tokens consumed; no new custom properties.

    Flips Gate 19e (CONTACT-05) GREEN. With Plans 04-01 + 04-02
    landed, scripts/verify-build.sh exits 0 across all Phase 4 gates.

    Refs: CONTACT-05"
    ```

    Confirm commit: `git log -1 --oneline --name-only`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; bash scripts/verify-build.sh 2&gt;&amp;1 | tee /tmp/p4p3-done.log; rc=${PIPESTATUS[1]}; [[ $rc -eq 0 ]] &amp;&amp; grep -q 'ALL GREEN' /tmp/p4p3-done.log &amp;&amp; ! grep -qE '^  FAIL:' /tmp/p4p3-done.log &amp;&amp; echo "PLAN-04-03-DONE"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` shows commit message starting with `feat(04-03):`.
    - `git log -1 --name-only` shows ONLY `src/pages/about.astro` modified.
    - `bash scripts/verify-build.sh; echo $?` returns 0 (all Phase 4 gates GREEN, assuming Plan 04-02 also landed — if not, the script still returns 0 because Gates 21+22 don't yet exist).
    - The output contains the literal final line `ALL GREEN`.
    - The output contains NO `^  FAIL:` lines.
    - `grep -c 'target="_blank"' dist/about/index.html` returns 2 (header LinkedIn + About-block LinkedIn).
    - Every `target="_blank"` anchor in the built site carries both `noopener` and `noreferrer` tokens in its rel attribute (Gate 20 conformance — manual cross-verification step in the action passes).
    - `grep -c 'calendly' dist/about/index.html` returns 0 (Calendly user-lock honored).
    - The mailto delivery walk is recorded as a manual UAT step in `.planning/phases/04-navigation-secondary-surfaces/04-VALIDATION.md` — NOT automatable but noted.
  </acceptance_criteria>
  <done>Plan 04-03 commit landed; CONTACT-05 closed; Gate 19e GREEN; full verify-build.sh exits 0; Phase 4 ready for UAT (mailto deliverability walk + visual chrome walk per 04-VALIDATION.md Manual-Only Verifications).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| origin (caleblim.com/about) → external host (linkedin.com) | Outbound user-initiated navigation in a new tab from the About contact block. Same threat model as Plan 04-01's header LinkedIn link. |
| origin (caleblim.com/about) → user MUA (mail client) | Mailto handoff. Same threat model as Plan 04-01's header mailto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | LinkedIn anchor in About contact block — reverse tabnabbing via `target="_blank"` | mitigate | Emit `rel="noopener"` on the anchor. Same canonical mitigation as Plan 04-01's header anchor. Verified by Gate 20 (already in place from Plan 04-01); this is the SECOND occurrence in the codebase. |
| T-04-10 | Information disclosure | LinkedIn anchor — Referer leak | mitigate | Emit `rel="noreferrer"` on the anchor. Same pair as Plan 04-01. Gate 20 enforces. |
| T-04-11 | Information disclosure | mailto address exposed to scrapers (second occurrence on the site — header + About) | accept | Same accepted risk as Plan 04-01 T-04-03. Mailto is public infrastructure for a job-seeking portfolio; obfuscation breaks no-JS recruiters + screen readers. Gmail's spam filter is the operative defense. Doubling exposure (header + About) does not materially change the risk surface — scrapers index the page once. |
</threat_model>

<verification>
- `bash scripts/verify-build.sh` exits 0 with no FAIL: lines, ending in `ALL GREEN`.
- `dist/about/index.html` renders a `<section class="contact">` inside `<article class="about">`, containing two contact rows (email + LinkedIn) with correct hrefs.
- The LinkedIn anchor in the About contact block carries the `target="_blank" rel="noopener noreferrer"` trio.
- No Calendly reference appears anywhere in the built About page.
- Existing About artifacts (bio paragraph, resume-link, back-pill, h1) preserved byte-identical.
- No new CSS custom property was introduced (locked Phase 3 tokens consumed via `var(...)`).
</verification>

<success_criteria>
- CONTACT-05 closed: About page hosts a slightly-larger contact block with email + LinkedIn (Calendly skipped per user lock).
- Gate 19e (Plan 04-01's stub) flips RED → GREEN.
- One commit (`feat(04-03): About contact block — email + LinkedIn`) landed.
- Full Phase 4 ready for human UAT: mailto deliverability walk (send a test email from a different account to caleblimster@gmail.com and verify receipt) + the visual chrome walk per 04-VALIDATION.md Manual-Only Verifications.
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-secondary-surfaces/04-03-SUMMARY.md` covering: the contact block layout (grid columns, mono labels, serif anchors), the SECOND occurrence of the external-link-safety trio in the codebase, the Calendly-skipped decision (with reference to user lock), and confirmation that the Phase 4 verification suite is now fully GREEN.
</output>
