---
phase: 02-asset-pipeline-real-content
plan: 05
type: execute
wave: 2
depends_on: [02-06]
files_modified:
  - src/content/pieces/design-real-piece/index.md
  - src/content/pieces/design-real-piece/hero.png
  - src/content/pieces/design-real-piece/source.pdf
  - src/content/pieces/finance-real-piece/index.md
  - src/content/pieces/finance-real-piece/hero.png
  - src/content/pieces/finance-real-piece/source.pdf
  - src/content/pieces/marketing-real-piece/index.md
  - src/content/pieces/marketing-real-piece/hero.png
  - src/content/pieces/marketing-real-piece/source.pdf
autonomous: false
gap_closure: true
requirements: [PIECE-03, PIECE-04, PIECE-06, FOUND-05]
tags: [gap-closure, content-authoring, real-pieces, found-05, sc1-sc2-sc5, pdf-pipeline-exercise]

user_setup:
  - service: caleb-piece-assets
    why: "Per D-13, Plan 02-05 is collaborative — Caleb supplies real piece assets piece-by-piece. Each piece needs: a real title, a hero image (.jpg/.png/.webp), CRO blurbs in his practitioner-coded voice (drafted with Claude, edited by Caleb), and at least one piece needs a source.pdf with pdfPaginate so SC1/SC5 + Gates 7/10/11 are exercised end-to-end against real content. Without these inputs, the placeholder pieces cannot be replaced and the gap stays open."
    env_vars: []
    dashboard_config: []
  - service: nda-content-gate
    why: "Per threat model T-02-01 (Plan 01) — before each source.pdf is committed via D-01 colocation, Caleb confirms the PDF contains no NDA / employer-confidential client material. Manual judgment gate, enforced at the per-piece checkpoint."
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "All 3 PLACEHOLDER pieces (design-real-piece / finance-real-piece / marketing-real-piece) have been replaced with real Caleb-authored content: real title (no 'Phase 1 Skeleton —' prefix), real hero image (not the byte-identical 15922-byte solid-color placeholder PNG), and real CRO blurbs (Context 3-6 lines, Role 1-3 lines, Outcome 1-3 lines per D-12 / PIECE-02)"
    - "FOUND-05 strong-category floor met in spirit (D-10): design + marketing each have at least 1 non-PLACEHOLDER piece with draft: false; finance is bonus"
    - "ZERO occurrences of the literal substring PLACEHOLDER in src/content/pieces/*/index.md (closes SC2 voice gap; Gate 12d in Plan 02-07 will lock this)"
    - "ZERO occurrences of banned filler phrases (passionate / multidisciplinary / intersection of) in any piece's CRO blurbs (D-09 / D-12 voice rule; Gate 12e in Plan 02-07 will lock this)"
    - "At least ONE piece has source.pdf colocated AND pdfPaginate: [N1, N2, ...] set in frontmatter — exercises SC1 (rasterization runs against real input) and SC5 (paginated <img> sequence renders) and Gates 7 + 10 (currently no-op passes)"
    - "At least ONE piece has fullPdf: /source-pdfs/<slug>.pdf set in frontmatter (canonical path per Plan 02-06 WR-02 contract) — exercises PIECE-06 + Gate 11 + Plan 02-06's WR-02 assertion (positive case: assertion passes silently)"
    - "For every piece with source.pdf set: public/generated/pdf-thumbs/<slug>/cover.webp + .cache.json exist (Plan 01 pipeline produced real outputs against real input — first time in this codebase)"
    - "For every piece with pdfPaginate: [N, ...] (where N != 1): public/generated/pdf-thumbs/<slug>/page-N.webp exists for each N"
    - "For every piece with fullPdf set: public/source-pdfs/<slug>.pdf exists (Plan 01's copySourcePdf side-effect ran)"
    - "Each piece's frontmatter passes Zod schema validation (npx astro sync exits 0)"
    - "npm run build && npm run test:smoke exits 0 with ALL GREEN against the real-content tree (Gates 1-11 hold; Gate 12 lands in Plan 02-07)"
    - "D-13 honored: each piece is authored via the per-piece collaborative checkpoint flow — Caleb supplies the asset, Claude drafts CRO from the asset + Caleb's seed, Caleb edits and locks. No bulk pre-drafting; no fabricated content."
  artifacts:
    - path: "src/content/pieces/design-real-piece/index.md"
      provides: "Real Graphic Design piece — full CRO in practitioner-coded voice, no PLACEHOLDER, real hero asset"
      contains: "category: design"
    - path: "src/content/pieces/finance-real-piece/index.md"
      provides: "Real Finance piece — full CRO, real hero, ideally with source.pdf + pdfPaginate (Finance decks are PDF-native — natural fit per D-13)"
      contains: "category: finance"
    - path: "src/content/pieces/marketing-real-piece/index.md"
      provides: "Real Marketing piece — full CRO, real hero, optionally with source.pdf"
      contains: "category: marketing"
    - path: "public/generated/pdf-thumbs/"
      provides: "Real WebP rasterizations from Plan 01's pdf-preprocess.mjs running against real source.pdf input — first time this directory has actual content"
      contains: ""
    - path: "public/source-pdfs/"
      provides: "Real source PDF copies from Plan 01's copySourcePdf side-effect — first time this directory has actual content"
      contains: ""
  key_links:
    - from: "src/content/pieces/<slug>/source.pdf"
      to: "public/generated/pdf-thumbs/<slug>/cover.webp + page-N.webp + .cache.json"
      via: "scripts/pdf-preprocess.mjs prebuild hook (Plan 01 pipeline) consuming real input"
      pattern: "/generated/pdf-thumbs/"
    - from: "src/content/pieces/<slug>/index.md frontmatter pdfPaginate"
      to: "rendered <img src=\"/generated/pdf-thumbs/<slug>/page-N.webp\"> tags in dist/<cat>/<slug>/index.html"
      via: "Plan 03's detail template paginated section"
      pattern: "/generated/pdf-thumbs/"
    - from: "src/content/pieces/<slug>/index.md frontmatter fullPdf"
      to: "rendered <a href=\"/source-pdfs/<slug>.pdf\" download>Open full PDF</a> in dist/<cat>/<slug>/index.html + public/source-pdfs/<slug>.pdf file"
      via: "Plan 02-06 WR-02 contract (fullPdf MUST equal /source-pdfs/<slug>.pdf) + Plan 03 template fullPdf block + Plan 01 copySourcePdf side-effect"
      pattern: "Open full PDF"
---

<objective>
Land Caleb's real content. This is the integration plan deferred from Plan 02-04 — the pipeline (Plan 01), about/resume (Plan 02), template (Plan 03), and now-fixed pipeline correctness (Plan 02-06) are all wired and exercised against placeholders; this plan replaces the 3 PLACEHOLDER pieces with real content per D-13's per-piece collaborative flow.

Per D-10 ("in spirit, not numbers"): the floor is design + marketing as the strong categories; finance is bonus where the asset is ready. Per D-13: the flow is collaborative — Caleb supplies the asset, Claude drafts CRO, Caleb edits and locks. NO fabricated content; NO bulk pre-drafting; NO assumptions about what the piece is about until Caleb tells us.

**At least one piece must exercise the full PDF pipeline** (source.pdf + pdfPaginate + fullPdf) so SC1, SC5, Gates 7/10/11 stop being silent no-op passes. Per D-13 the natural fit is Finance (decks are PDF-native) or Design if it's a multi-page presentation. Caleb picks which.

**Plan 02-06 prerequisite:** This plan depends on 02-06 because that plan added the fullPdf canonical-path contract assertion (WR-02). Any piece that sets `fullPdf:` in this plan MUST set it to exactly `/source-pdfs/<slug>.pdf` or the build will fail loudly. The contract is documented in 02-06's user-facing notes; this plan will set fullPdf correctly per the contract.

`autonomous: false` because Task 1 has multiple `checkpoint:human-action` sub-steps (one per piece — Caleb supplies asset + collaborates on CRO blurbs). The orchestrator's `--gaps-only` filter will pause at each checkpoint and surface the resume signal.

Purpose: Close SC1 (real-data exercise), SC2 (real CRO content), SC5 (paginated render against real input), and FOUND-05 (asymmetric distribution against real content). Closes 4 of the 6 gaps from 02-VERIFICATION.md (the other 2 — Gate 12 implementation + commit generated outputs — land in Plan 02-07 which depends on this plan).

Output: 3 real pieces in the source tree replacing the Phase 1 PLACEHOLDERs; real generated outputs in public/generated/ and public/source-pdfs/ (committed in Plan 02-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-asset-pipeline-real-content/02-CONTEXT.md
@.planning/phases/02-asset-pipeline-real-content/02-RESEARCH.md
@.planning/phases/02-asset-pipeline-real-content/02-PATTERNS.md
@.planning/phases/02-asset-pipeline-real-content/02-VERIFICATION.md

# Existing PLACEHOLDER pieces — TO BE REPLACED:
@src/content/pieces/design-real-piece/index.md
@src/content/pieces/finance-real-piece/index.md
@src/content/pieces/marketing-real-piece/index.md

# Plan dependencies:
# - Plan 01: scripts/pdf-preprocess.mjs (prebuild hook) + Gate 7
# - Plan 02: about page + resume + Gates 8-9
# - Plan 03: src/pages/[category]/[slug].astro (paginated template + fullPdf block) + Gates 10-11
# - Plan 02-06: pipeline-correctness fixes — draft-skip + orphan-prune + WR-02 fullPdf canonical-path contract
# - Plan 02-04 (already complete): Task 1 deleted phase-1-skeleton; Tasks 2-4 deferred to this plan and 02-07

<interfaces>
<!-- Real-piece frontmatter contract (per Phase 1 D-04 colocation + Plan 01 D-07 schema migration + Plan 02-06 WR-02 fullPdf canonical-path): -->
```yaml
---
title: "Real piece title — must NOT contain 'PLACEHOLDER' or 'Phase 1 Skeleton'"
category: design  # OR finance / marketing — bare value, NOT quoted (Phase 1 convention)
order: 1  # gallery sort key — lower = earlier
draft: false  # MUST be false to render
hero: "./hero.png"  # OR ./hero.jpg / ./hero.webp — colocated; resolved by Astro image() helper
role: |
  Lead designer.
  Owned brand identity application across deck + 3 case-study spreads.
  Output: 12-slide presentation, locked logo system.
outcome: |
  Shipped to client; deck used in Q3 board pitch.
  Replaced two prior templates as the team's brand standard.
context: |
  G15's case competition required a brand identity proposal for a fictional client.
  Brief: 4 weeks, full system from logo through 12-slide presentation.
  Constraint: must read as both pitch artifact and durable brand standard.

# Optional — only for pieces that have source.pdf colocated and want paginated rendering:
pdfPaginate: [1, 5, 12, 23]  # 1-indexed; preserves Caleb's chosen order (D-09: NO re-sort)

# Optional — only for pieces shareable as full-PDF download.
# WR-02 (Plan 02-06): MUST equal /source-pdfs/<slug>.pdf or build fails.
fullPdf: "/source-pdfs/design-real-piece.pdf"
---
```

<!-- D-12 voice rules: -->
<!-- - Practitioner-coded verbs: modeled, exported, shipped, validated, designed, wrote, calculated, built, owned, led -->
<!-- - NOT: collaborated, explored, engaged, learned about, was passionate (these are dabbler-coded) -->
<!-- - Cite specific tools, named outputs, role -->
<!-- - Banned phrases (case-insensitive): passionate / multidisciplinary / intersection of -->

<!-- Colocated assets per piece (D-01, D-04 from Phase 1): -->
<!-- src/content/pieces/<slug>/ -->
<!-- ├── index.md       (frontmatter) -->
<!-- ├── hero.png|jpg|webp  (manually-exported hero — DIFFERENT from auto-generated cover.webp; Pitfall 8) -->
<!-- └── source.pdf     (optional; only when piece has a deck/PDF; rasterizer consumes this) -->

<!-- D-05 filename contract for generated outputs (Plan 01 enforces; this plan triggers it for the first time against real input): -->
<!-- public/generated/pdf-thumbs/<slug>/ -->
<!-- ├── cover.webp           (page 1) -->
<!-- ├── page-{N}.webp        (one per N in pdfPaginate, where N != 1) -->
<!-- └── .cache.json          (sidecar with {inputHash, generatedAt, pages:[{n,w,h,bytes,file}]}) -->
<!-- public/source-pdfs/ -->
<!-- └── <slug>.pdf           (only when fullPdf set; Plan 02-06 WR-02 enforces canonical path) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Replace 3 PLACEHOLDER pieces with real Caleb-authored content (per-piece collaborative flow per D-13)</name>
  <files>src/content/pieces/design-real-piece/index.md, src/content/pieces/design-real-piece/hero.png, src/content/pieces/design-real-piece/source.pdf, src/content/pieces/finance-real-piece/index.md, src/content/pieces/finance-real-piece/hero.png, src/content/pieces/finance-real-piece/source.pdf, src/content/pieces/marketing-real-piece/index.md, src/content/pieces/marketing-real-piece/hero.png, src/content/pieces/marketing-real-piece/source.pdf</files>
  <read_first>
    - .planning/phases/02-asset-pipeline-real-content/02-CONTEXT.md §"Content Scope & Piece Set" (D-10 / D-12 / D-13 — voice rules + per-piece authoring flow + "in spirit not numbers")
    - .planning/phases/02-asset-pipeline-real-content/02-CONTEXT.md §"Specific Ideas" (practitioner-coded voice expanded examples)
    - .planning/REQUIREMENTS.md §"Piece Detail" (PIECE-02 line counts; PIECE-03 PDF rasterization; PIECE-04 paginated; PIECE-06 Open full PDF)
    - .planning/PROJECT.md §"Constraints" (PITFALLS — banned phrases for blurb voice)
    - .planning/phases/02-asset-pipeline-real-content/02-PATTERNS.md §"src/content/pieces/<slug>/index.md" (frontmatter shape; YAML quoting conventions)
    - .planning/phases/02-asset-pipeline-real-content/02-VERIFICATION.md §"SC2 / FOUND-05" gap details (lines 14-39 — what specifically failed and what the close criteria look like)
    - src/content/pieces/design-real-piece/index.md (existing PLACEHOLDER — to be replaced)
    - src/content/pieces/finance-real-piece/index.md (existing PLACEHOLDER — to be replaced)
    - src/content/pieces/marketing-real-piece/index.md (existing PLACEHOLDER — to be replaced)
    - .planning/phases/02-asset-pipeline-real-content/02-06-SUMMARY.md (Plan 02-06 closeout — confirms fullPdf canonical-path contract is in place; this plan must set fullPdf to exactly /source-pdfs/<slug>.pdf or build fails)
  </read_first>
  <what-built>
    Per D-13 collaborative authoring: each of the 3 PLACEHOLDER pieces (design-real-piece, finance-real-piece, marketing-real-piece) is replaced with real Caleb-supplied content via the per-piece flow described in Plan 02-04 Task 2 (reproduced and refined here). Caleb supplies the asset (hero image and/or source PDF); Claude drafts CRO blurbs from the asset + Caleb's contextual seed; Caleb edits and locks per piece.

    **Per D-10's "in spirit, not numbers" coverage:** Target ≥1 piece each in design + marketing (the strong categories per FOUND-05); finance is a bonus thicker category if Caleb has the asset ready. The existing 3 placeholder slugs ARE the target — replacing all three lands the SC2 gap close.

    **At least ONE piece must exercise the full PDF pipeline** (`source.pdf` colocated + `pdfPaginate: [...]` set) so SC1, SC5, Gate 7, Gate 10 stop being silent no-op passes. Per D-13 the natural fit is the Finance piece (decks are PDF-native) or Design if it's a multi-page presentation. Caleb picks which.

    **At least ONE piece should set `fullPdf`** so PIECE-06 + Gate 11 + Plan 02-06's WR-02 assertion are exercised. The same piece as the PDF deck above is fine. Per Plan 02-06 WR-02 contract: the fullPdf value MUST equal `/source-pdfs/<slug>.pdf` exactly or the build fails — this is the positive-case test of that assertion.

    **Scope guard:** If Caleb supplies more than the 3 baseline pieces (e.g. 2 design + 1 finance + 2 marketing — the 5-piece minimum from D-10), this task absorbs them via the same flow. New slugs go in NEW directories: `src/content/pieces/<new-slug>/index.md` + `hero.<ext>` + optional `source.pdf`. If Caleb supplies 6+ pieces, split this task into 1a/1b at a natural per-piece break (after pieces 3 lands → commit + verify partial → resume with 1b for the rest) to keep context cost bounded.
  </what-built>
  <how-to-verify>
    Per-piece collaborative flow (repeat for each of design + finance + marketing — at minimum design + marketing per FOUND-05's strong categories):

    **Step 1 — Caleb supplies the asset:**
    - Caleb provides the absolute path of the hero image (`.jpg` / `.png` / `.webp`) on his Mac
    - If applicable: Caleb provides the absolute path of a source PDF (slide deck, multi-page document)
    - Caleb confirms the PDF (if any) contains no NDA / employer-confidential client material per T-02-01 — manual judgment gate
    - Caleb provides minimal contextual seed for CRO drafting (e.g. "this was for the G15 case competition; my role was lead designer; outcome: deck used in their pitch round")

    **Step 2 — Claude drafts CRO blurbs from the asset + Caleb's seed:**
    - Context: 3-6 lines (per D-12 / PIECE-02). Sets the situation: who/what/when/why.
    - Role: 1-3 lines. What Caleb specifically did. Practitioner-coded verbs.
    - Outcome: 1-3 lines. What shipped, what changed, what was used. Concrete and named.
    - Self-check before presenting to Caleb:
      - Word count fits the Context 3-6 / Role 1-3 / Outcome 1-3 line spec
      - `grep -iE 'passionate|multidisciplinary|intersection of'` returns no matches in the draft
      - The literal substring `PLACEHOLDER` does NOT appear anywhere
      - Names at least one specific tool / role / output / number
      - First-person where natural (`I led...` / `Designed...` — depending on flow)

    **Step 3 — Claude proposes pdfPaginate (if PDF exists):**
    - Inspects the source PDF's page count via the existing `scripts/pdf-poc.mjs` pattern (or by running pdfjs's `getDocument().promise.then(d => d.numPages)` ad-hoc)
    - Suggests 3-6 representative slide numbers (cover, key insight, money chart, conclusion — per D-08; PIECE-04)
    - Caleb either accepts or hand-picks his own array

    **Step 4 — Claude proposes `fullPdf` decision:**
    - Asks Caleb: "Is this PDF shareable as a full download (no NDA / client-confidential surfaces)?"
    - If yes: set `fullPdf: "/source-pdfs/<slug>.pdf"` — must match the slug exactly per Plan 02-06 WR-02 contract
    - If no: omit the `fullPdf` field entirely (the prebuild script will not copy the source PDF; the Open full PDF link will not render)

    **Step 5 — Caleb edits and locks:**
    - Approves the CRO triple, the pdfPaginate array (if any), and the fullPdf decision
    - Provides any voice-tweak feedback for the next piece (so subsequent pieces match Caleb's voice consistently)

    **Step 6 — Claude commits the piece's frontmatter + assets + runs the pipeline:**
    - Replaces `src/content/pieces/<slug>/index.md` with the locked frontmatter
    - Replaces `src/content/pieces/<slug>/hero.<ext>` (deletes the Phase 1 PLACEHOLDER PNG; copies in the real hero from Caleb's path; if extension changes from .png to .jpg/.webp, update the `hero:` frontmatter field accordingly)
    - If applicable: copies the source PDF to `src/content/pieces/<slug>/source.pdf`
    - Runs `npm run build` to fire the prebuild → pdf-preprocess.mjs (if source.pdf exists) → astro build
    - Runs `npm run test:smoke` to verify Gates 7 + 10 + 11 are green for this piece (gates skip the non-PDF pieces silently)

    **Step 7 — Per-piece UAT:**
    - Caleb opens `dist/<category>/<slug>/index.html` (via `npx astro preview` or by inspecting the file directly), confirms:
      - Hero image renders correctly (sized, not broken)
      - Context / Role / Outcome blurbs read in his voice (practitioner-coded)
      - If pdfPaginate set: paginated `<img>` sequence renders below Outcome with correct page selections in correct order
      - If fullPdf set: "Open full PDF" link appears and downloads the source PDF when clicked
    - Caleb signs off per piece with: "Piece <slug> approved" (with optional voice-tweak feedback for the next piece)

    Repeat the cycle for each of the 3 real pieces (design + finance + marketing — at minimum design + marketing). After all are landed, run a single end-to-end check:

    ```bash
    npm run build && npm run test:smoke
    ```

    Expected: exit 0, ALL GREEN. Note: Gate 12 (piece count + distribution + no PLACEHOLDER + no banned phrases + no phase-1-skeleton) lands in Plan 02-07 — this plan only needs Gates 1-11 to pass. The acceptance criteria below explicitly check Gates 1-11 plus the source-tree shape that Gate 12 will subsequently lock.
  </how-to-verify>
  <action>
    Drive the per-piece flow above. For each of the 3 pieces, execute steps 1-7 in order. Pause for Caleb at the asset-supply step (1), the CRO-edit step (5), and the per-piece UAT step (7).

    **Frontmatter authoring conventions** (from 02-PATTERNS.md):
    - YAML quoting: double-quoted strings for free-text fields (`title`, `hero`, `fullPdf`); multi-line CRO via `|` block scalars
    - `category: design` (bare, no quotes — enum value)
    - `draft: false` (bare boolean)
    - `order: 1` (bare integer)
    - `hero: "./hero.png"` (double-quoted relative path; update extension if Caleb supplies .jpg/.webp)
    - `pdfPaginate: [1, 5, 12]` (YAML inline array, integers)
    - `fullPdf: "/source-pdfs/<slug>.pdf"` (double-quoted; MUST equal canonical path per Plan 02-06 WR-02)

    **Prebuild integration** (D-02): when a piece's `source.pdf` is added, the next `npm run build` automatically fires `scripts/pdf-preprocess.mjs` which discovers the new PDF, hashes it, rasterizes page 1 + each page in pdfPaginate, writes outputs to `public/generated/pdf-thumbs/<slug>/` + (if fullPdf set) copies the PDF to `public/source-pdfs/<slug>.pdf`, and writes `.cache.json`. Re-runs are no-ops via the hash cache.

    **WR-02 positive case (per Plan 02-06):** the first time a piece sets `fullPdf:` AND has source.pdf, the prebuild's contract assertion fires and verifies fullPdf equals /source-pdfs/<slug>.pdf. If the assertion passes silently and the build continues, the contract is exercised positively. If it fails (build aborts with `WR-02 contract violation`), Caleb's frontmatter doesn't match the canonical path — fix the frontmatter to use the canonical value and re-run.

    **CR-01 positive case (per Plan 02-06):** if at any point during this task Caleb wants to commit a piece partially-done (in-flight CRO blurbs, asset not yet shareable), set `draft: true` in frontmatter. The prebuild will SKIP that piece (logged as `SKIP <slug> (draft)`); no public/ outputs will leak. When Caleb is ready to publish, flip `draft: false` and re-run.

    **DO NOT:**
    - Use the Phase 1 PLACEHOLDER hero generation pattern (15.5KB solid-color PNG) for any real piece. Real heroes come from Caleb. The current heroes (`hero.png` for all 3 pieces) ARE the placeholder PNG and MUST be replaced with Caleb-supplied images.
    - Skip the per-piece UAT — voice judgment is Caleb's call (RESEARCH.md "Manual-Only Verifications").
    - Commit the source PDF Caleb provides directly to git BEFORE confirming no NDA/confidential content (T-02-01 manual gate).
    - Touch `src/content.config.ts` (Plan 01 owns the schema; this plan just authors against it).
    - Touch `src/pages/[category]/[slug].astro` (Plan 03 owns the template).
    - Touch `scripts/verify-build.sh` (this plan stops at Gates 1-11; Plan 02-07 owns Gate 12).
    - Touch `scripts/pdf-preprocess.mjs` (Plan 02-06 owns the pipeline fixes; this plan just exercises them).
    - Fabricate piece content. Per D-13: NO bulk pre-drafting, NO assumptions about what the piece is about until Caleb tells us. If Caleb is not present at the checkpoint, the orchestrator's `--gaps-only` filter will pause execution; do NOT fill in placeholder content "for now".
    - Re-add a personal-real-piece. Per D-11 + 02-04 SUMMARY: Personal stays empty unless Caleb explicitly supplies a piece. Plan 02-04 Task 1 already deleted phase-1-skeleton; Personal gallery currently renders the empty-state branch and that is the intended terminal state for Phase 2.

    **If Caleb supplies more than 3 pieces** (e.g. 2 strong design + 1 marketing + 1 finance + 1 personal — the 5-piece D-10 stretch): land each via the same flow. New pieces beyond the 3 placeholder slugs go in NEW directories: `src/content/pieces/<new-slug>/index.md` + `hero.<ext>` + optional `source.pdf`. Update files_modified mentally; the actual frontmatter `files_modified` field stays as written (gap closure plans operate on observable state, not exhaustive file enumeration).

    **If Caleb's Personal Projects piece materializes** (FUTURE-05 — stretch): land it via the same flow at `src/content/pieces/<personal-slug>/index.md` with `category: personal`. CONTEXT D-11 confirms Personal stays empty by default and Phase 4 / SPLASH-04 handles the splash-card drop.

    **CF Pages Linux parity** (Phase 1 deferred A1 — per CONTEXT "Claude's Discretion"): this is the natural place to surface that risk. After Caleb approves the first PDF-deck piece, the executor MAY run a Docker-simulated build:
    ```bash
    docker run --rm -v "$PWD:/app" -w /app node:22-bookworm bash -c "npm ci && npm run build"
    ```
    Bookworm is glibc 2.36 (close enough to CF Pages V3's 2.35). If `@napi-rs/canvas-linux-x64-gnu` fails to load there, it's a Phase 2 blocker — surface to Caleb, halt, and triage (Pitfall 6 fallback: install `@napi-rs/canvas` directly at the 0.1.x line as a hard pin). Document the chosen path in the SUMMARY. If skipped, document the A1 risk acceptance.

    **Final checkpoint signal:** after all 3 pieces land and per-piece UAT passes, present a final summary to Caleb listing each piece's slug + category + (if PDF) pdfPaginate values + (if fullPdf) the fullPdf value. Caleb's resume signal "All pieces approved" or "Add piece <new-slug> also" closes the task.
  </action>
  <acceptance_criteria>
    - At least 3 pieces in `src/content/pieces/` have `draft: false` AND zero occurrences of the literal substring `PLACEHOLDER` in their `index.md`: `grep -L 'PLACEHOLDER' src/content/pieces/*/index.md | wc -l` returns at least 3
    - At least 1 piece per category in `[design, marketing]` has `draft: false` and is non-PLACEHOLDER (FOUND-05 strong-category floor per D-10): `grep -l '^category: design' src/content/pieces/*/index.md | xargs grep -L 'PLACEHOLDER' | wc -l` >= 1 AND same for marketing
    - The existing 3 placeholder slugs (design-real-piece, finance-real-piece, marketing-real-piece) all have `index.md` files without the literal `Phase 1 Skeleton` or `PLACEHOLDER` substrings: `grep -E 'Phase 1 Skeleton|PLACEHOLDER' src/content/pieces/*/index.md | wc -l` returns 0
    - The existing 3 hero.png files are no longer the byte-identical 15922-byte placeholder. Verify via: `find src/content/pieces -name 'hero.*' -size 15922c | wc -l` returns 0 (none of the heroes are the exact placeholder size)
    - Each piece's `index.md` passes Zod schema validation: `npx astro sync` exits 0
    - At least 1 piece has `source.pdf` colocated AND has `pdfPaginate: [...]` set in frontmatter (exercises SC1, SC5, Gates 7 + 10): `find src/content/pieces -name 'source.pdf' | wc -l` >= 1 AND at least one of those pieces' `index.md` matches `^pdfPaginate:` via grep
    - At least 1 piece has `fullPdf:` set in frontmatter (exercises Gate 11 + Plan 02-06 WR-02 positive case): `grep -l '^fullPdf:' src/content/pieces/*/index.md | wc -l` >= 1
    - For every piece with `source.pdf`: `public/generated/pdf-thumbs/<slug>/cover.webp` exists AND `.cache.json` exists (Plan 01 pipeline produced real outputs)
    - For every piece with `pdfPaginate: [N, ...]` (where N != 1 in the array): `public/generated/pdf-thumbs/<slug>/page-N.webp` exists for each such N
    - For every piece with `fullPdf:`: `public/source-pdfs/<slug>.pdf` exists (Plan 01 copySourcePdf side-effect ran), AND the fullPdf frontmatter value equals `/source-pdfs/<slug>.pdf` (Plan 02-06 WR-02 positive case — assertion passed silently)
    - Banned-phrase grep returns NO matches: `grep -v '^#' src/content/pieces/*/index.md | grep -iE 'passionate|multidisciplinary|intersection of'` returns nothing (filters YAML comments per planner critical-rule on header-prose self-invalidation)
    - `npm run build` exits 0 (prebuild + astro build both succeed against real input — first time)
    - `npm run test:smoke` exits 0 with `ALL GREEN` across Gates 1-11 (Gate 12 is Plan 02-07's job)
    - For pieces with source.pdf: build log contains an `OK <slug>/cover.webp <W>x<H> (<KB>KB)` line for cover and one per pdfPaginate page (this is the prebuild's success log proving real rasterization happened)
    - Caleb has approved each piece individually via per-piece UAT (per Resume signal: "Piece <slug> approved")
    - Caleb has confirmed no NDA / employer-confidential PDF was committed (T-02-01 manual gate, recorded in SUMMARY)
    - The three Phase 1 PLACEHOLDER pieces (design-real-piece, finance-real-piece, marketing-real-piece) are STILL present as directories — slugs preserved per ROADMAP and gallery-sort `order` consistency. Only their CONTENTS (frontmatter, hero, optionally source.pdf) are replaced.
    - `git status` shows modifications (not deletions) for the 3 piece directories — confirming this task replaced content rather than removed pieces
  </acceptance_criteria>
  <resume-signal>
    Per piece: Caleb says "Piece <slug> approved" with optional voice-tweak feedback for the next piece. After all pieces lock: "All pieces approved" OR "Add piece <new-slug> also" (if Caleb has more ready). If Caleb wants to defer a piece to a later session, "Defer <slug>" — that piece can ship as `draft: true` in this plan and be revisited; the other pieces still land.
  </resume-signal>
  <done>
    All 3 PLACEHOLDER pieces (design-real-piece + finance-real-piece + marketing-real-piece) replaced with real Caleb-authored content via the per-piece collaborative flow per D-13. At least design + marketing land with non-PLACEHOLDER frontmatter and real hero assets. At least 1 piece exercises source.pdf + pdfPaginate (SC1, SC5, Gates 7 + 10) and at least 1 piece exercises fullPdf with the canonical /source-pdfs/<slug>.pdf path (Plan 02-06 WR-02 positive case). Generated outputs land in public/generated/ and public/source-pdfs/ but are NOT committed yet — that's Plan 02-07's job. Caleb has UAT-signed-off each piece individually. Phase 2 SC1, SC2, SC5, FOUND-05 gaps are now factually closed; Plan 02-07 will lock them via Gate 12 + commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caleb's source PDFs → committed git history (D-01) | Source PDFs become permanent in git. NDA / employer-confidential metadata cannot be retroactively removed without history rewrite. Manual per-piece gate at checkpoint Step 1. |
| Caleb's voice in CRO blurbs → public web | Practitioner-coded claims ("modeled X, shipped Y") become public statements. Caleb owns truthfulness; planner enforces only the voice rules (D-09 / D-12) and no-PLACEHOLDER. |
| Generated outputs (public/generated, public/source-pdfs) → web-served | Hash sidecars + WebP thumbs are public and durable. Plan 02-06 closed CR-01 (draft leak) and WR-01 (orphan leak); this plan exercises both fixes by exclusively committing draft: false pieces with canonical fullPdf paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 (cross-ref to Plan 01) | Information Disclosure | Source PDFs containing NDA / client-confidential material committed via D-01 colocation | mitigate | Per-piece manual gate in Task 1 Step 1: Caleb confirms each PDF is shareable BEFORE the executor copies it to src/content/pieces/<slug>/source.pdf. HUMAN judgment gate; no code-level enforcement possible without metadata-aware classifier. Documented in SUMMARY. |
| T-02-35 | Information Disclosure | Hero images Caleb supplies may contain client logos / recognizable artifacts that haven't been reviewed for shareability | mitigate | Same gate as T-02-01 — Caleb's per-piece "this is shareable" confirmation covers all colocated assets, not just PDFs. Plan 02-06's CR-01 draft-skip provides a soft-fallback: Caleb can set draft: true on an in-flight piece if shareability is in flux, and the asset will not ship to public/. |
| T-02-36 | Spoofing | A piece's CRO blurb claims an outcome Caleb can't back up (overstated role, fabricated outcome) | accept | Caleb is sole author and gatekeeper of his own claims (T-02-24 carries forward from Plan 02-04). Voice judgment is taste-driven. The per-piece UAT in Step 7 is the control. |
| T-02-37 | Tampering | Plan 02-06's WR-02 fullPdf assertion fires on a typo: Caleb sets fullPdf: "/source-pdfs/design-real-pieces.pdf" (with extra 's'); build fails with clear error naming the slug + wrong value + expected value | mitigate | This is intentional — the assertion is a positive defense against silent 404. Caleb fixes the frontmatter to match the canonical path; build succeeds. Documented in Plan 02-06 user-facing notes. |
| T-02-38 | DoS | Repository size grows by N source PDFs colocated under src/content/pieces/<slug>/source.pdf — for a 3-piece set with one PDF, ~5-50MB. Within git's groan threshold per Plan 04 T-02-27 | accept | Per Pitfall 7 (RESEARCH.md). Phase 6 may surface as maintenance friction; git-LFS migration out of scope. |

**Severity assessment:** No HIGH severity threats introduced by this plan. The most consequential (T-02-01 NDA / confidential PDF) is mitigated by the per-piece manual gate, hardened by Plan 02-06's CR-01 draft-skip safety valve. Plan 02-06's WR-02 assertion eliminates the silent 404 vector before this plan exercises it for the first time.

**Cross-references:**
- Plan 01: PDF supply-chain + canvas-factory + cache-tampering (T-02-01 through T-02-09)
- Plan 02: Resume EXIF + bio voice (T-02-10 through T-02-16)
- Plan 03: Template injection + cache-tampering at render (T-02-17 through T-02-23)
- Plan 04: Content/voice + repo size (T-02-24 through T-02-30)
- Plan 02-06: Pipeline-correctness (T-02-31 through T-02-34)
- This plan (T-02-35 through T-02-38) — real-content authoring

**Phase 1 deferred verification (CF Pages Linux parity, A1):** This plan surfaces it as an optional Docker check before committing the first real PDF (per CONTEXT "Claude's Discretion"). Whatever path the executor takes (Docker run, push to CF preview, or skip with documented A1 risk acceptance), the choice is recorded in 02-05-SUMMARY.md.
</threat_model>

<verification>
After Task 1 completes:

1. **Source tree carries real content:**
   - `grep -L 'PLACEHOLDER' src/content/pieces/*/index.md | wc -l` >= 3 (at least 3 pieces are PLACEHOLDER-free)
   - `grep -E 'Phase 1 Skeleton|PLACEHOLDER' src/content/pieces/*/index.md | wc -l` returns 0
   - `find src/content/pieces -name 'hero.*' -size 15922c | wc -l` returns 0 (no hero is the byte-identical placeholder)

2. **PDF pipeline exercised against real input:**
   - At least one piece has `src/content/pieces/<slug>/source.pdf`
   - At least one of those pieces has `pdfPaginate: [...]` in frontmatter
   - `public/generated/pdf-thumbs/<slug>/cover.webp` exists for each PDF piece
   - `public/generated/pdf-thumbs/<slug>/page-N.webp` exists for each N in each piece's pdfPaginate (where N != 1)
   - `public/generated/pdf-thumbs/<slug>/.cache.json` exists with valid JSON

3. **fullPdf canonical path exercised (Plan 02-06 WR-02 positive case):**
   - At least one piece has `fullPdf: "/source-pdfs/<slug>.pdf"` in frontmatter
   - `public/source-pdfs/<slug>.pdf` exists for each such piece
   - The build did NOT abort with WR-02 contract violation (assertion passed silently for canonical-path values)

4. **FOUND-05 strong-category floor met:**
   - design has at least 1 non-PLACEHOLDER piece with `draft: false`
   - marketing has at least 1 non-PLACEHOLDER piece with `draft: false`
   - finance is bonus (not strictly required by D-10's "in spirit" reading)

5. **Voice rules honored:**
   - `grep -v '^#' src/content/pieces/*/index.md | grep -iE 'passionate|multidisciplinary|intersection of'` returns nothing

6. **Smoke gates 1-11 pass:**
   - `npm run build && npm run test:smoke` exits 0 with `ALL GREEN`
   - Build log shows real rasterization for PDF pieces (`OK <slug>/cover.webp` lines)
   - Smoke log shows Gate 7 OK, Gate 10 OK (matching `<img>` tags), Gate 11 OK (fullPdf link with download attribute)

7. **Per-piece UAT signed off:**
   - Each piece has Caleb's "Piece <slug> approved" sign-off recorded in 02-05-SUMMARY.md
   - T-02-01 NDA gate confirmed per piece (recorded in SUMMARY)

8. **Plan 02-07 unblocked:**
   - Source tree carries real content; Plan 02-07's Gate 12 (piece count + distribution + no-PLACEHOLDER + no-banned-phrases) can now be implemented and run against real data
   - Generated outputs in public/generated/ and public/source-pdfs/ exist; Plan 02-07's commit step has files to commit
</verification>

<success_criteria>
- [ ] At least 3 pieces with `draft: false` and zero PLACEHOLDER substring in `src/content/pieces/*/index.md`
- [ ] At least 1 piece each in `[design, marketing]` with non-PLACEHOLDER content (FOUND-05 strong-category floor per D-10)
- [ ] All 3 hero.png files replaced with real Caleb-supplied hero images (no longer 15922-byte placeholder)
- [ ] At least 1 piece has `source.pdf` colocated + `pdfPaginate: [...]` set (SC1 + SC5 + Gates 7 + 10 exercised)
- [ ] At least 1 piece has `fullPdf: "/source-pdfs/<slug>.pdf"` set with canonical path (Gate 11 + Plan 02-06 WR-02 positive case)
- [ ] All CRO blurbs follow D-12 voice rules: practitioner-coded, named tools/outputs, no banned phrases
- [ ] Generated outputs (`public/generated/pdf-thumbs/<slug>/` + `public/source-pdfs/<slug>.pdf`) exist for each PDF piece (committed in Plan 02-07)
- [ ] `npx astro sync` exits 0 (Zod schema validation passes for every piece)
- [ ] `npm run build && npm run test:smoke` exits 0 with `ALL GREEN` across Gates 1-11
- [ ] Caleb has UAT-signed-off each piece individually (per-piece "Piece <slug> approved")
- [ ] T-02-01 NDA gate confirmed for each PDF (recorded in SUMMARY)
- [ ] Optional: CF Pages Linux parity verified via Docker simulation OR documented A1 risk acceptance in SUMMARY
- [ ] Plan 02-06 WR-02 contract exercised positively (assertion passed silently for canonical fullPdf values)
- [ ] Plan 02-06 CR-01 contract NOT triggered (no piece committed with draft: true and source.pdf — but the safety valve is documented for future sessions)
</success_criteria>

<output>
After Task 1 completes, create `.planning/phases/02-asset-pipeline-real-content/02-05-SUMMARY.md` documenting:

- **Per-piece breakdown:** for each piece landed, list slug + category + title + whether PDF deck (with pdfPaginate values) + whether fullPdf set + hero asset filename + word counts for Context/Role/Outcome
- **Total piece count + distribution table** (design / finance / personal / marketing) with non-PLACEHOLDER count per category
- **PDF pipeline exercise evidence:** for each piece with source.pdf, list the generated output files (cover.webp + page-N.webp + .cache.json) with their byte sizes
- **Plan 02-06 contract exercises:** confirm WR-02 positive case (canonical fullPdf path passed assertion silently); note whether CR-01 was exercised (any draft: true pieces during the session) — typically no, but if yes document the SKIP log lines
- **CF Pages Linux parity verification path:** Docker simulated, CF Pages preview branch, or local-only with documented A1 risk acceptance
- **Caleb workflow notes:** the per-piece pattern for adding new pieces in Phase 6's GitHub.dev flow:
  1. Create directory: `src/content/pieces/<new-slug>/`
  2. Drop hero asset (.png/.jpg/.webp) and optional source.pdf into the directory
  3. Write index.md frontmatter (use this plan's pieces as templates)
  4. Commit; CF Pages auto-runs prebuild → emits thumbs → builds → deploys
- **Open follow-ups for Plan 02-07:**
  - Implement Gate 12 (sub-gates 12a-12e) to lock the now-clean source tree
  - Commit `public/generated/**` and `public/source-pdfs/**` per D-03
  - Add a draft-piece smoke check (CR-01 negative case: a fixture piece with draft: true + source.pdf must NOT produce public/ outputs)
  - Final integrated UAT
- **Open follow-ups for /gsd-verify-work:** any per-piece UAT issues Caleb flagged that didn't gate the wave but want tracking
- **D-13 honored:** confirm no piece content was fabricated; every CRO triple is Caleb-edited and Caleb-locked
</output>
