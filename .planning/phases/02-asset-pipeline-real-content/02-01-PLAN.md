---
phase: 02-asset-pipeline-real-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/content.config.ts
  - scripts/pdf-preprocess.mjs
  - scripts/verify-build.sh
autonomous: true
requirements: [PIECE-03]
tags: [pdf-rasterization, prebuild-hook, schema-migration, hash-cache, sharp-webp, build-pipeline]

user_setup: []

must_haves:
  truths:
    - "Running `npm run build` automatically triggers `scripts/pdf-preprocess.mjs` before astro build (npm prebuild lifecycle)"
    - "When `npm run build` runs against any piece directory containing a `source.pdf`, a `public/generated/pdf-thumbs/[slug]/cover.webp` file is created"
    - "When the schema field `pdfPaginate: [N1, N2, ...]` is set, `public/generated/pdf-thumbs/[slug]/page-{N}.webp` is created for each N"
    - "A `.cache.json` sidecar lives next to the generated thumbs and stores `{inputHash, generatedAt, pages: [{n,w,h,bytes,file}]}`"
    - "Re-running `npm run build` with no PDF changes is a no-op (SKIP messages, no re-rasterization) — hash cache hit"
    - "The Zod schema accepts `pdfPaginate: [1,5,12]` (array form) and rejects `pdfPaginate: true` (Phase 1 boolean form)"
    - "`scripts/verify-build.sh` includes Gate 7 that asserts every piece with `source.pdf` has both `cover.webp` and `.cache.json`"
    - "D-01: Source PDFs commit to git colocated at `src/content/pieces/[slug]/source.pdf` (extends Phase 1 D-04 colocation pattern); pre-commit author check confirms no NDA/confidential client material is being committed"
    - "D-02: `scripts/pdf-preprocess.mjs` runs as the npm `prebuild` lifecycle hook (not a manual command, not an Astro integration), with hash-based incremental caching keyed off PDF content hash recorded in `.cache.json`"
    - "D-04: WebP output at 1600px long-edge with quality 80 (~80KB target per page); Astro `<Image>` derives smaller responsive variants via Sharp at build time"
    - "D-06: PNG output and 2000px+ resolutions are rejected — WebP @ 1600px is the floor that meets FOUND-02's first-paint <2s budget without sacrificing slide-deck typography readability (per-piece override allowed if a specific deck has fine typography that degrades)"
    - "D-07: `pdfPaginate` schema migrates from Phase 1's `z.boolean().optional()` to `z.array(z.number().int().positive()).optional()` — array of 1-indexed page numbers; absent means single-page (cover only); breaking change but no data migration since field was unused in Phase 1"
    - "D-08: Caleb hand-curates which pages represent each deck via the `pdfPaginate` array; auto-pick heuristics (first/middle/last, evenly-spaced, first-N) are explicitly rejected — mechanical selection produces transition slides and disclaimers instead of money charts"
  artifacts:
    - path: "scripts/pdf-preprocess.mjs"
      provides: "Build-time PDF rasterization pipeline (discover, hash-cache, render, encode, write)"
      min_lines: 100
      contains: "pdfjs-dist/legacy/build/pdf.mjs"
    - path: "src/content.config.ts"
      provides: "Updated Zod schema with pdfPaginate as array(number).positive() + .describe() annotations"
      contains: "z.array(z.number().int().positive())"
    - path: "package.json"
      provides: "npm prebuild hook + pdf-preprocess script alias + pdfjs-dist moved to deps + gray-matter devDep"
      contains: "\"prebuild\":"
    - path: "scripts/verify-build.sh"
      provides: "Phase 2 Gate 7 — every piece with source.pdf has cover.webp + .cache.json"
      contains: "Phase 2 gates"
  key_links:
    - from: "package.json scripts.prebuild"
      to: "scripts/pdf-preprocess.mjs"
      via: "node invocation via npm lifecycle"
      pattern: "\"prebuild\":\\s*\"node scripts/pdf-preprocess.mjs\""
    - from: "scripts/pdf-preprocess.mjs"
      to: "pdfjs-dist verbatim Mozilla pattern"
      via: "import from pdfjs-dist/legacy/build/pdf.mjs + pdfDocument.canvasFactory"
      pattern: "pdfjs-dist/legacy/build/pdf.mjs"
    - from: "scripts/pdf-preprocess.mjs"
      to: "sharp WebP encoder"
      via: "sharp(pngBuf).resize(...).webp({quality:80}).toBuffer()"
      pattern: "\\.webp\\(\\{\\s*quality:\\s*80"
    - from: "scripts/verify-build.sh"
      to: "public/generated/pdf-thumbs/[slug]"
      via: "bash gate 7 file existence checks"
      pattern: "Phase 2 gates"
---

<objective>
Productionize Phase 1's PDF rasterization POC into a build-time pipeline. This plan delivers SC1 (`scripts/pdf-preprocess.mjs` runs as prebuild hook, emits cover.webp + paginated page-N.webp + .cache.json) and the schema migration that paginated rendering depends on (D-07).

Per PATTERNS.md planner-recommended path: extend `scripts/verify-build.sh` with Gate 7 (single bash file pattern Phase 1 established), NOT a separate `scripts/check-prebuild-outputs.mjs`. The 5-script-fragmentation listed in 02-VALIDATION.md is rejected — bash gates 7–11 across plans 01/02/03 keep the verifier coherent.

This is the foundation slice. Plans 02, 03, and 04 all depend on the schema migration (`pdfPaginate: number[]`) and on the pipeline emitting outputs at known paths.

Purpose: Phase 2 SC1 + the unblocker for SC5 paginated rendering.
Output: Working `npm run build` that auto-rasterizes any PDF in a piece directory, writes WebP thumbs to `public/generated/pdf-thumbs/[slug]/`, caches by sha256 hash, and exits clean. Gate 7 in verify-build.sh asserts the contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-asset-pipeline-real-content/02-CONTEXT.md
@.planning/phases/02-asset-pipeline-real-content/02-RESEARCH.md
@.planning/phases/02-asset-pipeline-real-content/02-PATTERNS.md
@.planning/phases/02-asset-pipeline-real-content/02-VALIDATION.md
@.planning/phases/01-walking-skeleton/01-03-SUMMARY.md

# Source files this plan modifies
@package.json
@src/content.config.ts
@scripts/pdf-poc.mjs
@scripts/verify-build.sh

<interfaces>
<!-- Key contracts. Executor uses these directly — no codebase exploration needed. -->

From scripts/pdf-poc.mjs (the pattern to extend, NOT rewrite):
```javascript
import { getDocument } from 'pdfjs-dist/legacy/build/pdf.mjs';
const CMAP_URL = './node_modules/pdfjs-dist/cmaps/';
const STANDARD_FONT_DATA_URL = './node_modules/pdfjs-dist/standard_fonts/';
// pdfDocument.canvasFactory is the built-in NodeCanvasFactory (pdfjs 5.x)
// Do NOT set GlobalWorkerOptions.workerSrc in Node (Pitfall 3 in Phase 1 RESEARCH)
```

From src/content.config.ts (Phase 1 scaffold — only `pdfPaginate` field changes):
```typescript
// BEFORE (Phase 1 D-03):
pdfPaginate: z.boolean().optional(),
// AFTER (Phase 2 D-07):
pdfPaginate: z.array(z.number().int().positive()).optional()
  .describe('1-indexed page numbers from source.pdf to render below the hero. e.g. [1, 5, 12, 23, 47] renders pages 1, 5, 12, 23, 47 in that order. Page 1 (cover) renders automatically; including it in this array is harmless but redundant.'),
```

From package.json (current scripts block — extend, don't replace):
```json
"scripts": {
  "dev": "astro dev",
  "start": "astro dev",
  "build": "astro build",
  "preview": "astro preview",
  "astro": "astro",
  "pdf-poc": "node scripts/pdf-poc.mjs",
  "test:smoke": "bash scripts/verify-build.sh"
}
```

Cache sidecar shape (the contract Plans 03 + 04 read):
```json
{
  "inputHash": "sha256-hex-of-pdf-bytes-plus-paginate-json-plus-pipeline-version",
  "generatedAt": "2026-05-10T...Z",
  "pages": [
    { "n": 1, "w": 1600, "h": 1131, "bytes": 81234, "file": "cover.webp" },
    { "n": 5, "w": 1600, "h": 1131, "bytes": 78901, "file": "page-5.webp" }
  ]
}
```

Filename contract (D-05):
- Page 1 → `cover.webp` (NOT `page-1.webp`)
- Other pages → `page-{N}.webp` where N is the LITERAL source-PDF page number (preserves traceability)

Exit code contract (Phase 1 pdf-poc.mjs idiom):
- 0: success
- 1: generic failure (npm prebuild aborts → astro build does not run)
- 2: missing input precondition
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Schema migration + package.json updates (deps + prebuild hook)</name>
  <files>src/content.config.ts, package.json, package-lock.json</files>
  <read_first>
    - src/content.config.ts (the file being modified)
    - package.json (the file being modified)
    - .planning/phases/02-asset-pipeline-real-content/02-CONTEXT.md §"PDF Source Storage & Build Integration" (D-02 prebuild hook) and §"`pdfPaginate` Frontmatter Schema" (D-07 array migration)
    - .planning/phases/02-asset-pipeline-real-content/02-RESEARCH.md §"Code Examples > Example 2" (verbatim schema migration text — copy `.describe()` strings verbatim)
    - .planning/phases/02-asset-pipeline-real-content/02-RESEARCH.md §"Common Pitfalls > Pitfall 6" (CF Pages devDeps risk — drives pdfjs-dist move to deps)
    - .planning/phases/02-asset-pipeline-real-content/02-PATTERNS.md §"src/content.config.ts (MODIFY)" and §"package.json (MODIFY)" (analog patterns)
  </read_first>
  <behavior>
    - Test 1: After `npm install`, `npm run prebuild` exists and runs `node scripts/pdf-preprocess.mjs` (script may not exist yet at task end — that's Task 2 — but the alias must be wired)
    - Test 2: `npx astro sync` succeeds with the migrated schema
    - Test 3: A test piece with `pdfPaginate: true` (boolean) FAILS schema validation; a test piece with `pdfPaginate: [1, 5, 12]` PASSES
    - Test 4: `pdfjs-dist` appears in `dependencies` (NOT `devDependencies`); `gray-matter` appears in `devDependencies`; `sharp` appears in `devDependencies` with explicit pin
  </behavior>
  <action>
    Make three coordinated edits, in this order:

    **Edit 1 — `src/content.config.ts`:** Replace the three Phase 1 forward-compat optional lines (lines 20–22 currently: `pdfPaginate: z.boolean().optional(),` / `fullPdf: z.string().optional(),` / `outcomeTagline: z.string().optional(),`) with the verbatim Phase 2 D-07 / D-17 form from 02-RESEARCH.md Example 2:

    ```typescript
        // D-07: Phase 2 migration — boolean → number[]; 1-indexed page numbers (Pitfall 4)
        pdfPaginate: z.array(z.number().int().positive())
          .optional()
          .describe('1-indexed page numbers from source.pdf to render below the hero. e.g. [1, 5, 12, 23, 47] renders pages 1, 5, 12, 23, 47 in that order. Page 1 (cover) renders automatically; including it in this array is harmless but redundant.'),
        // D-17: PIECE-06 — points at /source-pdfs/[slug].pdf (build-time copy)
        fullPdf: z.string().optional()
          .describe('Path to the full PDF for the "Open full PDF" link. Typically /source-pdfs/[slug].pdf — the prebuild script copies source.pdf to this location when this field is set.'),
        outcomeTagline: z.string().optional()
          .describe('Deferred — CONTENT-01, v2 only. Phase 2 ignores.'),
    ```

    Leave all other schema fields untouched. The `({ image }) => z.object(...)` function form, `CATEGORIES` enum import, `.min(1)` on required strings, `image()` helper, and `order` / `draft` fields all stay verbatim.

    **Edit 2 — `package.json` scripts block:** Insert two new entries between `"pdf-poc"` and `"test:smoke"`:

    ```json
        "pdf-preprocess": "node scripts/pdf-preprocess.mjs",
        "prebuild": "node scripts/pdf-preprocess.mjs",
    ```

    The `prebuild` key fires automatically before `build` per npm lifecycle (D-02, RESEARCH.md Pattern 1). The `pdf-preprocess` alias gives Caleb a manual rerun path during `astro dev` (Pitfall 5).

    **Edit 3 — `package.json` dependencies / devDependencies:**
    - Move `pdfjs-dist@^5.7.284` from `devDependencies` to `dependencies` (Pitfall 6 mitigation — eliminates A2 risk re CF Pages NODE_ENV).
    - Add `gray-matter` to `devDependencies` at version `^4.0.3` (frontmatter parser; Wave 0 gap from RESEARCH.md §"Wave 0 Gaps").
    - Add `sharp` to `devDependencies` at `^0.34.5` (explicit pin; currently transitive via Astro per RESEARCH.md §"Standard Stack").

    After editing package.json, run `npm install --no-audit --no-fund` to update package-lock.json.

    **Do NOT** touch any other field in package.json (name, version, type, private, etc.). **Do NOT** add `@napi-rs/canvas` as a direct dep — it MUST stay transitive at 0.1.x via pdfjs-dist (Pitfall 2 in RESEARCH.md; bumping to 1.0 breaks `pdfDocument.canvasFactory`).
  </action>
  <verify>
    <automated>
    cd /Users/caleb/projects/new-project &amp;&amp;
    grep -q 'z.array(z.number().int().positive())' src/content.config.ts &amp;&amp;
    grep -q '1-indexed page numbers from source.pdf' src/content.config.ts &amp;&amp;
    grep -q '"prebuild": "node scripts/pdf-preprocess.mjs"' package.json &amp;&amp;
    grep -q '"pdf-preprocess": "node scripts/pdf-preprocess.mjs"' package.json &amp;&amp;
    node -e "const p=require('./package.json'); if(!p.dependencies['pdfjs-dist']) {console.error('FAIL: pdfjs-dist not in dependencies'); process.exit(1)}" &amp;&amp;
    node -e "const p=require('./package.json'); if(p.devDependencies['pdfjs-dist']) {console.error('FAIL: pdfjs-dist still in devDependencies'); process.exit(1)}" &amp;&amp;
    node -e "const p=require('./package.json'); if(!p.devDependencies['gray-matter']) {console.error('FAIL: gray-matter not in devDependencies'); process.exit(1)}" &amp;&amp;
    node -e "const p=require('./package.json'); if(!p.devDependencies['sharp']) {console.error('FAIL: sharp not in devDependencies'); process.exit(1)}" &amp;&amp;
    npx astro sync 2>&amp;1 | grep -qE '(synced|Generated|content collection)'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'z.array(z.number().int().positive())' src/content.config.ts` returns 1
    - `grep -c '\.describe(' src/content.config.ts` returns 3 (one for pdfPaginate, fullPdf, outcomeTagline)
    - `grep -c 'pdfPaginate: z.boolean()' src/content.config.ts` returns 0 (Phase 1 form removed)
    - `node -e "const p=require('./package.json'); console.log(p.dependencies['pdfjs-dist'])"` outputs `^5.7.284` (or compatible pin)
    - `node -e "const p=require('./package.json'); console.log(p.devDependencies['gray-matter'])"` outputs a `^4.x.x` semver string
    - `node -e "const p=require('./package.json'); console.log(p.devDependencies['sharp'])"` outputs a `^0.34.x` semver string
    - `node -e "const p=require('./package.json'); console.log(p.scripts.prebuild)"` outputs `node scripts/pdf-preprocess.mjs`
    - `npx astro sync` exits 0
    - Schema fault-injection: temporarily add `pdfPaginate: true` to any piece's frontmatter → `npx astro sync` fails with a Zod validation error mentioning array; revert the test edit afterward (DO NOT commit it)
  </acceptance_criteria>
  <done>
    Schema migrated to array form with .describe() annotations matching RESEARCH.md Example 2 verbatim. package.json has the prebuild hook + pdf-preprocess alias + pdfjs-dist moved to deps + gray-matter/sharp pinned in devDeps. `npm install` completes without errors. `npx astro sync` succeeds.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement scripts/pdf-preprocess.mjs (build-time PDF pipeline)</name>
  <files>scripts/pdf-preprocess.mjs</files>
  <read_first>
    - scripts/pdf-poc.mjs (the seed pattern — Phase 2 extends, does NOT rewrite)
    - .planning/phases/02-asset-pipeline-real-content/02-RESEARCH.md §"Code Examples > Example 1" (the full skeleton to copy — lines 467–593; this IS the implementation)
    - .planning/phases/02-asset-pipeline-real-content/02-RESEARCH.md §"Architecture Patterns" (Patterns 1–5; especially Pattern 2 hash cache and Pattern 3 verbatim Mozilla pattern)
    - .planning/phases/02-asset-pipeline-real-content/02-RESEARCH.md §"Common Pitfalls" (Pitfalls 2, 3, 4 — canvas factory API, CMap URLs, 1-indexed)
    - .planning/phases/02-asset-pipeline-real-content/02-RESEARCH.md §"Assumptions Log > A7" (include pipeline-version constant in hash so script edits invalidate cache)
    - .planning/phases/02-asset-pipeline-real-content/02-PATTERNS.md §"scripts/pdf-preprocess.mjs (NEW)" (extension recipe from POC)
    - .planning/phases/02-asset-pipeline-real-content/02-CONTEXT.md §"Output Format & Resolution" (D-04, D-05, D-06) and §"`pdfPaginate` Frontmatter Schema" (D-07, D-08, D-09)
  </read_first>
  <behavior>
    - Test 1: Running `node scripts/pdf-preprocess.mjs` against a directory tree with zero pieces having `source.pdf` exits 0 with output `Found 0 pieces with source.pdf` then `DONE`.
    - Test 2: When a piece directory contains `source.pdf` and the cache is empty, the script writes `public/generated/pdf-thumbs/[slug]/cover.webp` and `.cache.json`. The .cache.json contains `inputHash`, `generatedAt`, and a `pages` array with at least one entry `{n:1, w, h, bytes, file:"cover.webp"}`.
    - Test 3: When a piece sets `pdfPaginate: [1, 5, 12]` in frontmatter, the script writes `cover.webp` (page 1) AND `page-5.webp` AND `page-12.webp`. Page 1 is NOT re-rendered as `page-1.webp` (filename contract D-05).
    - Test 4: When `pdfPaginate: [99]` references a page beyond `numPages`, the script emits `WARN [slug]: pdfPaginate references page 99 but PDF has N` to stderr and continues without crashing.
    - Test 5: Re-running the script with no PDF changes produces `SKIP [slug] (cache hit)` for each piece and exits 0 without re-encoding.
    - Test 6: When a piece sets `fullPdf` in frontmatter, `public/source-pdfs/[slug].pdf` is created (a copy of the source).
    - Test 7: Editing the script's `RENDER_SCALE` or `WEBP_QUALITY` constant invalidates the cache via the pipeline-version key (`.update('|v=2')` in the hash function).
  </behavior>
  <action>
    Create `scripts/pdf-preprocess.mjs` based on the verbatim template from `02-RESEARCH.md` §"Code Examples > Example 1" (lines 467–593). Copy the structure exactly — the executor MAY tweak whitespace and comments but MUST preserve every load-bearing line below.

    **Required imports (verbatim — no substitutions):**
    ```javascript
    import fs from 'node:fs/promises';
    import path from 'node:path';
    import { createHash } from 'node:crypto';
    import { getDocument } from 'pdfjs-dist/legacy/build/pdf.mjs';
    import sharp from 'sharp';
    import matter from 'gray-matter';
    ```
    DO NOT import from `pdfjs-dist` directly (must use `/legacy/build/pdf.mjs` — Phase 1 RESEARCH Pitfall). DO NOT import `@napi-rs/canvas` directly (must come transitively via `pdfDocument.canvasFactory` — Pitfall 2). DO NOT set `GlobalWorkerOptions.workerSrc` (Pitfall 3 in Phase 1 RESEARCH).

    **Required constants:**
    ```javascript
    const PIECES_DIR = path.resolve('src/content/pieces');
    const OUTPUT_DIR = path.resolve('public/generated/pdf-thumbs');
    const SOURCE_PDF_DIR = path.resolve('public/source-pdfs');
    const CMAP_URL = './node_modules/pdfjs-dist/cmaps/';
    const STANDARD_FONT_DATA_URL = './node_modules/pdfjs-dist/standard_fonts/';
    const RESIZE_LONG_EDGE = 1600;
    const WEBP_QUALITY = 80;
    const RENDER_SCALE = 2.0;
    const PIPELINE_VERSION = 'v2';  // bump when render constants or output format change (per A7)
    ```

    **Required functions (must exist with these names and approximate signatures):**

    1. `async function discoverPieces()` — `fs.readdir(PIECES_DIR)`, parse each `index.md` with `matter()`, return `[{ slug, sourcePdfPath, pdfPaginate, fullPdf }]` for pieces where `source.pdf` exists. Skip pieces without `source.pdf`. DO NOT use `astro:content` `getCollection` (Anti-Pattern in RESEARCH.md — only works inside Astro runtime).

    2. `async function hashInputs(pdfPath, pdfPaginate)` — sha256 over `(pdfBytes + '|paginate=' + JSON.stringify(pdfPaginate??[]) + '|v=' + PIPELINE_VERSION)`. Per A7, the pipeline-version key MUST be in the hash so editing `RENDER_SCALE` / `WEBP_QUALITY` invalidates cache.

    3. `async function rasterizePiece({ slug, sourcePdfPath, pdfPaginate, fullPdf })` — full pipeline per RESEARCH.md Example 1 lines 514–574:
       - Compute `inputHash`. Read `.cache.json` if exists. If `cached.inputHash === inputHash` → log `SKIP [slug] (cache hit)`, still call `copySourcePdf` if `fullPdf` set, return.
       - Otherwise: `mkdir -p` the thumb dir. Read PDF bytes. Call `getDocument({ data, cMapUrl: CMAP_URL, cMapPacked: true, standardFontDataUrl: STANDARD_FONT_DATA_URL }).promise`.
       - Compute `pagesToRender = [1, ...((pdfPaginate ?? []).filter(n => n !== 1))]` — page 1 always first; dedupe page 1 from paginate array.
       - For each `pageNum`: skip with `console.warn` if `pageNum > numPages`. Otherwise: `getPage(pageNum)`, viewport at `RENDER_SCALE`, `pdfDocument.canvasFactory.create(w, h)`, `page.render(...)`, `canvas.toBuffer('image/png')`, `page.cleanup()`, then Sharp encode:
         ```javascript
         const webp = await sharp(pngBuf)
           .resize({ width: RESIZE_LONG_EDGE, height: RESIZE_LONG_EDGE, fit: 'inside', withoutEnlargement: true })
           .webp({ quality: WEBP_QUALITY })
           .toBuffer();
         ```
       - Filename: `pageNum === 1 ? 'cover.webp' : \`page-${pageNum}.webp\``. (D-05 — page 1 → cover.webp; other pages → literal page number.)
       - Push `{ n: pageNum, w, h, bytes: webp.length, file: outName }` to `pageMeta`. Log `OK [slug]/[file] [w]x[h] ([kb]KB)`.
       - After loop: `await pdfDocument.cleanup()`. If `fullPdf` is set, `copySourcePdf(slug, sourcePdfPath)`. Write `.cache.json` with `{ inputHash, generatedAt: new Date().toISOString(), pages: pageMeta }`.

    4. `async function copySourcePdf(slug, sourcePdfPath)` — `mkdir -p SOURCE_PDF_DIR`, `fs.copyFile(sourcePdfPath, path.join(SOURCE_PDF_DIR, \`${slug}.pdf\`))`.

    5. `async function main()` — `discoverPieces()`, log `Found N pieces with source.pdf`, loop `rasterizePiece` for each. Wrap each piece in try/catch that logs `FAIL [slug]: [err.message]` and `process.exit(1)`. After loop: log `DONE`. Top-level: `main().catch(err => { console.error(err); process.exit(1); });`.

    **Diagnostic console pattern (PATTERNS.md §"Shared Patterns > Diagnostic console pattern"):**
    - `console.log` for `OK ...` (per-output success), `SKIP ...` (cache hit), `Found N pieces`, `DONE`
    - `console.warn` for `WARN [slug]: ...` (out-of-range pdfPaginate page; soft skip + continue)
    - `console.error` for `FAIL [slug]: ...` (hard failure; exit non-zero)

    **Exit code contract (PATTERNS.md §"Process exit-code contract"):**
    - `process.exit(0)` on full success (DONE printed)
    - `process.exit(1)` on any rasterization crash, hash mismatch, or unhandled throw

    No need to delete `scripts/pdf-poc.mjs` in this task — leave it; cleanup is out of scope. Do NOT add `--init` / argv parsing; the script is invoked with no args by the prebuild hook.
  </action>
  <verify>
    <automated>
    cd /Users/caleb/projects/new-project &amp;&amp;
    test -f scripts/pdf-preprocess.mjs &amp;&amp;
    grep -q "from 'pdfjs-dist/legacy/build/pdf.mjs'" scripts/pdf-preprocess.mjs &amp;&amp;
    grep -q "import sharp from 'sharp'" scripts/pdf-preprocess.mjs &amp;&amp;
    grep -q "import matter from 'gray-matter'" scripts/pdf-preprocess.mjs &amp;&amp;
    grep -q "import { createHash } from 'node:crypto'" scripts/pdf-preprocess.mjs &amp;&amp;
    grep -q "pdfDocument.canvasFactory" scripts/pdf-preprocess.mjs &amp;&amp;
    ! grep -q "GlobalWorkerOptions" scripts/pdf-preprocess.mjs &amp;&amp;
    ! grep -q "@napi-rs/canvas" scripts/pdf-preprocess.mjs &amp;&amp;
    grep -q "PIPELINE_VERSION" scripts/pdf-preprocess.mjs &amp;&amp;
    grep -q "RESIZE_LONG_EDGE = 1600" scripts/pdf-preprocess.mjs &amp;&amp;
    grep -q "WEBP_QUALITY = 80" scripts/pdf-preprocess.mjs &amp;&amp;
    grep -q "fit: 'inside'" scripts/pdf-preprocess.mjs &amp;&amp;
    grep -q "cover.webp" scripts/pdf-preprocess.mjs &amp;&amp;
    grep -q "page-\${pageNum}.webp" scripts/pdf-preprocess.mjs &amp;&amp;
    npm run prebuild 2>&amp;1 | tail -5 | grep -qE '(DONE|Found 0 pieces)'
    </automated>
  </verify>
  <acceptance_criteria>
    - `scripts/pdf-preprocess.mjs` exists and is at least 80 lines
    - `grep -c "from 'pdfjs-dist/legacy/build/pdf.mjs'" scripts/pdf-preprocess.mjs` returns 1 (verbatim Mozilla entry — Pitfall 3 mitigation)
    - `grep -c "GlobalWorkerOptions" scripts/pdf-preprocess.mjs` returns 0 (Phase 1 RESEARCH Pitfall — must NOT set workerSrc in Node)
    - `grep -c "@napi-rs/canvas" scripts/pdf-preprocess.mjs` returns 0 (Pitfall 2 — must come transitively via pdfDocument.canvasFactory)
    - `grep -c "pdfDocument.canvasFactory" scripts/pdf-preprocess.mjs` returns at least 1
    - `grep -c "PIPELINE_VERSION" scripts/pdf-preprocess.mjs` returns at least 2 (declared once + used in hash once per A7)
    - `grep -c "fit: 'inside'" scripts/pdf-preprocess.mjs` returns 1 (long-edge resize, NOT crop — Anti-Pattern in RESEARCH.md)
    - `grep -c 'cover.webp' scripts/pdf-preprocess.mjs` returns at least 1 (D-05 page 1 filename contract)
    - `grep -cE 'page-\$\{pageNum\}\.webp|page-\$\{n\}\.webp' scripts/pdf-preprocess.mjs` returns at least 1 (D-05 paginated filename uses literal page number)
    - `grep -c 'gray-matter' scripts/pdf-preprocess.mjs` returns at least 1 (frontmatter parser — anti-pattern: do NOT regex-parse frontmatter)
    - `npm run prebuild` exits 0 against the current Phase 1 piece tree (no source.pdf files exist yet) — output ends with `DONE`
    - Script does NOT call `getCollection` or import from `astro:content` (anti-pattern — that API only works in Astro runtime)
  </acceptance_criteria>
  <done>
    `scripts/pdf-preprocess.mjs` exists, follows the verbatim Mozilla pattern, uses sharp for WebP encoding, hashes inputs with pipeline-version key, writes `.cache.json` sidecar with the documented shape, and runs cleanly via `npm run prebuild` against the current (PDF-free) Phase 1 piece tree. Page-1 outputs are named `cover.webp`; paginated outputs use literal source-PDF page numbers. The fullPdf side-effect copies source.pdf to public/source-pdfs/[slug].pdf when the frontmatter flag is set.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend scripts/verify-build.sh with Phase 2 Gate 7 (thumbs + cache existence) + smoke-test the full build</name>
  <files>scripts/verify-build.sh</files>
  <read_first>
    - scripts/verify-build.sh (the file being modified — preserve Phase 1 gates 1–6 verbatim)
    - .planning/phases/02-asset-pipeline-real-content/02-RESEARCH.md §"Code Examples > Example 4" (verbatim Gate 7 bash to copy — lines 666–686)
    - .planning/phases/02-asset-pipeline-real-content/02-PATTERNS.md §"scripts/verify-build.sh (MODIFY)" (gate idiom: numbered comment + if/else/grep -q + fail=1 + OK echo)
    - .planning/phases/02-asset-pipeline-real-content/02-VALIDATION.md §"Phase 2 Success Criteria → Verification" (SC1)
    - .planning/phases/02-asset-pipeline-real-content/02-CONTEXT.md §"PDF Source Storage" (D-03 — outputs ARE committed; verify-build.sh asserts presence as the canonical contract)
  </read_first>
  <behavior>
    - Test 1: After Task 2 lands, running `npm run build && npm run test:smoke` against a tree with zero source.pdf files passes ALL GREEN (no piece triggers Gate 7 — it's a no-op in the empty case).
    - Test 2: When a hypothetical piece has source.pdf but no thumbs (e.g. cache deleted manually), Gate 7 FAILs with `FAIL: [slug] has source.pdf but no [thumb_dir]/cover.webp`.
    - Test 3: When a piece has cover.webp but no .cache.json, Gate 7 FAILs with `FAIL: [slug] has cover.webp but no .cache.json sidecar`.
    - Test 4: Verifier preserves Phase 1's 6 gates and the ALL GREEN / FAILED summary block.
  </behavior>
  <action>
    Open `scripts/verify-build.sh`. Update the title comment header (lines 2–3) and the printed banner (lines 12–13) to reflect Phase 2:

    Replace `# Phase 1 smoke verification: runs over dist/ after \`npm run build\`.` with `# Phase 1 + 2 smoke verification: runs over dist/ + public/generated/ after \`npm run build\`.`

    Replace the printed banner:
    ```bash
    echo "Phase 1 smoke verification"
    echo "=========================="
    ```
    with:
    ```bash
    echo "Phase 1 + 2 smoke verification"
    echo "=============================="
    ```

    Then INSERT the following block AFTER Phase 1 Gate 6 (the existing `done < <(find ...)` block ending around line 72) and BEFORE the final `echo "=========================="` summary block. Copy verbatim from RESEARCH.md Example 4 lines 666–686:

    ```bash

    # Gate 7: every piece with source.pdf has a thumb generated + cache sidecar
    echo
    echo "Phase 2 gates"
    echo "============="
    shopt -s nullglob
    for piece_dir in src/content/pieces/*/; do
      slug=$(basename "$piece_dir")
      if [[ -f "$piece_dir/source.pdf" ]]; then
        thumb_dir="public/generated/pdf-thumbs/$slug"
        if [[ ! -f "$thumb_dir/cover.webp" ]]; then
          echo "  FAIL: $slug has source.pdf but no $thumb_dir/cover.webp"
          fail=1
        elif [[ ! -f "$thumb_dir/.cache.json" ]]; then
          echo "  FAIL: $slug has cover.webp but no .cache.json sidecar"
          fail=1
        else
          echo "  OK: $slug has cover.webp + cache"
        fi
      fi
    done
    ```

    Preserve Gate 6's trailing `echo "  OK: PIECE-02 ..."` line, the final `echo "=========================="`, and the `if [[ $fail -eq 0 ]]` summary block VERBATIM. Plans 02 + 03 will append Gates 8/9 and 10/11 after Gate 7 in this same `Phase 2 gates` section.

    Do NOT add gates 8–11 in this task — those belong to Plans 02 (resume + bio gates) and 03 (paginated + fullPdf gates).

    After editing, run `npm run build && npm run test:smoke` to confirm: prebuild fires, runs pdf-preprocess.mjs (no-op since no source.pdf), astro builds, smoke test runs and prints `Phase 2 gates` heading, no FAIL lines, ALL GREEN.
  </action>
  <verify>
    <automated>
    cd /Users/caleb/projects/new-project &amp;&amp;
    grep -q 'Phase 1 + 2 smoke verification' scripts/verify-build.sh &amp;&amp;
    grep -q '# Gate 7:' scripts/verify-build.sh &amp;&amp;
    grep -q 'Phase 2 gates' scripts/verify-build.sh &amp;&amp;
    grep -q 'thumb_dir=.public/generated/pdf-thumbs' scripts/verify-build.sh &amp;&amp;
    grep -q 'cover.webp' scripts/verify-build.sh &amp;&amp;
    grep -q '.cache.json sidecar' scripts/verify-build.sh &amp;&amp;
    bash -n scripts/verify-build.sh &amp;&amp;
    npm run build 2>&amp;1 | tail -3 &amp;&amp;
    npm run test:smoke 2>&amp;1 | tail -10 | grep -q 'ALL GREEN'
    </automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/verify-build.sh` (syntax check) exits 0
    - `grep -c '# Gate 7:' scripts/verify-build.sh` returns 1
    - `grep -c 'Phase 2 gates' scripts/verify-build.sh` returns 1
    - `grep -c '# Gate 1:' scripts/verify-build.sh` returns 1 (Phase 1 gates preserved)
    - `grep -c '# Gate 6' scripts/verify-build.sh` returns 1 OR Phase 1 Gate 6 logic still visible (preserved)
    - `grep -c 'ALL GREEN' scripts/verify-build.sh` returns 1 (summary block preserved)
    - `grep -c 'shopt -s nullglob' scripts/verify-build.sh` returns at least 1 (handles empty piece dirs gracefully)
    - `npm run build` exits 0 (prebuild fires, runs pdf-preprocess.mjs as no-op, astro build succeeds)
    - `npm run test:smoke` exits 0 with `ALL GREEN` in output (no piece has source.pdf yet, so Gate 7 is a no-op pass)
    - The smoke output contains the literal substring `Phase 2 gates` (proves the new section ran)
  </acceptance_criteria>
  <done>
    `scripts/verify-build.sh` carries Phase 1 Gates 1–6 verbatim, has a new `Phase 2 gates` section with Gate 7 asserting cover.webp + .cache.json existence for any piece with source.pdf, and the ALL GREEN summary block intact. `npm run build && npm run test:smoke` exits 0 against the current piece tree (no source.pdf files yet — Gate 7 is a no-op skip). The pipeline plumbing is end-to-end green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caleb's Mac → git | Source PDFs (potentially containing client metadata, embedded fonts, EXIF) are committed via D-01 colocation. Boundary check: confirm no NDA / confidential client material before commit. |
| pdfjs-dist + @napi-rs/canvas → build pipeline | Both libraries process arbitrary PDF input (Caleb's portfolio decks). Supply-chain risk via npm dependency tree. |
| Build script → filesystem | `scripts/pdf-preprocess.mjs` writes to `public/generated/pdf-thumbs/` and `public/source-pdfs/`. Path-injection risk if slug values are not validated. |
| Generated outputs → CDN (Cloudflare Pages) | `public/generated/pdf-thumbs/**` and `public/source-pdfs/**` served as static assets. No SSR, no auth, no PII processing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Source PDF colocation in git (D-01) | mitigate | Manual gate before each piece commits in Plan 04: confirm PDF contains no NDA / client-confidential content. Documented as an authoring checklist; not enforced at code level since source-of-truth is Caleb's judgment about his own work history. |
| T-02-02 | Information Disclosure | Source PDF metadata leak via committed `source.pdf` files | accept | Phase 2 explicitly stops at "no NDA material" gate (T-02-01). Rich metadata (author name, software fingerprints) in committed PDFs is acceptable risk for Caleb's own portfolio work where he is the author. Resume PDF separately gets EXIF-stripped (Plan 02 — separate threat T-02-04). |
| T-02-03 | Information Disclosure | Supply-chain compromise of `pdfjs-dist`, `@napi-rs/canvas`, `sharp`, or `gray-matter` could exfiltrate source PDFs at build time | mitigate | Pin versions explicitly: `pdfjs-dist@^5.7.284` (pinned in Phase 1, moved to deps in this plan), `@napi-rs/canvas` at transitive `0.1.x` (Pitfall 2 — DO NOT bump to 1.0), `sharp@^0.34.5` (explicit pin added this plan), `gray-matter@^4.0.3` (explicit pin added this plan). Document upgrade path in SUMMARY: `npm audit` before any dep bump. |
| T-02-04 | Information Disclosure | Resume PDF EXIF leak (author email, software fingerprints, machine name) | mitigate | OUT OF SCOPE FOR THIS PLAN — addressed in Plan 02 (resume EXIF-strip via exiftool + qpdf pipeline; Pitfall 9 in RESEARCH.md). Cited here for trust-boundary completeness. |
| T-02-05 | Tampering | Slug-derived path injection in `scripts/pdf-preprocess.mjs` (`path.join(OUTPUT_DIR, slug, 'cover.webp')` could escape OUTPUT_DIR if `slug` is `..` or contains `/`) | mitigate | Slug is `path.basename(piece_dir)` from `fs.readdir(PIECES_DIR)` — already filesystem-validated. Filesystem cannot return `..` or `/` from `readdir`. ACCEPTED as inherent input safety; no extra validation added. |
| T-02-06 | Denial of Service | Malicious or malformed PDF causes pdfjs-dist to hang or OOM the build | accept | All input PDFs are Caleb's own portfolio assets. Threat model assumes trusted input. CF Pages build has a hard timeout (~25min) which would surface a hang. Phase 1 POC already validated 28MB / 64-page deck cleanly. |
| T-02-07 | Tampering | Cache-poisoning via attacker editing `.cache.json` to skip rasterization of malicious PDF | accept | `.cache.json` is committed to git per D-03 — any change shows in `git diff`. Caleb is sole committer to this repo. ACCEPTED. |
| T-02-08 | Repudiation | Build fails silently if `pdfjs-dist` is missing under hypothetical CF Pages NODE_ENV=production | mitigate | This plan moves `pdfjs-dist` from devDependencies → dependencies (per Pitfall 6). Eliminates A2 risk. Verified by Task 1 acceptance criterion (`p.dependencies['pdfjs-dist']` non-null). |
| T-02-09 | Information Disclosure | `public/generated/pdf-thumbs/[slug]/.cache.json` leaks generation timestamps and file sizes | accept | Public static asset; metadata is non-sensitive (timestamp + byte counts). No remediation. |

**Severity assessment:** No HIGH severity threats. All mitigations are baked into the task actions; no separate mitigation tasks needed.

**Phase 1 deferred verification (CF Pages Linux parity, Assumption A1):** This plan introduces the prebuild hook that A1 depends on. Real verification requires pushing to a CF Pages preview branch — that's out-of-scope for this plan (no CF account connection assumed). Recommendation surfaced in Plan 04 SUMMARY: optionally Docker-simulate (`docker run --rm node:22-bookworm`) before committing real piece PDFs. NOT a blocking dependency for this plan since the local macOS verification of `npm run build` is sufficient to prove the pipeline plumbing.
</threat_model>

<verification>
After all three tasks complete, the full slice is verifiable end-to-end:

1. **Schema migration locked in:**
   - `npx astro sync` exits 0
   - Manual fault-injection: a piece with `pdfPaginate: true` causes `astro sync` to fail with a clear array-shape error (don't commit the test edit)
   - `grep` confirms `z.array(z.number().int().positive())` and three `.describe()` annotations

2. **Pipeline runs end-to-end:**
   - `npm run build` exits 0
   - Build output shows `pdf-preprocess.mjs` ran first (look for `Found N pieces with source.pdf` followed by `DONE`)
   - Then astro build runs and emits dist/

3. **Verifier covers the new contract:**
   - `npm run test:smoke` exits 0 with `ALL GREEN`
   - Output contains the literal `Phase 2 gates` heading
   - Phase 1 gates 1–6 still present and green

4. **Cache behavior validated (manual exercise — not gated, but executor should run once for confidence):**
   - Drop a small test PDF at `src/content/pieces/design-real-piece/source.pdf`
   - Run `npm run build` → expect `OK design-real-piece/cover.webp ...` log line
   - Verify `public/generated/pdf-thumbs/design-real-piece/cover.webp` and `.cache.json` exist
   - Re-run `npm run build` → expect `SKIP design-real-piece (cache hit)`
   - REVERT the test (delete the source.pdf and the generated outputs); do NOT commit them — real piece authoring is Plan 04's job
</verification>

<success_criteria>
- [ ] `src/content.config.ts` schema migrated to `pdfPaginate: z.array(z.number().int().positive())` with three `.describe()` annotations matching RESEARCH.md Example 2 verbatim
- [ ] `package.json` has `prebuild` + `pdf-preprocess` script aliases pointing at `node scripts/pdf-preprocess.mjs`
- [ ] `pdfjs-dist` is in `dependencies` (NOT `devDependencies`) — eliminates Pitfall 6 / A2 risk
- [ ] `gray-matter` and `sharp` are explicit `devDependencies` with semver pins
- [ ] `scripts/pdf-preprocess.mjs` exists, uses verbatim Mozilla pattern (`pdfjs-dist/legacy/build/pdf.mjs` + `pdfDocument.canvasFactory`), encodes WebP via Sharp at 1600px long-edge q80, hashes with pipeline-version key, writes `.cache.json` sidecar
- [ ] `scripts/verify-build.sh` carries Phase 1 Gates 1–6 verbatim AND adds Gate 7 (thumbs + cache existence)
- [ ] `npm run build && npm run test:smoke` exits 0 with `ALL GREEN` against the current piece tree (no `source.pdf` files yet)
- [ ] No `@napi-rs/canvas` direct import (must come transitively via pdfjs canvasFactory)
- [ ] No `GlobalWorkerOptions.workerSrc` set (Phase 1 Pitfall — would break legacy entry)
- [ ] No `getCollection` / `astro:content` import in pdf-preprocess.mjs (anti-pattern — Astro runtime API)
</success_criteria>

<output>
After all three tasks pass verification, create `.planning/phases/02-asset-pipeline-real-content/02-01-SUMMARY.md` documenting:
- The schema migration (D-07)
- The package.json changes (deps shuffle, prebuild hook, gray-matter + sharp pins)
- The pdf-preprocess.mjs implementation (filename contract, hash contract, exit codes)
- The verify-build.sh Gate 7 extension
- A "Caleb workflow notes" section for Pitfall 5 (prebuild does NOT fire on `astro dev` — manual rerun via `npm run pdf-preprocess`)
- A "Next plan readiness" section: Plan 02 unblocked (independent — about page + resume); Plan 03 unblocked but should run after Plan 02 due to verify-build.sh sequencing; Plan 04 unblocked but depends on Plans 02 + 03.
</output>
