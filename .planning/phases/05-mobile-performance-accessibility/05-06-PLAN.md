---
phase: 5
plan: 06
type: execute
wave: 2
depends_on: [03, 04, 05]
files_modified:
  - src/styles/tokens.css
  - src/components/DisciplineCard.astro
  - src/pages/index.astro
autonomous: true
requirements:
  - FOUND-03
must_haves:
  truths:
    - "The global `*, *::before, *::after` reduced-motion clamp at `tokens.css:63-70` is REMOVED"
    - "The line `.b-card:hover, .b-piece:hover { transform: none !important; }` inside that block is REMOVED"
    - "Under `prefers-reduced-motion: reduce`, the four EXEMPT motions still fire: card hover-tilt (3D rotateX/Y, 380ms transition), card click-shake (220ms keyframe), liquid-glass overlay fade (380ms opacity transition), lime-dot pulse on StatusPill (1.6s infinite)"
    - "Under `prefers-reduced-motion: reduce`, the four DISABLED motions are suppressed: portrait carousel auto-advance, portrait carousel slide CSS transition, discipline-card entrance shake (750ms keyframe), bio-card entrance shake (750ms keyframe)"
    - "Card rest tilts (`transform: rotate(±1deg)` static — no transition or animation property) continue to apply at all times (they were never motion)"
    - "The slow-scroll on `/about?to=contact` falls back to instant jump under reduced-motion (was already implemented in D-05 ship)"
    - "Phase 1–4 gates 1–22 + Phase 5 Gates 23/24/25 all pass"
  artifacts:
    - path: "src/styles/tokens.css"
      provides: "Removes the `*` selector reduced-motion clamp block; keeps every other rule intact (including the `.visually-hidden` utility added by Plan 05-03 and the `--sp-3` + amended comments from Plan 05-05)"
    - path: "src/components/DisciplineCard.astro"
      provides: "Adds `@media (prefers-reduced-motion: reduce) { .b-card { animation: none; } }` to disable the entrance shake keyframe (was previously clobbered by the global * clamp)"
    - path: "src/pages/index.astro"
      provides: "Adds `@media (prefers-reduced-motion: reduce) { .b-bio { animation: none; } }` to disable bio-card entrance shake; verifies existing carousel slide-transition guard (index.astro:198-200) still fires after global block is removed"
  key_links:
    - from: "tokens.css (global hammer removed)"
      to: "exempt motions (hover-tilt, click-shake, glass, pulse) now run under reduced-motion"
      via: "no override at all — the absence of the global clamp is what lets them through"
      pattern: "(NOT @media \\(prefers-reduced-motion: reduce\\) \\{\\s*\\*,)"
    - from: "DisciplineCard.astro + index.astro (per-source disables)"
      to: "explicit suppression of entrance shakes"
      via: "surgical `@media (prefers-reduced-motion: reduce) { .b-card { animation: none } }` rules"
      pattern: "@media \\(prefers-reduced-motion: reduce\\) \\{[^}]*animation: none"
---

<objective>
Wave 2 — execute the surgical reduced-motion pass per D-08, 05-UI-SPEC §"Interaction Contract — Motion Policy", and 05-RESEARCH §3.4. This is the architectural correction that lets D-08's amended FOUND-03 policy actually take effect: the current global `*` clamp in `tokens.css` silently kills the four motions D-08 wants to keep.

Purpose: SC3 — `prefers-reduced-motion: reduce` honored per D-08 exemption policy. Today, toggling Reduce Motion on macOS kills the hover-tilt, click-shake, and liquid-glass — which D-08 classifies as essential interaction feedback. After this plan, the policy matches the contract.

Output: Three surgical edits — remove the global hammer from `tokens.css`, add two per-source disables in `DisciplineCard.astro` and `index.astro`. Net code volume: tiny. Net architectural correctness: large.

**Critical dependency:** Plan 05-04 already removed the three per-component `@media (prefers-reduced-motion) .b-piece:hover { transform: none }` blocks in Gallery{A12,B35,C68}.astro. Do NOT re-add them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mobile-performance-accessibility/05-CONTEXT.md
@.planning/phases/05-mobile-performance-accessibility/05-RESEARCH.md
@.planning/phases/05-mobile-performance-accessibility/05-UI-SPEC.md
@.planning/phases/05-mobile-performance-accessibility/05-04-SUMMARY.md
@.planning/phases/05-mobile-performance-accessibility/05-05-SUMMARY.md
@src/styles/tokens.css
@src/components/DisciplineCard.astro

<interfaces>
<!-- Current global hammer (tokens.css:63-70, must DELETE in Task 1): -->
<!-- @media (prefers-reduced-motion: reduce) { -->
<!--   *, *::before, *::after { -->
<!--     animation-duration: 0.01ms !important; -->
<!--     animation-iteration-count: 1 !important; -->
<!--     transition-duration: 0.01ms !important; -->
<!--   } -->
<!--   .b-card:hover, .b-piece:hover { transform: none !important; } -->
<!-- } -->

<!-- D-08 exempt motions (must still fire after the hammer is gone — no action needed; they just stop being clobbered): -->
<!--   #5  DisciplineCard.astro:281-287 — card hover-tilt (transition 0.38s) -->
<!--   #6  DisciplineCard.astro:336-338 — card click-shake (220ms keyframe) -->
<!--   #7  DisciplineCard.astro:81      — liquid-glass overlay fade (transition: opacity 380ms) -->
<!--   #8  index.astro:373-374           — bio-card hover-tilt -->
<!--   #9  index.astro:386-387           — bio-card liquid-glass -->
<!--   #10 DisciplineCard.astro:156, 181, 209, 237 — card rest tilts (STATIC transform, no animation property) -->
<!--   #11 StatusPill.astro:71           — lime-dot pulse (animation: pulse 1.6s infinite) -->
<!--   #12 StatusPill.astro:49-51        — hover scale -->
<!--   #13 index.astro:625-630           — role-link click shake -->
<!--   #16 [category].astro/[slug].astro/about.astro — back-pill color transition (color-only, no spatial motion) -->
<!--   #17 about.astro:248-258           — values-pill hover translateY -->
<!--   #18 [slug].astro:282              — pager link color transition -->
<!--   #19 Gallery* — tile hover scale+rotate (Plan 05-04 removed the per-component @media block; D-08 keeps these exempt) -->

<!-- D-08 disabled motions (must add per-source @media block where not already present): -->
<!--   #1  index.astro:738-745 — carousel auto-advance (already JS-guarded; no action) -->
<!--   #2  index.astro:198-200 — carousel slide transition (PARTIAL guard exists; verify it still fires after global hammer removed) -->
<!--   #3  DisciplineCard.astro:319 — card entrance shake (NEEDS new @media block — Task 2) -->
<!--   #4  index.astro:375      — bio-card entrance shake (NEEDS new @media block — Task 3) -->
<!--   #14 about.astro:355-368 — slow-scroll (already JS-guarded; no action) -->
<!--   #15 Base.astro:117      — .skip transition (already CSS-guarded at 125-127; no action) -->
<!--   #20 Plan 05-07 owns the new touch-entrance shimmer (D-07) — its CSS-side disable lives there, not here -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the global *-selector reduced-motion clamp from tokens.css</name>
  <files>src/styles/tokens.css</files>
  <behavior>
    - `grep -E '^\s*\*,\s*\*::before' src/styles/tokens.css` returns zero matches.
    - `grep -F 'transform: none !important' src/styles/tokens.css` returns zero matches.
    - The `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { ... } .b-card:hover, .b-piece:hover { ... } }` block is GONE.
    - Every OTHER rule in tokens.css is unchanged: `--sp-3: 12px` (Plan 05-05), amended `--terracotta` comment (Plan 05-05), `.visually-hidden` utility (Plan 05-03), all color/spacing/font-size tokens.
    - `npm run build` succeeds (Astro recompiles scoped CSS hashes — expected).
    - After this commit, on a desktop with macOS Reduce Motion ON, the discipline-card hover-tilt is broken in the OPPOSITE way — it now FIRES under reduced-motion (which is what D-08 wants). The entrance shake also still fires (will be fixed in Tasks 2+3).
  </behavior>
  <action>
    Open `src/styles/tokens.css`. Locate the block starting at line ~63 with `@media (prefers-reduced-motion: reduce) {` and extending through the closing `}` for that media query (~line 70). It contains:
    - The `*, *::before, *::after { animation-duration: 0.01ms !important; ... }` rule
    - The `.b-card:hover, .b-piece:hover { transform: none !important; }` rule

    Delete the ENTIRE `@media` block (opening line through closing brace). Per 05-RESEARCH §3.4 step 1: this is "remove the global hammer." Per 05-UI-SPEC §"Implementation contract" step 1.

    Do NOT add any replacement rule here. The per-source disables for the two entrance shakes land in Tasks 2 and 3 in their respective component files (D-08 step 2 "add surgical disables at each non-exempt motion source").

    Do NOT touch any other token, comment, or rule. The `.visually-hidden` utility added by Plan 05-03 stays. The `--sp-3` and `--terracotta` comment from Plan 05-05 stays.

    After the edit, run `npm run build`. Astro recompiles scoped CSS hashes — that's expected and not a regression.
  </action>
  <verify>
    <automated>! grep -nE '^\s*\*,\s*\*::before' src/styles/tokens.css &amp;&amp; ! grep -F 'transform: none !important' src/styles/tokens.css &amp;&amp; grep -F -- '--sp-3: 12px' src/styles/tokens.css &amp;&amp; grep -F 'visually-hidden' src/styles/tokens.css &amp;&amp; npm run build</automated>
  </verify>
  <done>
    Global hammer removed. Plan 05-03's `.visually-hidden` and Plan 05-05's `--sp-3` + `--terracotta` amendments still present. Build succeeds. Advances SC3 (architectural correctness — exempt motions are now ungated).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add surgical reduced-motion disable for discipline-card entrance shake in DisciplineCard.astro</name>
  <files>src/components/DisciplineCard.astro</files>
  <behavior>
    - Under `prefers-reduced-motion: reduce`, the `.b-card` element does NOT run its 750ms entrance shake (`card-shake` keyframe at DisciplineCard.astro:319).
    - Under `prefers-reduced-motion: no-preference`, the entrance shake still fires once per page load (existing behavior preserved).
    - The card hover-tilt (transform: perspective(1200px) rotateX/Y), click-shake (220ms keyframe), and liquid-glass overlay continue to fire under BOTH preferences (D-08 exempt — they're untouched by this task; the absence of the global hammer is what lets them through under reduced-motion).
    - Card rest tilts (rotate(±1deg) static at lines 156/181/209/237) continue to apply at all times (never motion).
    - No regression to existing card hover/click/focus behavior on desktop or touch (touch-hover gating lands in Plan 05-07).
  </behavior>
  <action>
    Open `src/components/DisciplineCard.astro`. In the `<style>` block (or scoped style), AFTER the existing `@keyframes card-shake { ... }` definition and AFTER the rule that applies it (around line 319 — `.b-card { animation: card-shake 750ms ease-in-out 400ms 1; ... }` or similar), append:

    ```
    @media (prefers-reduced-motion: reduce) {
      .b-card {
        animation: none;
      }
    }
    ```

    Per 05-RESEARCH §3.4 step 2 ("Card entrance shake → Add `@media (prefers-reduced-motion: reduce) { .b-card { animation: none; } }` to DisciplineCard.astro after line 320"). Per 05-UI-SPEC §"Implementation contract" step 3.

    Do NOT touch:
    - The `.b-card:hover` rule (still applies transform: perspective(1200px) rotateX(...)... — exempt per D-08 #5)
    - The `.b-card.is-shaking` rule (220ms click-shake — exempt per D-08 #6; Plan 05-07's entrance-shimmer is separate)
    - The `::before` liquid-glass pseudo-element (exempt per D-08 #7)
    - The `.b-card.k1/.k2/.k3/.k4` rest-tilt rotate transforms (static — exempt per D-08 #10)
    - Plan 05-04's `@media (prefers-reduced-motion) .b-piece:hover` removal — that's on Gallery* components, not this one
    - Plan 05-07's upcoming hover-gate `@media (hover: hover) and (pointer: fine)` wrapping — that's a future edit

    Run `npm run build`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; grep -E '@media \(prefers-reduced-motion: reduce\) \{[^}]*\.b-card \{[^}]*animation: none' src/components/DisciplineCard.astro</automated>
  </verify>
  <done>
    Surgical reduced-motion disable for `.b-card` entrance shake is in place. Hover-tilt, click-shake, liquid-glass, rest tilts all unaffected (exempt). Advances SC3.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add surgical reduced-motion disable for bio-card entrance shake in index.astro + verify carousel slide-transition guard</name>
  <files>src/pages/index.astro</files>
  <behavior>
    - Under `prefers-reduced-motion: reduce`, the `.b-bio` element does NOT run its 750ms entrance shake (`bio-shake` keyframe at index.astro:375).
    - The carousel slide CSS transition (index.astro:189 — `transition: transform 650ms cubic-bezier(...)`) is suppressed under reduced-motion (existing partial guard at index.astro:198-200 should still apply — verify it does after the global hammer is gone; if the existing guard was `transition-duration: 0` it should still work).
    - The carousel auto-advance JS guard (index.astro:738-745) still fires (already correct — JS reads `window.matchMedia('(prefers-reduced-motion: reduce)').matches` at start).
    - The bio-card hover-tilt + liquid-glass continue to fire under both preferences (D-08 #8, #9 exempt).
    - The role-link click-shake at index.astro:625-630 continues to fire under both preferences (D-08 #13 exempt — user-initiated).
    - `bash scripts/verify-build.sh` — Gates 1–25 all GREEN.
    - Visual smoke: load splash with macOS Reduce Motion ON — bio card does NOT shake on entrance; cards do NOT shake; carousel does NOT auto-advance; hover a card → tilt+glass FIRE; click role-link → role-link shakes; lime-dot pulses.
  </behavior>
  <action>
    Open `src/pages/index.astro`. In the `<style>` block, AFTER the existing `@keyframes bio-shake { ... }` definition and AFTER the `.b-bio { animation: bio-shake 750ms ease-in-out 400ms 1; ... }` rule (around line 375), append:

    ```
    @media (prefers-reduced-motion: reduce) {
      .b-bio {
        animation: none;
      }
    }
    ```

    Per 05-RESEARCH §3.4 step 2 ("Bio card entrance shake → Add `@media (prefers-reduced-motion: reduce) { .b-bio { animation: none; } }` to index.astro after line 376"). Per 05-UI-SPEC §"Implementation contract" step 3.

    Then **verify** (don't modify) the existing carousel slide-transition guard at index.astro:198-200. It should look like:
    ```
    @media (prefers-reduced-motion: reduce) {
      .b-carousel .slide { transition: none; }  /* or transition-duration: 0 */
    }
    ```
    If present and shaped like above, leave it alone. If MISSING (the global hammer used to absorb its job), add it now at the appropriate location near the carousel slide rule. Per 05-RESEARCH §2.3 motion source #2 + §3.4 step 2 ("Carousel slide transition... already partial at 198-200; verify it still fires once the * clamp is gone").

    Do NOT touch:
    - The carousel auto-advance JS at index.astro:738-745 (already JS-gated correctly)
    - The bio-card hover rules (exempt — Plan 05-07's hover-gate wrap is a later edit)
    - The role-link click-shake (exempt)
    - The `<Image priority>` work Plan 05-04 just landed
    - Any token-sweep edit Plan 05-05 just landed

    After the edit run `npm run build && bash scripts/verify-build.sh`. All 25 gates MUST be GREEN.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/05-06-t3.log &amp;&amp; ! grep -F 'FAIL' /tmp/05-06-t3.log &amp;&amp; grep -E '@media \(prefers-reduced-motion: reduce\) \{[^}]*\.b-bio \{[^}]*animation: none' src/pages/index.astro</automated>
  </verify>
  <done>
    Surgical reduced-motion disable for `.b-bio` entrance shake is in place. Carousel slide-transition guard verified present. All Phase 1–5 Gates GREEN. Manual reduced-motion verification recorded in Plan 05-08. Advances SC3.
  </done>
</task>

</tasks>

<verification>
- `bash scripts/verify-build.sh` — all 25 gates OK
- macOS Reduce Motion ON test (manual, recorded in 05-08): cards don't shake on entrance, bio doesn't shake on entrance, carousel doesn't auto-advance, slow-scroll on `/about?to=contact` falls back to instant, but: hover a card → tilt+glass FIRE; click a role-link → role-link shakes; lime-dot pulses
- macOS Reduce Motion OFF test: all motions work as before Phase 5
</verification>

<success_criteria>
SC3 is closed. The D-08 amended FOUND-03 policy is implemented surgically rather than via a global hammer. The four exempt motions fire under reduced-motion; the four disabled motions are suppressed; the static rest tilts always apply.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-performance-accessibility/05-06-SUMMARY.md`. Note:
- The architectural correction (delete global hammer + add 2 per-source disables) is small in code volume but large in correctness
- The carousel slide-transition guard at index.astro:198-200 was VERIFIED PRESENT, not added — Phase 3 had it; the global hammer was making it redundant
- The three per-Gallery `transform: none !important` blocks were already removed by Plan 05-04 — re-confirm they stayed gone
</output>
