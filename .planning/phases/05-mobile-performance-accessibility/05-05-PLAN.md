---
phase: 5
plan: 05
type: execute
wave: 1
depends_on: [01, 03, 04]
files_modified:
  - src/styles/tokens.css
  - src/layouts/Base.astro
  - src/pages/404.astro
  - src/pages/[category].astro
  - src/pages/[category]/[slug].astro
  - src/pages/about.astro
  - src/pages/index.astro
  - src/components/DisciplineCard.astro
  - .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md
autonomous: true
requirements:
  - FOUND-01
must_haves:
  truths:
    - "`src/styles/tokens.css` declares `--sp-3: 12px` between `--sp-2` and `--sp-4` (fixes the `var(--sp-3)` silent failure at about.astro:98 per 05-RESEARCH §5.3)"
    - "`src/styles/tokens.css` `--terracotta` comment no longer says 'decorative accent only' — replaced with the load-bearing description from 05-UI-SPEC §Token registration audit"
    - "`src/styles/tokens.css` `--lime` comment exists and documents the rationale (status-indicator pop, distinct from `--acid` content accent)"
    - "Zero raw `font-size: Npx` (or `Nrem`) literals anywhere under `src/components/`, `src/pages/`, `src/layouts/` — every literal migrated per `05-TOKEN-MAP.md`"
    - "`05-VERIFICATION.md` --terracotta audit table is filled in (every occurrence has a Verdict)"
    - "Gate 25 in `scripts/verify-build.sh` reports OK"
    - "Phase 1–4 gates 1–22 still pass (no regression)"
  artifacts:
    - path: "src/styles/tokens.css"
      provides: "`--sp-3: 12px` added (closes silent failure); `--terracotta` comment amended; `--lime` comment confirmed; optional `--fs-foot: 14px` added if 05-TOKEN-MAP.md flagged 3+ uses"
    - path: "all 8 sweep targets (Base, 404, [category], [slug], about, index, DisciplineCard)"
      provides: "Every raw font-size literal replaced with its mapped --fs-* token per 05-TOKEN-MAP.md; spacing literals optionally swept where mapped"
    - path: ".planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md"
      provides: "`## --terracotta Audit` table populated with per-occurrence verdicts; corresponding source edits applied where verdict was 'replace'"
  key_links:
    - from: "src/styles/tokens.css"
      to: "Gate 25 in verify-build.sh"
      via: "the grep gate locks zero raw font-size literals outside this file"
      pattern: "font-size:\\s*[0-9]+(\\.[0-9]+)?px"
    - from: ".planning/phases/05-mobile-performance-accessibility/05-TOKEN-MAP.md (from Plan 05-01)"
      to: "this plan's sweep"
      via: "each row in TOKEN-MAP dictates one source-line edit"
      pattern: "5-TOKEN-MAP.md"
---

<objective>
Wave 1 — close WARNING-1 from Phase 4 UI-REVIEW (token discipline drift). Sweep ~21 remaining raw `font-size: Npx` literals (Plan 05-04 already killed 6 in gallery deco drops) across 8 source files, register `--sp-3`, amend `--terracotta` and `--lime` comments, populate the `--terracotta` audit in `05-VERIFICATION.md`.

Purpose: SC6 — design-token hygiene. Once Gate 25 is green, no future commit can re-introduce a raw `font-size: Npx` outside `tokens.css` without breaking the build (Q7 resolution: friction is the point).

Output: One `tokens.css` edit (`--sp-3` + comment amendments), 7 source-file sweeps file-by-file per `05-TOKEN-MAP.md`, and one `05-VERIFICATION.md` update. Gate 25 turns green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mobile-performance-accessibility/05-CONTEXT.md
@.planning/phases/05-mobile-performance-accessibility/05-RESEARCH.md
@.planning/phases/05-mobile-performance-accessibility/05-UI-SPEC.md
@.planning/phases/05-mobile-performance-accessibility/05-TOKEN-MAP.md
@.planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md
@src/styles/tokens.css

<interfaces>
<!-- tokens.css current state (verified 05-RESEARCH §2.5): -->
<!--   --lime: #b8c945           (line 16, registered, with rationale comment — Phase 4 UAT added it) -->
<!--   --terracotta: #82785d     (registered, comment says "decorative accent only" — WRONG; UI-REVIEW found 16 load-bearing uses) -->
<!--   --sp-1..--sp-10           (scale: 4, 8, 16, 24, 32, 48, 64 — note --sp-3 MISSING; about.astro:98 consumes var(--sp-3) which currently resolves invalid → gap collapses to 0) -->
<!--   --fs-display, --fs-cat, --fs-q, --fs-card, --fs-h3, --fs-ttl, --fs-body, --fs-tile-role, --fs-mono, --fs-card-no, --fs-deco-numeral (11 roles per OVERRIDE-01) -->
<!--   The global @media (prefers-reduced-motion) *,*::before,*::after clamp at lines 63-70 is owned by Plan 05-06; do NOT touch it here -->

<!-- Sweep order (per 05-UI-SPEC §"Per-file sweep order", smallest to largest): -->
<!--   1. Base.astro       (1 literal: 14px in .foot .center) -->
<!--   2. 404.astro        (1 literal: 18px) -->
<!--   3. [category].astro (1 literal) -->
<!--   4. [slug].astro     (2 literals: 16px ×2 resume + pager-title) -->
<!--   5. DisciplineCard.astro (4 literals: 10px ×2, clamp(28..44px), clamp(22..30px)) -->
<!--   6. about.astro      (4 font literals — 0.92rem, 11px, 13px, 16px) -->
<!--   7. index.astro      (9 font literals — 11px ×4, 18px, 22px, 13px, 32px, 1.7em — biggest concentration) -->
<!--   8. Gallery* ×3      (240px/90px ALREADY REMOVED by Plan 05-04 deco drop — verify still removed) -->

<!-- 05-TOKEN-MAP.md (authored in Plan 05-01) is the per-row decision table. Each row says: file:line | literal | target token | reason. -->
<!-- This plan EXECUTES the table. It does NOT re-decide mappings (D-18: planner-owned). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Amend tokens.css — add --sp-3, fix --terracotta and --lime comments</name>
  <files>src/styles/tokens.css</files>
  <behavior>
    - `grep -F -- '--sp-3: 12px' src/styles/tokens.css` returns ≥1 match.
    - `grep -F 'decorative accent only' src/styles/tokens.css` returns 0 matches (comment dropped).
    - `--terracotta` comment matches the 05-UI-SPEC text "interactive accent — used for hover/focus/link feedback across about.astro + topbar nav-link hover" (or substantively equivalent).
    - `--lime` line still exists with a rationale comment describing it as the status-indicator pop, distinct from `--acid`.
    - If `05-TOKEN-MAP.md` §"Token additions to tokens.css" listed any other tokens (e.g. `--fs-foot: 14px`), those are added too — but only those.
    - Built site renders (about.astro photo-wireframes track now shows a 12px gap where it previously collapsed to 0).
  </behavior>
  <action>
    Open `src/styles/tokens.css`. Three surgical edits:

    1. **Add `--sp-3: 12px`** between the existing `--sp-2` and `--sp-4` declarations (the scale is currently 4/8/16/24/32/48/64; this closes the 8→16 gap with 12). Inline comment: `/* 12px — closes scale gap, consumed by about.astro:98 photo-track gap */`. Per 05-RESEARCH §5.3 this also fixes a silent bug.

    2. **Amend `--terracotta` comment.** Find the line `--terracotta: #82785d` and replace whatever comment currently sits next to it (likely "decorative accent only") with: `/* interactive accent — hover/focus/link feedback across about.astro + topbar nav-link hover (re-classified Phase 5, was 'decorative only' — UI-REVIEW counted 16 load-bearing uses) */`. Per D-17(b).

    3. **Verify/document `--lime` rationale.** Find `--lime: #b8c945`. Per 05-RESEARCH §2.5 it already has a rationale comment. If that comment exists and is clear, leave as-is. If it's missing or generic, add: `/* status-indicator pop (StatusPill dot + focus outline) — distinct from --acid which is the content-discipline accent for Personal */`. Per D-17(a) Q-resolution.

    4. **Apply any other token additions from `05-TOKEN-MAP.md` §"Token additions to tokens.css".** Plan 05-01 decided which off-scale literals (e.g. 14px footer) get new tokens vs. migrate. If TOKEN-MAP lists `--fs-foot: 14px` or `--fs-deco-xl: 240px` etc., add those here. If it lists none beyond `--sp-3`, that's the answer — don't invent tokens.

    Do NOT touch:
    - The global `@media (prefers-reduced-motion) *,*::before,*::after` block at lines 63-70 — Plan 05-06 owns it.
    - The `.visually-hidden` utility class added by Plan 05-03.
    - Any other existing token.

    After the edit, sanity-check: `npm run build` succeeds.
  </action>
  <verify>
    <automated>grep -F -- '--sp-3: 12px' src/styles/tokens.css &amp;&amp; ! grep -F 'decorative accent only' src/styles/tokens.css &amp;&amp; grep -F -- '--terracotta' src/styles/tokens.css &amp;&amp; grep -F -- '--lime' src/styles/tokens.css &amp;&amp; npm run build</automated>
  </verify>
  <done>
    `--sp-3` is registered, fixing the about.astro:98 silent failure. `--terracotta` is documented as load-bearing. `--lime` has a clear rationale. Any TOKEN-MAP-mandated tokens added. Build succeeds. Advances SC6 (token registry side).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sweep raw font-size literals (small files first: Base, 404, [category], [slug], DisciplineCard)</name>
  <files>src/layouts/Base.astro, src/pages/404.astro, src/pages/[category].astro, src/pages/[category]/[slug].astro, src/components/DisciplineCard.astro</files>
  <behavior>
    - After this task, running `grep -rnE 'font-size:\s*[0-9]+(\.[0-9]+)?px' src/layouts/ src/components/DisciplineCard.astro src/pages/404.astro src/pages/\[category\]*` returns ZERO matches. (Doesn't yet cover about.astro or index.astro — those land in Task 3. So Gate 25 isn't fully GREEN until Task 3.)
    - Each replaced literal corresponds to its `05-TOKEN-MAP.md` row.
    - Visual diff: pages should be near-identical at desktop (rounding errors ≤1px are acceptable per 05-UI-SPEC §"Font-size literal sweep" "accept visual rounding").
  </behavior>
  <action>
    Open `.planning/phases/05-mobile-performance-accessibility/05-TOKEN-MAP.md`. For each file below, edit ONLY the lines flagged in the TOKEN-MAP table. Do NOT touch any other line.

    **File 1: `src/layouts/Base.astro`** — 1 literal per 05-RESEARCH §2.5 inventory (`14px` in `.foot .center`). Replace with the token TOKEN-MAP specified. If TOKEN-MAP said "add --fs-foot: 14px" (already done in Task 1), use `var(--fs-foot)`. If it said "migrate to --fs-tile-role (accept 1px shrink to 13px)", use `var(--fs-tile-role)`. Honor TOKEN-MAP verbatim.

    **File 2: `src/pages/404.astro`** — 1 literal (`18px`). Honor TOKEN-MAP.

    **File 3: `src/pages/[category].astro`** — 1 literal. Honor TOKEN-MAP.

    **File 4: `src/pages/[category]/[slug].astro`** — 2 literals (`16px` ×2 on resume link + pager-title per 05-RESEARCH §2.5). Honor TOKEN-MAP (likely `--fs-body` which is 15.5px — 0.5px shrink acceptable). Critical: do NOT touch the `<Image>` priority/sizes work Plan 05-04 just landed on this file. Do NOT touch the pager hover rules (Plan 05-07).

    **File 5: `src/components/DisciplineCard.astro`** — 4 literals per inventory: `10px` ×2 (k-class card-name + card-no), `clamp(28px, ..., 44px)`, `clamp(22px, ..., 30px)`. For the two `10px` literals: TOKEN-MAP likely says `--fs-card-no` (9px — accept 1px shrink) or add a new `--fs-micro: 10px`. For the `clamp()` values: per 05-UI-SPEC mapping rule "Add a new font-size token only if the literal appears 3+ times site-wide" — both clamps are one-off; TOKEN-MAP likely says "migrate to closest existing scale" OR "add `--fs-card-major: clamp(...)`". Honor whichever it chose.

    After editing run `npm run build && bash scripts/verify-build.sh`. Gate 25 will still report FAIL (about.astro and index.astro literals remain) — that's expected at this checkpoint. All OTHER gates must remain GREEN.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/05-05-t2.log &amp;&amp; ! grep -F 'FAIL: Gate 19' /tmp/05-05-t2.log &amp;&amp; ! grep -F 'FAIL: Gate 22' /tmp/05-05-t2.log &amp;&amp; test "$(grep -rnE 'font-size:\s*[0-9]+(\.[0-9]+)?px' src/layouts/ src/components/DisciplineCard.astro src/pages/404.astro src/pages/\[category\]*.astro src/pages/\[category\]/ 2>/dev/null | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>
    The 5 small/medium files are clean (zero raw `font-size: Npx` literals). All Phase 1–4 gates remain GREEN. Gate 25 remains FAIL pending Task 3. Advances SC6 (sweep half).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sweep about.astro + index.astro (biggest concentrations) + populate --terracotta audit + close Gate 25</name>
  <files>src/pages/about.astro, src/pages/index.astro, .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md</files>
  <behavior>
    - `grep -rnE 'font-size:\s*[0-9]+(\.[0-9]+)?px' src/components/ src/pages/ src/layouts/` returns ZERO matches.
    - `bash scripts/verify-build.sh` reports `OK: Gate 25`.
    - `05-VERIFICATION.md` `## --terracotta Audit` table has every row's Verdict filled in (load-bearing / replace). Where Verdict was 'replace', the source edit is also applied within this task.
    - All Phase 1–4 gates still pass.
  </behavior>
  <action>
    **Part A: about.astro sweep.** Per 05-RESEARCH §2.5: 4 font literals (`0.92rem`, `11px`, `13px`, `16px`). `0.92rem` and `1.7em` per 05-UI-SPEC mapping rule "Relative units are legitimate" — TOKEN-MAP likely says keep with an inline comment. The three px literals migrate per TOKEN-MAP rows. Do NOT touch any spacing literals here unless TOKEN-MAP lists them (D-17(c) gate-25 target is font-size only; the spacing sweep is opportunistic). Do NOT touch the slow-scroll script.

    **Part B: index.astro sweep.** Per 05-RESEARCH §2.5: 9 font literals (`11px` ×4, `18px`, `22px`, `13px`, `32px`, `1.7em`). Honor TOKEN-MAP for each. Critical: do NOT touch the `<Image priority>` work Plan 05-04 landed. Do NOT touch carousel scripts. Do NOT touch hover rules (Plan 05-07's territory). Do NOT touch entrance shake keyframes (Plan 05-06's territory).

    Per 05-UI-SPEC mapping rule, `1.7em` (b-bio-arrow) and `0.92rem` (about values-pill) stay as relative units — keep with an inline `/* intentional: relative to parent line-height */` comment if TOKEN-MAP didn't already say something specific. The grep gate only matches `Npx` patterns, so `em`/`rem` literals don't trigger Gate 25 — they're allowed.

    **Part C: populate `--terracotta` audit in `05-VERIFICATION.md`.** Open `05-VERIFICATION.md` `## --terracotta Audit` section (created by Plan 05-01 with the 16 pre-seeded rows). For each row, fill the Verdict column with either:
    - `load-bearing` — the use is functional (hover state, focus ring, scrollbar thumb, dashed border). No source edit; the use stays.
    - `replace` — the use is decorative-only and there's a better token (e.g. `--ink` for a focus ring, `--accent` for a hover state). If you mark a row 'replace', ALSO edit the source line to use the chosen replacement token.

    Per 05-UI-SPEC: most of the 16 are load-bearing per D-17(b) re-classification. Only flag 'replace' where the use is genuinely incidental.

    **Part D: tick the audit checkbox.** In `05-VERIFICATION.md` `## Phase Exit Sign-Off`, tick `[x] --terracotta audit complete`. Do NOT tick the other Phase Exit checkboxes — those belong to Plan 05-08.

    After all edits run `npm run build && bash scripts/verify-build.sh`. Gate 25 MUST be GREEN. Every other Gate MUST be GREEN. If Gate 22 (pager parity) regresses, your index.astro edits broke role-link or a pager href — fix.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/05-05-t3.log &amp;&amp; grep -F 'OK: Gate 25' /tmp/05-05-t3.log &amp;&amp; ! grep -F 'FAIL' /tmp/05-05-t3.log &amp;&amp; grep -F '\[x\] --terracotta audit complete' .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md</automated>
  </verify>
  <done>
    Zero raw font-size px literals in source. Gate 25 GREEN. `--terracotta` audit recorded with verdicts. Phase 4 WARNING-1 closed; the carry-over note in ROADMAP §Phase 5 SC6 can be re-classified as "closed by Plan 05-05." Advances SC6.
  </done>
</task>

</tasks>

<verification>
- `bash scripts/verify-build.sh` — Gate 25 OK; all other gates unchanged
- `grep -rnE 'font-size:\s*[0-9]+(\.[0-9]+)?px' src/components/ src/pages/ src/layouts/` returns zero matches
- `05-VERIFICATION.md --terracotta` table fully filled
- Visual smoke at desktop on `/` and `/about`: pages look identical (±1px on swept literals) to pre-sweep
- `var(--sp-3)` resolves to 12px (verify in browser devtools on `.about .photos-track`)
</verification>

<success_criteria>
SC6 is closed. Token registry is hygienic: `--sp-3` registered, `--terracotta` documented accurately, `--lime` documented with rationale, zero raw font-size px literals outside tokens.css. Gate 25 locks the contract against regression — any future commit that re-introduces a raw `font-size: Npx` outside tokens.css will break the build. Per Q7 resolution: the friction is the point.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-performance-accessibility/05-05-SUMMARY.md`. Note explicitly:
- Total literals swept: original ~21 + Plan 05-04's 6 deco drops = ~27 (matches 05-RESEARCH §2.5 inventory)
- `--terracotta` audit verdict distribution (X load-bearing / Y replace)
- Any new tokens registered beyond `--sp-3` (if TOKEN-MAP demanded any)
- Confirmation that `var(--sp-3)` silent failure is closed
</output>
