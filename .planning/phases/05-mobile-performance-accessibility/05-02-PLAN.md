---
phase: 5
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md
autonomous: false
requirements:
  - FOUND-02
user_setup:
  - service: vercel
    why: "SC2 (Lighthouse Perf ≥85, A11y ≥95, LCP <2s) must be measured against a real deployed preview URL per D-13 (amended 2026-05-18). GitHub repo `C-lb/caleb-lim-portfolio` is already pushed; only the Vercel project import remains."
    env_vars: []
    dashboard_config:
      - task: "Import the C-lb/caleb-lim-portfolio repo into Vercel via vercel.com/new"
        location: "https://vercel.com/new — select GitHub → C-lb/caleb-lim-portfolio → accept the auto-detected Astro framework preset"
      - task: "Verify preview URL deploys on push (branch alias `caleb-lim-portfolio-git-phase-5-c-lb.vercel.app`)"
        location: "Vercel Dashboard → Project → Deployments"
must_haves:
  truths:
    - "Vercel project for repo `C-lb/caleb-lim-portfolio` exists, framework preset is Astro, production branch is `main`"
    - "A push to a non-main branch produces a Vercel preview URL of the shape `caleb-lim-portfolio-git-<branch>-c-lb.vercel.app` within ~60s"
    - "Hitting that preview URL returns the splash HTML (200, not 401/404/`needs login`)"
    - "Real iPhone Safari can also reach that preview URL over cellular"
  artifacts:
    - path: ".planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md"
      provides: "Records the production URL, the phase-5 branch alias, and the first-deploy verification (this fills the `Lighthouse Scores` table's URL header row + a 'Vercel import verified' line in Phase Exit Sign-Off)"
  key_links:
    - from: "GitHub C-lb/caleb-lim-portfolio main branch"
      to: "Vercel production deployment"
      via: "Vercel GitHub integration auto-deploy"
      pattern: "push to main → caleb-lim-portfolio.vercel.app"
    - from: "GitHub feature branches (e.g. `phase-5`)"
      to: "Vercel preview deployment"
      via: "Vercel GitHub integration auto-deploy on branch push"
      pattern: "push to <branch> → caleb-lim-portfolio-git-<branch>-c-lb.vercel.app"
---

<objective>
Wave 1 — bootstrap the Vercel preview pipeline. Per 05-RESEARCH §1 finding #2 this is the cheapest unblocked task and it gates SC2 measurement (Lighthouse cannot run against a URL that doesn't exist). GitHub repo `C-lb/caleb-lim-portfolio` is already pushed (05-CONTEXT amendment 2026-05-18); the work that remains is the Vercel-side import + a first-push smoke test.

Purpose: SC2 instrumentation. After this plan, every later Wave-1 / Wave-2 commit auto-deploys a preview URL Caleb can paste into `scripts/lighthouse-audit.sh`.

Output: Vercel project exists; production + preview URLs verified live; record in `05-VERIFICATION.md`. **No source file changes** — this plan is dashboard-only + a single record-keeping edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-mobile-performance-accessibility/05-CONTEXT.md
@.planning/phases/05-mobile-performance-accessibility/05-RESEARCH.md

<interfaces>
<!-- Vercel preview URL pattern (verified at 05-CONTEXT.md code_context + 05-RESEARCH §4.3): -->
<!--   Production:  caleb-lim-portfolio.vercel.app -->
<!--   Branch alias: caleb-lim-portfolio-git-<branch>-c-lb.vercel.app -->
<!--   Per-deploy:   caleb-lim-portfolio-<commit-hash>-c-lb.vercel.app -->
<!-- Lighthouse audits target the branch alias (D-13). -->

<!-- Astro auto-detection facts Vercel applies (verified at 05-RESEARCH §4.2): -->
<!--   Framework preset: Astro -->
<!--   Build command: npm run build -->
<!--   Output dir: dist -->
<!--   Node version: 22 (set NODE_VERSION=22 env var if first build complains) -->
<!--   No vercel.json required for default Astro static build. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User imports the GitHub repo into Vercel</name>
  <what-built>Nothing automated — this is a one-time dashboard action Claude literally cannot do (Vercel requires Caleb to authorize the GitHub app on his own account).</what-built>
  <how-to-verify>
    Caleb walks the import flow per 05-RESEARCH §4.2:

    1. Open https://vercel.com/new (sign in with the same GitHub account that owns `C-lb`).
    2. Click **Continue with GitHub** → authorize the Vercel app on the `C-lb/caleb-lim-portfolio` repo. Scope it to that single repo if prompted.
    3. Vercel scans the repo; the project name pre-fills as `caleb-lim-portfolio`. **Accept the default** — changing it later breaks URLs.
    4. Confirm the auto-detected fields: Framework Preset = **Astro**, Build Command = `npm run build`, Output Directory = `dist`. Production Branch = `main`.
    5. Skip environment variables (none required).
    6. Click **Deploy**.
    7. Wait for the build to reach **Ready** (~30–60s).
    8. Open the production URL → confirm splash renders. If the splash 401s with "needs login," disable Deployment Protection in **Project Settings → Deployment Protection → Vercel Authentication = Standard Protection → Disabled** (this site is public).
    9. Record results: production URL, first-deploy commit hash, build duration. Add these to `05-VERIFICATION.md` under a new section `## Vercel Project Bootstrap` (Claude will edit this file in Task 3 — you just need to paste the values).

    **Per D-13:** Caleb is the only one who can do steps 1–8. Claude cannot log in to Vercel.
  </how-to-verify>
  <resume-signal>Type "vercel-imported" + paste the production URL Vercel emitted (e.g. `caleb-lim-portfolio.vercel.app`), OR describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Push the phase-5 branch and verify the preview alias deploys</name>
  <files>(none — git + curl only)</files>
  <action>
    Smoke-test the Vercel preview pipeline. Run these commands from the project root `/Users/caleb/projects/personal-website`:

    1. `git status` — confirm the working tree is clean (or that any inflight changes are staged onto the current branch).
    2. `git branch --show-current` — if not on `phase-5`, run `git checkout -b phase-5` (or `git checkout phase-5` if it exists).
    3. `git commit --allow-empty -m "test(phase-5): trigger vercel preview deploy"` — empty commit; this is the trigger.
    4. `git push -u origin phase-5` (or just `git push` if already tracking).
    5. Wait 30–90 seconds. Then `curl -sI https://caleb-lim-portfolio-git-phase-5-c-lb.vercel.app | head -3`. The first line MUST be `HTTP/2 200`. If it's `401`, deployment protection is still on — surface this to the user as a checkpoint (re-route to Task 1 step 8).
    6. If 200, run `curl -s https://caleb-lim-portfolio-git-phase-5-c-lb.vercel.app | grep -c "CALEB LIM"`. The result MUST be ≥1 — proves the splash HTML is what Vercel served, not a placeholder.
    7. Print the verified preview URL (full URL string) to stdout so Task 3 can pick it up.

    Per 05-RESEARCH §5.4: Vercel's edge propagation is near-instant once the deployment is Ready. If `curl` fails immediately after the push, wait up to 90 seconds and retry — Vercel's "Building → Deploying → Ready" pipeline takes that long. If 90s isn't enough, surface it as a Task-1 retry checkpoint.

    Do NOT add `vercel.json`. Do NOT modify `astro.config.mjs`. Do NOT add a Vercel SDK or the `@vercel/astro` adapter — this is a default static build per 05-RESEARCH §4.2 step 6.
  </action>
  <verify>
    <automated>curl -sI "https://caleb-lim-portfolio-git-phase-5-c-lb.vercel.app" | head -1 | grep -F '200' &amp;&amp; curl -s "https://caleb-lim-portfolio-git-phase-5-c-lb.vercel.app" | grep -F 'CALEB LIM' | head -1</automated>
  </verify>
  <done>
    `phase-5` branch is pushed. Preview URL `caleb-lim-portfolio-git-phase-5-c-lb.vercel.app` returns HTTP 200 and serves splash HTML containing the "CALEB LIM" wordmark. Production URL is unchanged from Task 1 (no commit pushed to `main`). Advances SC2 (preview pipeline live).
  </done>
</task>

<task type="auto">
  <name>Task 3: Record Vercel bootstrap in 05-VERIFICATION.md</name>
  <files>.planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md</files>
  <action>
    Open `05-VERIFICATION.md` (created in Plan 05-01 Task 3). Add a new section `## Vercel Project Bootstrap (SC2, D-13 amended)` between the `## Real-Device Test Rig` section and the `## Critical-Path Walk` section. Populate it with verbatim values from Tasks 1 + 2:

    - Production URL (from Task 1 step 9 user input)
    - Preview branch alias for `phase-5` (verified live in Task 2)
    - First-deploy commit hash (from Task 1 step 9)
    - First-deploy build duration (from Task 1 step 9)
    - Date verified (`2026-05-18` or the actual run date)

    Then update the `## Lighthouse Scores` table header note from "URL: TBD" to "URL: <preview-alias-from-Task-2>" — this is the URL Plan 05-08 will pass to `scripts/lighthouse-audit.sh`.

    Then tick `[x] Vercel import verified` in the `## Phase Exit Sign-Off` section (add this line if Plan 05-01 didn't include it).

    Do NOT fill in any other section. Do NOT fabricate Lighthouse scores. Real scores get recorded by Plan 05-08 after all Wave-1 + Wave-2 changes land.
  </action>
  <verify>
    <automated>grep -F 'Vercel Project Bootstrap' .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md &amp;&amp; grep -F 'caleb-lim-portfolio-git-phase-5-c-lb.vercel.app' .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md</automated>
  </verify>
  <done>
    `05-VERIFICATION.md` now records the Vercel project state. SC2 is unblocked: every subsequent push to `phase-5` produces a preview Lighthouse can audit. Advances SC2.
  </done>
</task>

</tasks>

<verification>
- `curl -sI https://caleb-lim-portfolio.vercel.app | head -1` returns 200 (production live)
- `curl -sI https://caleb-lim-portfolio-git-phase-5-c-lb.vercel.app | head -1` returns 200 (preview live)
- `.planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md` contains the `## Vercel Project Bootstrap` section with both URLs
- No file in `/Users/caleb/projects/personal-website` outside `.planning/` was modified by this plan (no `vercel.json`, no adapter changes)
</verification>

<success_criteria>
The Lighthouse + real-device pipeline can run. After this plan, any later push to `phase-5` redeploys the preview within ~60s; Plan 05-08 only needs to call `bash scripts/lighthouse-audit.sh https://caleb-lim-portfolio-git-phase-5-c-lb.vercel.app` to produce SC2 evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-performance-accessibility/05-02-SUMMARY.md` per the standard summary template. Note explicitly: no source files were modified; this plan was 100% infrastructure setup. Record the production URL + preview URL + first-deploy hash for future agents to reference.
</output>
