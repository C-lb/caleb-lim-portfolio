---
phase: 5
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/lighthouse-audit.sh
  - scripts/verify-build.sh
  - .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md
  - .planning/phases/05-mobile-performance-accessibility/05-TOKEN-MAP.md
  - .gitignore
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
must_haves:
  truths:
    - "`scripts/lighthouse-audit.sh` runs against a Vercel preview URL argument and emits Perf/A11y/LCP HTML+full-JSON per route, PLUS a per-route durable `<slug>-summary.json` containing only the three SC2 metrics (perf/a11y/lcp_ms). Allow-list gitignore commits the summary files; raw reports stay local."
    - "`scripts/verify-build.sh` exits non-zero when (a) the ≤700px topbar collapse is missing from built HTML (Gate 23), (b) a populated gallery emits zero `<img>` (Gate 24), (c) any `font-size: Npx` literal appears outside `tokens.css` (Gate 25)"
    - "`05-VERIFICATION.md` template exists with empty sections every downstream Wave-2 plan can fill"
    - "`05-TOKEN-MAP.md` table exists, listing every raw `font-size`/spacing literal in the codebase with its target token decision"
  artifacts:
    - path: "scripts/lighthouse-audit.sh"
      provides: "Manual Lighthouse mobile audit batch runner against a Vercel preview URL (D-13, D-15, D-16)"
    - path: "scripts/verify-build.sh"
      provides: "Adds Gate 23 (topbar ≤700px), Gate 24 (gallery <img> count), Gate 25 (zero raw font-size literals)"
    - path: ".planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md"
      provides: "Manual-verification template (real-iPhone walk, Lighthouse table, reduced-motion walk, --terracotta audit, iPhone model/iOS record per D-14)"
    - path: ".planning/phases/05-mobile-performance-accessibility/05-TOKEN-MAP.md"
      provides: "Literal→token mapping table that drives Plan 05-05's sweep (planner-owned per D-18)"
  key_links:
    - from: "scripts/lighthouse-audit.sh"
      to: "Vercel preview URL"
      via: "first positional arg, default `https://caleb-lim-portfolio-git-phase-5-c-lb.vercel.app` per 05-RESEARCH §4.4"
      pattern: 'npx lighthouse "\$PREVIEW_URL\$route"'
    - from: "scripts/verify-build.sh Gates 23/24/25"
      to: "Plans 05-03 / 05-04 / 05-05 (Wave 1)"
      via: "smoke gates Wave-1 plans must turn green"
      pattern: "Gate 2[345]"
---

<objective>
Wave 0 — build the validation harness every Wave-1 and Wave-2 task verifies against.

Purpose: Nyquist compliance — no downstream task can have `<automated>` checks until the harness exists. Today there is no `lighthouse-audit.sh`, no Gate 23/24/25 in `verify-build.sh`, no `05-VERIFICATION.md` stub, no `05-TOKEN-MAP.md`. All five must land before any Wave-1 plan can claim done.

Output: One new shell script, three new gates appended to the existing `scripts/verify-build.sh` (which already runs Gates 1–22), one manual-verification template stub, one token-map artifact. `.gitignore` updated to exclude `lighthouse-reports/` (matches the harness output path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-performance-accessibility/05-CONTEXT.md
@.planning/phases/05-mobile-performance-accessibility/05-RESEARCH.md
@.planning/phases/05-mobile-performance-accessibility/05-VALIDATION.md
@.planning/phases/05-mobile-performance-accessibility/05-UI-SPEC.md
@scripts/verify-build.sh

<interfaces>
<!-- verify-build.sh gate convention (from existing Gates 1-22): each gate prints "OK: ..." or "FAIL: ..." and increments a fail counter; the script exits non-zero if any gate failed. Pattern verified at scripts/verify-build.sh:21-854. -->
<!-- The script reads built HTML from dist/ (so `npm run build` must run first). Per 05-RESEARCH §6.4 the new gates follow the same pattern. -->
<!-- Lighthouse CLI is invoked via `npx lighthouse` — already available without adding a dep (npm fetches on demand). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/lighthouse-audit.sh + .gitignore update</name>
  <files>scripts/lighthouse-audit.sh, .gitignore</files>
  <action>
    Create `scripts/lighthouse-audit.sh` per 05-RESEARCH §4.4. The script: (1) takes `$1` as preview URL with a default of `https://caleb-lim-portfolio-git-phase-5-c-lb.vercel.app` (D-13 amended preview URL pattern); (2) iterates over five routes `/`, `/design`, `/marketing`, `/about`, `/design/<one-slug>` — discover the slug by listing `src/content/pieces/design-*` at runtime, pick the first non-draft `.md` filename, strip extension; (3) runs `npx lighthouse "$PREVIEW_URL$route" --form-factor=mobile --throttling-method=simulate --output=html,json --output-path="$OUT_DIR/$slug" --chrome-flags="--headless=new"`; (4) writes outputs to `.planning/phases/05-mobile-performance-accessibility/lighthouse/<slug>.{html,json}` — per 05-VALIDATION.md the file outputs land inside the phase dir for Wave-2 evidence; (5) after the loop, parses each JSON with `node` (already in repo) to extract `categories.performance.score`, `categories.accessibility.score`, and `audits["largest-contentful-paint"].numericValue`, prints a table-shaped summary, and exits non-zero if splash perf <0.85 OR splash a11y <0.95 OR splash LCP ≥2000ms; (6) **also emits a per-route durable evidence file `$OUT_DIR/$slug-summary.json` containing ONLY the three SC2 metrics** — shape: `{ "route": "/design", "perf": 92, "a11y": 100, "lcp_ms": 1480, "recorded_at": "<ISO timestamp>" }`. These small JSON files are the durable SC2 evidence (committed); the full HTML + raw JSON reports stay gitignored. Use `set -euo pipefail` like existing scripts. Make executable (`chmod +x`). Update `.gitignore` with an ALLOW-LIST pattern: ignore the full reports but explicitly UN-ignore the per-route summaries. Recipe (verbatim):
```
# Phase 5: gitignore raw Lighthouse reports, but commit per-route summary.json (SC2 durable evidence per 05-08 Task 1 step 7)
/.planning/phases/05-mobile-performance-accessibility/lighthouse/*.html
/.planning/phases/05-mobile-performance-accessibility/lighthouse/*.json
!/.planning/phases/05-mobile-performance-accessibility/lighthouse/*-summary.json
```
The negation pattern un-ignores files matching `*-summary.json` so `splash-summary.json`, `design-summary.json`, etc. ARE tracked. Verify with `git check-ignore -v .planning/phases/05-mobile-performance-accessibility/lighthouse/splash.json` (should be ignored) and `git check-ignore -v .planning/phases/05-mobile-performance-accessibility/lighthouse/splash-summary.json` (should NOT be ignored — exit code 1). Per D-16 manual-only — do NOT wire this into `verify-build.sh` or CI. Per scope-creep guard in 05-RESEARCH §6.1: do NOT add Vitest/Jest/Playwright. This is a Bash + node-CLI script only.
  </action>
  <verify>
    <automated>bash -n scripts/lighthouse-audit.sh &amp;&amp; test -x scripts/lighthouse-audit.sh &amp;&amp; grep -F 'npx lighthouse' scripts/lighthouse-audit.sh &amp;&amp; grep -F 'form-factor=mobile' scripts/lighthouse-audit.sh &amp;&amp; grep -F 'summary.json' scripts/lighthouse-audit.sh &amp;&amp; grep -F '05-mobile-performance-accessibility/lighthouse' .gitignore &amp;&amp; grep -F '!/.planning/phases/05-mobile-performance-accessibility/lighthouse/*-summary.json' .gitignore</automated>
  </verify>
  <done>
    `scripts/lighthouse-audit.sh` exists, is `chmod +x`, syntax-clean (`bash -n`), iterates over 5 routes, writes JSON+HTML into `.planning/phases/05-mobile-performance-accessibility/lighthouse/`, parses JSON for Perf/A11y/LCP, also emits per-route `<slug>-summary.json` (perf/a11y/lcp_ms only), exits non-zero on splash threshold miss. `.gitignore` excludes raw HTML+JSON but allow-lists `*-summary.json` (durable SC2 evidence). No new package.json dependency added. Advances SC2 (instrumentation).
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Gates 23, 24, 25 to scripts/verify-build.sh</name>
  <files>scripts/verify-build.sh</files>
  <action>
    Append three new gates to the end of `scripts/verify-build.sh` (after Gate 22 at line ~843, before the final fail-counter exit). Match the existing gate idiom verbatim (header comment `# Gate NN: <desc>`, `OK:`/`FAIL:` echoes, increment the same fail counter Gates 1–22 use — inspect lines 21–854 to confirm the variable name). The gates:

    - **Gate 23 — topbar ≤700px collapse present (SC1, D-01/D-02/D-03):** After `npm run build` populates `dist/`, grep `dist/index.html` AND `dist/design/index.html` AND `dist/about/index.html` for the literal string `@media (max-width: 700px)`. Astro inlines scoped CSS into each page, so each of those built HTMLs MUST contain the breakpoint. Additionally grep for `aria-label="Email Caleb"` OR `aria-label="Caleb on LinkedIn"` OR `aria-label="Download Caleb's resume"` to confirm the icon-row aria contract per 05-UI-SPEC §"Accessible naming for icon glyphs" is wired. FAIL if either check misses on any of the three pages.

    - **Gate 24 — populated gallery emits `<img>` (SC5, D-09–D-12):** On `dist/design/index.html`, count `<img` occurrences. Compute the expected piece count by listing non-draft Markdown files under `src/content/pieces/design-*.md` (mirror Gate 4's piece-count logic at scripts/verify-build.sh:54). The count of `<img` MUST be ≥ piece-count. Repeat for `dist/marketing/index.html` against `src/content/pieces/marketing-*.md`. FAIL otherwise.

    - **Gate 25 — zero raw `font-size: Npx` outside tokens.css (SC6, D-17(c)):** Run `grep -rnE 'font-size:\s*[0-9]+(\.[0-9]+)?px' src/components/ src/pages/ src/layouts/`. The exit code MUST be 1 (no matches). On exit code 0, print each offending file:line and FAIL. The grep deliberately excludes `src/styles/tokens.css` which is allowed to declare px font-sizes. Per Q7 resolution in 05-UI-SPEC the friction is the point — this gate locks regression. **Add an inline comment in the gate block** explaining the px-only scope vs 05-TOKEN-MAP.md's px/rem/em scope: `# Gate 25: px font-size literals only. rem/em allowed when relative-to-line-height is intentional (e.g. .92rem on about values-pill, 1.7em on bio-arrow). See 05-UI-SPEC §Typography "Relative units are legitimate." Do NOT widen this regex to rem/em without a UI-SPEC amendment first — the asymmetry vs 05-TOKEN-MAP.md is deliberate.`

    Do NOT touch Gates 1–22. Do NOT bump version comments at the top of the file unless one exists for the gate count. After editing, run `bash -n scripts/verify-build.sh` for syntax. Gate 23 and Gate 24 will be RED until Plans 05-03 / 05-04 land; Gate 25 will be RED until Plan 05-05 sweeps literals — that is correct and expected (matches Plan 04-03's Gate 19e "RED until plan lands" precedent at scripts/verify-build.sh:617). Surface "expected RED" comments inline so a future reader doesn't panic.
  </action>
  <verify>
    <automated>bash -n scripts/verify-build.sh &amp;&amp; grep -F '# Gate 23' scripts/verify-build.sh &amp;&amp; grep -F '# Gate 24' scripts/verify-build.sh &amp;&amp; grep -F '# Gate 25' scripts/verify-build.sh &amp;&amp; grep -F '@media (max-width: 700px)' scripts/verify-build.sh &amp;&amp; grep -E "grep -rnE 'font-size" scripts/verify-build.sh</automated>
  </verify>
  <done>
    Three new gates appended after Gate 22. Script remains syntax-clean. Gates 23–25 are expected to be RED at this commit (Plans 05-03/05-04/05-05 will turn them GREEN). No regression to Gates 1–22 (sanity: `npm run build &amp;&amp; bash scripts/verify-build.sh` still reports Gates 1–22 OK; new gates report FAIL with informative messages). Advances SC1 / SC5 / SC6 (instrumentation).
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 05-VERIFICATION.md template + 05-TOKEN-MAP.md skeleton</name>
  <files>.planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md, .planning/phases/05-mobile-performance-accessibility/05-TOKEN-MAP.md</files>
  <action>
    **`05-VERIFICATION.md`** — write a template stub (NOT a filled report). Sections, in order:

    1. `# Phase 5 — Verification Record` + frontmatter (`phase: 5`, `status: pending`, `recorded_at: TBD`)
    2. `## Real-Device Test Rig (D-14)` — placeholder fields: iPhone model: `TBD`, iOS version: `TBD`, network: `hotel-wifi simulation (home wifi off, cellular only)`, test date: `TBD`
    3. `## Critical-Path Walk (SC1, SC4)` — checklist mirroring 05-UI-SPEC §"Critical-path accessibility walk" steps 1–10 (each unchecked checkbox + a blank result/screenshot column)
    4. `## Lighthouse Scores (SC2, D-13/D-15)` — empty table with columns `Route | Perf | A11y | LCP (ms) | Pass?` and rows for `/`, `/design`, `/marketing`, `/about`, `/design/<slug>`. Note the source: `bash scripts/lighthouse-audit.sh <preview-url>` — JSON pull-quotes go below the table per route.
    5. `## Reduced-Motion Walk (SC3, D-08)` — checklist of the 9 verification steps from 05-UI-SPEC §"Verification walk (recorded in 05-VERIFICATION.md)"
    6. `## --terracotta Audit (SC6, D-17(b))` — table with columns `File | Line | Use | Verdict (load-bearing / replace)`. Pre-populated with the 16 occurrences flagged in 04-UI-REVIEW Pillar 3 — grep `src/` for `--terracotta` to seed rows. Each row's Verdict starts blank; Plan 05-05 fills it.
    7. `## Phase Exit Sign-Off` — final checklist: `[ ] All verify-build.sh gates green`, `[ ] All Lighthouse thresholds met`, `[ ] Real-device walk recorded`, `[ ] Reduced-motion walk recorded`, `[ ] --terracotta audit complete`.

    **`05-TOKEN-MAP.md`** — a planning artifact that Plan 05-05 consumes. Per 05-RESEARCH §3.6 step 2 + Q4 resolution in 05-UI-SPEC. Write the file with:

    1. `# Phase 5 — Token Migration Map` + intent: "every raw `font-size: Npx` and raw spacing literal in `src/components/`, `src/pages/`, `src/layouts/` mapped to its target token per D-17/D-18 and the rule-of-thumb '3+ uses → new token; else nearest existing scale.'"
    2. `## Source inventory` — run `grep -rnE 'font-size:\s*[0-9]+(\.[0-9]+)?(px|rem|em)' src/components/ src/pages/ src/layouts/` and paste verbatim into a fenced block (this is the ground truth Plan 05-05 sweeps against — embedding it locks the contract; if a literal isn't in this list, Plan 05-05 isn't responsible for it).
    3. `## Font-size mapping` — table with columns `File:line | Raw literal | Target token | Reason`. Pre-fill ALL rows from the §2.5 27-literal inventory in 05-RESEARCH.md. Apply the mapping rules from 05-UI-SPEC §"Font-size literal sweep" verbatim. For each off-scale literal (`14px`, `16px`, `18px`, `32px`), pick "migrate to nearest" OR "add new token" based on the 3+-uses rule — count occurrences across the source inventory first.
    4. `## Spacing mapping` — same shape, sourced from 05-UI-SPEC §"Spacing literal sweep" table. Pre-fill with `--sp-3: 12px (NEW)` for the `var(--sp-3)` consumer at about.astro:98 (silent-failure bug per 05-RESEARCH §5.3).
    5. `## Token additions to tokens.css` — explicit list of new tokens Plan 05-05 must register. Minimum: `--sp-3: 12px`. Add `--fs-foot: 14px` or `--fs-deco-xl: 240px` only if the 3+-uses count justifies it.

    Both files use the project's existing markdown conventions (frontmatter blocks, table headers, fenced code blocks). Token-map decisions are planner-owned per D-18 — don't let Plan 05-05 re-decide them.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md &amp;&amp; test -f .planning/phases/05-mobile-performance-accessibility/05-TOKEN-MAP.md &amp;&amp; grep -F 'Real-Device Test Rig' .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md &amp;&amp; grep -F 'Reduced-Motion Walk' .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md &amp;&amp; grep -F '--terracotta' .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md &amp;&amp; grep -F '--sp-3' .planning/phases/05-mobile-performance-accessibility/05-TOKEN-MAP.md &amp;&amp; grep -Ec '\| [a-zA-Z0-9_./-]+:[0-9]+' .planning/phases/05-mobile-performance-accessibility/05-TOKEN-MAP.md | awk '$1>=10'</automated>
  </verify>
  <done>
    `05-VERIFICATION.md` exists as a template (frontmatter `status: pending`), with all 7 sections empty/checklist-only — no fabricated test results. `05-TOKEN-MAP.md` exists, embeds the raw grep inventory, and has ≥10 font-size and ≥10 spacing rows pre-decided per D-18. Both files are planner artifacts, not executor artifacts — they exist to drive Wave-2 (05-08) and Wave-1 (05-05) respectively. Advances SC1/SC2/SC3/SC6 (instrumentation).
  </done>
</task>

</tasks>

<verification>
- `bash -n scripts/lighthouse-audit.sh` exits 0
- `bash -n scripts/verify-build.sh` exits 0
- `npm run build && bash scripts/verify-build.sh` reports Gates 1–22 OK; Gates 23/24/25 report FAIL (expected; the harness exists but the implementations haven't landed yet)
- `test -x scripts/lighthouse-audit.sh`
- Both planning artifacts (`05-VERIFICATION.md`, `05-TOKEN-MAP.md`) exist and conform to the structure above
</verification>

<success_criteria>
The Wave-0 harness is in place: a Wave-1 task can grep for `Gate 23 OK` after wiring the topbar collapse, can grep for `Gate 24 OK` after wiring gallery images, can grep for `Gate 25 OK` after sweeping literals. Wave-2 verification has a template to fill. Plan 05-05 has a decision table to sweep against. No new framework introduced. No regression to Phase 1–4 gates.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-performance-accessibility/05-01-SUMMARY.md` per the standard summary template, noting:
- Wave-0 harness ready; Gates 23/24/25 expected RED until Wave-1 lands
- `05-TOKEN-MAP.md` is the canonical literal→token mapping; Plan 05-05 MUST consume it verbatim
- `05-VERIFICATION.md` is the canonical Wave-2 record; Plan 05-08 MUST fill it in-place (not overwrite)
</output>
