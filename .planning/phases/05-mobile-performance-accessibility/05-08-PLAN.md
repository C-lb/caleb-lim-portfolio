---
phase: 5
plan: 08
type: execute
wave: 2
depends_on: [02, 03, 04, 05, 06, 07]
files_modified:
  - .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md
  - .planning/phases/05-mobile-performance-accessibility/lighthouse/
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
must_haves:
  truths:
    - "`scripts/lighthouse-audit.sh https://caleb-lim-portfolio-git-phase-5-c-lb.vercel.app` was run successfully; full HTML+JSON reports landed in `.planning/phases/05-mobile-performance-accessibility/lighthouse/` (gitignored) and the 5 per-route `*-summary.json` evidence files were committed (allow-listed)"
    - "All five routes in the Lighthouse table meet thresholds: Perf ≥85 on splash/gallery/detail/about; A11y ≥95 on all routes; LCP <2000ms on splash"
    - "Real iPhone Safari critical-path walk (10 steps from 05-UI-SPEC §Critical-path accessibility walk) is recorded in `05-VERIFICATION.md` with iPhone model + iOS version (D-14)"
    - "Reduced-motion walk (9 steps from 05-UI-SPEC §Verification walk) is recorded — exempt motions confirmed firing; disabled motions confirmed suppressed"
    - "`05-VERIFICATION.md` Phase Exit Sign-Off section has every checkbox ticked"
    - "Phase 1–5 gates 1–25 all GREEN at the final commit; this plan does NOT modify source — only records"
  artifacts:
    - path: ".planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md"
      provides: "Final filled-in verification record — Lighthouse scores table, real-device walk results, reduced-motion walk results, --terracotta audit verdicts (filled by Plan 05-05), Phase Exit Sign-Off all ticked"
    - path: ".planning/phases/05-mobile-performance-accessibility/lighthouse/*-summary.json"
      provides: "Per-route durable SC2 evidence (perf/a11y/lcp_ms) — allow-listed in .gitignore (Plan 05-01) and COMMITTED. Raw .html + full .json reports remain gitignored."
  key_links:
    - from: ".planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md"
      to: "SC2 evidence"
      via: "table values extracted from lighthouse/*.json"
      pattern: "Lighthouse Scores"
    - from: "Real iPhone walk results"
      to: "SC1 + SC4 evidence"
      via: "checklist in Critical-Path Walk section"
      pattern: "iPhone model"
---

<objective>
Wave 2 — phase-exit verification. After all source changes from Plans 05-02 through 05-07 land and push to the `phase-5` branch (auto-deploying a Vercel preview), this plan runs the quantitative Lighthouse audit, runs the qualitative real-iPhone walk, runs the reduced-motion walk, and records everything in `05-VERIFICATION.md`.

Purpose: Closes SC1 (real-device + topbar), SC2 (Lighthouse scores), SC3 (reduced-motion walk), SC4 (critical path), SC5 (visual confirmation on iPhone), SC6 (verdicts already filled by Plan 05-05). This is the phase-exit gate before Phase 6 ships to caleblim.com.

Output: One updated `05-VERIFICATION.md`, one populated `lighthouse/` directory inside the phase dir. No source code changes — this plan is purely evidence-recording.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mobile-performance-accessibility/05-CONTEXT.md
@.planning/phases/05-mobile-performance-accessibility/05-UI-SPEC.md
@.planning/phases/05-mobile-performance-accessibility/05-VALIDATION.md
@.planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md
@.planning/phases/05-mobile-performance-accessibility/05-RESEARCH.md
@scripts/lighthouse-audit.sh
@scripts/verify-build.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push phase-5, confirm Vercel preview is current, run Lighthouse audit</name>
  <files>.planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md, .planning/phases/05-mobile-performance-accessibility/lighthouse/</files>
  <action>
    Pre-flight: all Plans 05-02 through 05-07 are committed to the `phase-5` branch and pushed to GitHub. If any uncommitted work remains, surface to the user — this plan assumes the branch is clean and pushed.

    1. From project root, confirm branch state:
       ```
       git status                      # MUST be clean
       git branch --show-current       # MUST be phase-5
       git log --oneline origin/phase-5..HEAD   # MUST be empty (everything pushed)
       ```
       If any of these fail, halt and surface to user.

    2. Wait for Vercel to redeploy. The most recent push triggered a build; Vercel typically takes 30–60s. Confirm by curl:
       ```
       curl -sI https://caleb-lim-portfolio-git-phase-5-c-lb.vercel.app | head -1   # MUST be HTTP/2 200
       ```
       If 401 or 404, fall back to user checkpoint (deployment-protection check; same fallback as Plan 05-02 Task 2).

    3. Run the Lighthouse audit:
       ```
       bash scripts/lighthouse-audit.sh https://caleb-lim-portfolio-git-phase-5-c-lb.vercel.app
       ```
       This is the script Plan 05-01 authored. It loops over 5 routes, writes JSON+HTML per route to `.planning/phases/05-mobile-performance-accessibility/lighthouse/<slug>.{html,json}`, then prints a summary table.

    4. Parse each `lighthouse/*.json` for the three SC2 metrics:
       - `categories.performance.score * 100` per route — splash/gallery/detail must be ≥85
       - `categories.accessibility.score * 100` per route — all routes must be ≥95
       - `audits["largest-contentful-paint"].numericValue` ms on splash — must be <2000

       Use `node -e` or `jq` (project doesn't ship jq; node is already in repo) to extract values. Example:
       ```
       node -e "const r=require('./.planning/phases/05-mobile-performance-accessibility/lighthouse/splash.json'); console.log({perf:Math.round(r.categories.performance.score*100), a11y:Math.round(r.categories.accessibility.score*100), lcp:Math.round(r.audits['largest-contentful-paint'].numericValue)});"
       ```

    5. Open `05-VERIFICATION.md` `## Lighthouse Scores` section (created by Plan 05-01, header populated by Plan 05-02). Fill the table verbatim from the parsed values. For each row include a `Pass?` column entry (Y/N) per SC2 thresholds.

    6. **If any threshold misses:** Do NOT silently note the failure — surface it explicitly to the user as a checkpoint with the failing metric, the value, and the escalation path per 05-RESEARCH §3.7 steps 5-6 (downsize portrait3.jpg / portrait.jpg sources to ≤1120px; or force AVIF in `astro.config.mjs`). The escalation work would become a Plan 05-09 if needed. **If all pass**, proceed.

    7. **Commit durable SC2 evidence:** Plan 05-01 set up the .gitignore allow-list so `lighthouse/*-summary.json` files (per-route, three SC2 metrics each) are tracked, while `lighthouse/*.html` and the raw `lighthouse/*.json` stay local. Verify the audit emitted one summary per route — `ls .planning/phases/05-mobile-performance-accessibility/lighthouse/*-summary.json` should list 5 files (splash, design, marketing, about, detail). `git add` those summary files explicitly (e.g. `git add .planning/phases/05-mobile-performance-accessibility/lighthouse/*-summary.json`). In `05-VERIFICATION.md` reference the summary paths from each table row — e.g. `See: lighthouse/splash-summary.json` — rather than pull-quoting numeric values into the markdown body. The summary.json files are the canonical SC2 record; the markdown table is a human-readable index. (Rationale: pull-quoted numbers drift if a future Lighthouse run lands but the markdown isn't re-edited. Per-route summary files are immutable per run and survive re-audits as a versioned trail.) The HTML reports remain gitignored for forensic detail.
  </action>
  <verify>
    <automated>test -d .planning/phases/05-mobile-performance-accessibility/lighthouse &amp;&amp; test "$(ls .planning/phases/05-mobile-performance-accessibility/lighthouse/*-summary.json 2>/dev/null | wc -l | tr -d ' ')" -ge "1" &amp;&amp; grep -F 'Lighthouse Scores' .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md &amp;&amp; grep -F '/design' .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md &amp;&amp; grep -F 'summary.json' .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md</automated>
  </verify>
  <done>
    Lighthouse audit run; 5 route reports exist in `lighthouse/`; `05-VERIFICATION.md` table is populated with actual numbers; thresholds met OR escalation surfaced. Advances SC2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-iPhone critical-path walk + reduced-motion walk</name>
  <what-built>
    Plans 05-03 through 05-07 shipped: mobile topbar collapse at ≤700px, populated-gallery hero composition, token sweep, surgical reduced-motion architecture, touch-hover gating, entrance shimmer, StatusPill mobile shrink. Plan 05-08 Task 1 ran Lighthouse against the deployed preview URL.

    What remains is the qualitative real-device walk only Caleb can do per D-14 + 05-UI-SPEC §Critical-path accessibility walk.
  </what-built>
  <how-to-verify>
    Caleb walks the site on his own iPhone over hotel-wifi simulation (turn off home wifi, use cellular only). Two passes — once Reduce-Motion OFF, once Reduce-Motion ON.

    **Setup:**
    1. Disable home wifi on iPhone, leave cellular on, confirm signal ≥2 bars.
    2. Open Safari. Type the preview URL: `caleb-lim-portfolio-git-phase-5-c-lb.vercel.app`. Hit go.
    3. Note iPhone model + iOS version (Settings → General → About) — Claude will fill this into 05-VERIFICATION.md.

    **Pass 1: Reduce-Motion OFF (Settings → Accessibility → Motion → Reduce Motion = OFF)**

    Walk 10 steps from 05-UI-SPEC §"Critical-path accessibility walk" + 05-VERIFICATION.md `## Critical-Path Walk` checklist:
    1. Splash loads — composition readable, all 4 cards tappable, no horizontal scroll, no clipped text, topbar shows 3 icon glyphs right of brand
    2. Tap Graphic Design card — `/design` loads, tile shows HERO IMAGE (not olive slab), tile is tappable
    3. Tap tile — detail page loads, hero renders large above-fold, CRO blurbs readable, no horizontal scroll
    4. Scroll detail page — paginated slides (if present) scroll cleanly, no jank, no scroll-jacking
    5. Tap back-pill (`← Graphic Design`) — returns to `/design`
    6. Tap topbar envelope icon — Mail app opens with mailto draft
    7. Tap topbar "in" icon — LinkedIn opens in new tab/Safari window
    8. Tap topbar download arrow — resume PDF downloads + opens in iOS Files / system PDF viewer
    9. Tap OPEN-TO-ROLES pill — slow-scrolls to `/about#contact` (1.8s smooth scroll past the bio). **Tap-target sanity (SC1, StatusPill mobile shrink — D-04 / 05-07 Task 3):** confirm the pill is COMFORTABLY tappable with the iPhone thumb — no mis-taps into surrounding chrome (brand text below, status-bar above). The pill's effective height at ≤700px is ~27px which is below WCAG 2.5.8 AAA 44×44 by design (rationale in 05-UI-SPEC §"StatusPill mobile shrink": no neighboring tap targets within thumb-radius). **If a mis-tap occurs** — record it as a SC1 finding and file a Plan 05-09 follow-up to restore `.statuspill` padding so the pill is ≥44px tall (accept the slightly larger pill per 05-UI-SPEC fallback rule).
    10. Return to splash via brand link → switch to /marketing → second gallery loads with image tiles → walk a piece there too

    For each step, ✅ or ❌ + a one-line note. Screenshot each route on iPhone (splash / design / detail / about) and save into a `.planning/phases/05-mobile-performance-accessibility/screenshots/` folder if Caleb wants the visual record.

    **Pass 2: Reduce-Motion ON (Settings → Accessibility → Motion → Reduce Motion = ON)**

    Walk the 9-step reduced-motion checklist from 05-UI-SPEC §"Verification walk":
    1. Load splash → cards do NOT shake on entrance
    2. Tap-and-hold a card (or hover via Bluetooth mouse if available) → tilt+glass FIRE (exempt — D-08 #5/#7)
    3. Tap a role-link on the bio card → click-shake FIRES briefly (exempt — D-08 #13)
    4. Lime dot on StatusPill PULSES (exempt — D-08 #11)
    5. Tap OPEN-TO-ROLES → INSTANT JUMP to /about#contact, no smooth scroll (disabled — D-08 #14)
    6. Wait 3s on splash → carousel does NOT auto-advance (disabled — D-08 #1)
    7. Tap carousel arrow → slide transitions (user-initiated; acceptable to either snap or slide)
    8. Tab through nav with bluetooth keyboard if available → focus outlines fire crisply
    9. Visit `/design` gallery → tiles do NOT shimmer on entrance (touch entrance shimmer disabled under reduced-motion); tap a tile → navigates on first tap

    For each step, ✅ or ❌ + note.

    **Resume signal format:**
    Paste a Pass-1 + Pass-2 summary back in the chat. Claude will write the results into `05-VERIFICATION.md` `## Critical-Path Walk` and `## Reduced-Motion Walk` sections + tick the Phase Exit Sign-Off boxes. Include iPhone model + iOS version so D-14 record is complete.

    **If anything fails:**
    Describe what failed (which step, what saw, what expected). Claude will create a Plan 05-09 gap-closure for the failure rather than silently pass.
  </how-to-verify>
  <resume-signal>Type "iphone-passed" + paste a summary of the 19 steps (10 + 9), iPhone model, iOS version. OR type "iphone-failed" + describe the failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record real-device + reduced-motion results in 05-VERIFICATION.md and close phase</name>
  <files>.planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md</files>
  <action>
    Open `05-VERIFICATION.md`. Fill the following sections from the Task 2 checkpoint output:

    1. `## Real-Device Test Rig (D-14)` — iPhone model, iOS version, network description, test date.
    2. `## Critical-Path Walk` — populate each of the 10 step rows with the user's ✅/❌ + notes from Pass 1.
    3. `## Reduced-Motion Walk` — populate each of the 9 step rows with the user's ✅/❌ + notes from Pass 2.
    4. `## Phase Exit Sign-Off`:
       - `[x] All verify-build.sh gates green` — verify by running `bash scripts/verify-build.sh` one final time and confirming 25 OK lines.
       - `[x] All Lighthouse thresholds met` — already ticked if Task 1 passed; leave unticked if any threshold missed.
       - `[x] Real-device walk recorded` — tick if Task 2 Pass-1 succeeded.
       - `[x] Reduced-motion walk recorded` — tick if Task 2 Pass-2 succeeded.
       - `[x] --terracotta audit complete` — already ticked by Plan 05-05.

    Update the frontmatter: `status: complete` (was `pending`), `recorded_at: <today's date>`.

    Do NOT modify any source file. Do NOT re-run Lighthouse. Do NOT alter Plan 05-05's `--terracotta` audit. This task is purely transcription + final gate-check.

    Final verify: `bash scripts/verify-build.sh` reports 25/25 OK. If anything fails at this final check, escalate — the regression must be fixed before phase exit (would become a Plan 05-09 gap closure).
  </action>
  <verify>
    <automated>bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/05-08-final.log &amp;&amp; ! grep -F 'FAIL' /tmp/05-08-final.log &amp;&amp; grep -F 'status: complete' .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md &amp;&amp; grep -F 'iPhone' .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md &amp;&amp; ! grep -F 'TBD' .planning/phases/05-mobile-performance-accessibility/05-VERIFICATION.md</automated>
  </verify>
  <done>
    `05-VERIFICATION.md` is fully populated — no TBDs remain; status: complete; recorded_at filled; all Phase Exit Sign-Off boxes ticked. `bash scripts/verify-build.sh` final run reports all 25 gates GREEN. Phase 5 is verifiably complete. Advances SC1/SC2/SC3/SC4/SC5/SC6 evidence.
  </done>
</task>

</tasks>

<verification>
- `bash scripts/verify-build.sh` final run — all 25 gates OK
- `05-VERIFICATION.md` has no TBD placeholders; status: complete; all 6 SCs recorded with evidence
- Lighthouse JSON exists for splash, /design, /marketing, /about, /design/<slug> in `lighthouse/`
- Real-iPhone critical path complete (Pass 1) — recorded in Critical-Path Walk
- Reduced-motion behavior verified (Pass 2) — recorded in Reduced-Motion Walk
- No regressions to Phase 1–4 contracts (Gates 1–22 still GREEN)
</verification>

<success_criteria>
Phase 5 is shippable to Phase 6. All six SCs from ROADMAP §"Phase 5: Mobile, Performance, Accessibility" have evidence in `05-VERIFICATION.md`:
- SC1 (real-device iPhone pass + topbar collapse ≤700px) → Critical-Path Walk + Gate 23
- SC2 (Lighthouse Perf ≥85, A11y ≥95, LCP <2s on splash) → Lighthouse Scores table
- SC3 (prefers-reduced-motion per D-08) → Reduced-Motion Walk
- SC4 (critical path unbroken on iPhone) → Critical-Path Walk steps 2-8
- SC5 (gallery tiles render hero) → Critical-Path Walk step 2 + Gate 24
- SC6 (token hygiene) → --terracotta audit + Gate 25

Phase 4 carry-overs (BLOCKER-1, BLOCKER-2, WARNING-1) are closed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-performance-accessibility/05-08-SUMMARY.md` per the standard summary template. Then update `.planning/STATE.md`:
- `progress.completed_phases`: bump from 4 to 5
- `progress.completed_plans`: bump from 19 to 27 (Phase 5 added 8 plans)
- `last_activity`: today's date + "Phase 05 marked complete"
- `stopped_at`: "Phase 05 closed — ready for Phase 06 deploy"
- Update `Current Position` to point at Phase 06

Also update `.planning/ROADMAP.md`:
- Phase 5 line: change `[ ]` → `[x]`
- Phase 5 plan list: tick all 8 plans
</output>
