---
phase: 5
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/components/GalleryA12.astro
  - src/components/GalleryB35.astro
  - src/components/GalleryC68.astro
  - src/pages/index.astro
  - src/pages/[category]/[slug].astro
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
must_haves:
  truths:
    - "Each populated-discipline gallery tile renders the piece's hero `<img>` at `opacity: 1` in the LEFT 60% of the tile (NOT the previous `opacity: 0.55` watermark)"
    - "Each tile has a 4:5 portrait aspect-ratio overall; hero LEFT 60% / text RIGHT 40% above 540px; below 540px the layout flattens to hero TOP 60% / text BOTTOM 40% (vertical stack)"
    - "The `.deco` per-slot decoration element is removed from each gallery component — the hero is now the visual interest (Q1 resolution in 05-UI-SPEC)"
    - "Per-slot accent background stays on the text column (Q3 resolution in 05-UI-SPEC)"
    - "The p1 tile in each populated gallery has `<Image priority>` set (LCP candidate for /design and /marketing)"
    - "Portrait carousel first slide on splash has `priority={i === 0}` and `sizes=\"(max-width: 900px) 100vw, 280px\"` (LCP candidate for splash)"
    - "Detail-page hero has `priority` and `sizes=\"(max-width: 960px) 100vw, 960px\"` (above-fold LCP element)"
    - "Gate 24 in `scripts/verify-build.sh` reports OK"
    - "Phase 1–4 gates 1–22 still pass (no regression to splash, content, pdf-rasterization, or pager contracts — particularly Gate 22 gallery-order parity)"
  artifacts:
    - path: "src/components/GalleryA12.astro"
      provides: "Tile composition rewrite — `.b-piece` is grid 60/40, hero promoted to grid cell opacity 1, deco removed, priority on p1"
    - path: "src/components/GalleryB35.astro"
      provides: "Same tile internal rewrite, bucket outer grid spans preserved"
    - path: "src/components/GalleryC68.astro"
      provides: "Same tile internal rewrite, bucket outer grid spans preserved"
    - path: "src/pages/index.astro"
      provides: "Portrait carousel first slide gains priority={i === 0} and updated sizes — splash LCP intervention only; all other index.astro work belongs to Plan 05-05 (token sweep) and Plan 05-07 (hover gating)"
    - path: "src/pages/[category]/[slug].astro"
      provides: "Detail hero gains priority and updated sizes — detail LCP only; pager hover work belongs to Plan 05-07"
  key_links:
    - from: "gallery tile composition (3 components)"
      to: "piece hero asset"
      via: "<Image src={piece.data.hero} priority={slot === 1} sizes=\"(max-width: 900px) 50vw, 240px\" widths={[280, 560]} class=\"cover\" />"
      pattern: "fetchpriority=\"high\""
    - from: "splash carousel"
      to: "first portrait image LCP"
      via: "index.astro <Image priority={i === 0} sizes=\"(max-width: 900px) 100vw, 280px\" />"
      pattern: 'priority=\{i === 0\}'
    - from: "detail hero"
      to: "above-fold LCP element"
      via: "[slug].astro <Image priority sizes=\"(max-width: 960px) 100vw, 960px\" />"
      pattern: 'sizes="\(max-width: 960px\)'
---

<objective>
Wave 1 — close BLOCKER-2 from Phase 4 UI-REVIEW (empty gallery slabs) AND ship the LCP-critical `priority`/`sizes` interventions per 05-RESEARCH §3.7 steps 1–4.

Purpose: SC5 (populated galleries render hero images) + half of SC2 (splash/gallery/detail LCP). Phase 4 review misdiagnosed the bug — the hero is already wired but sits at `opacity: 0.55` behind a colored fill. This plan promotes it to a 60% grid cell, drops the now-redundant `.deco`, and adds `priority`/`sizes` to the three LCP-candidate elements.

Output: Three Gallery* components rewritten in lockstep; carousel `<Image>` updated; detail hero `<Image>` updated. Gate 24 turns green; LCP-target images carry `priority` + responsive `sizes`. Per 05-RESEARCH §3.7 interventions 1–4, this should clear SC2 LCP without needing source-image downsize or AVIF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mobile-performance-accessibility/05-CONTEXT.md
@.planning/phases/05-mobile-performance-accessibility/05-RESEARCH.md
@.planning/phases/05-mobile-performance-accessibility/05-UI-SPEC.md
@.planning/phases/04-navigation-secondary-surfaces/04-UI-REVIEW.md
@src/components/GalleryA12.astro
@src/components/GalleryB35.astro
@src/components/GalleryC68.astro

<interfaces>
<!-- Current tile structure (verbatim per 05-RESEARCH §2.2 — all three components share it): -->
<!-- <a class="b-piece p{slot}"> -->
<!--   <span class="tag">0X / 0Y</span> -->
<!--   <Image src={piece.data.hero} alt={piece.data.title} class="cover" />  ← already wired -->
<!--   <div class="meta"> -->
<!--     <span class="ttl">{title}</span> -->
<!--     <span class="role">{role}</span> -->
<!--   </div> -->
<!--   <span class="deco" aria-hidden="true"></span>                          ← drop per Q1 -->
<!-- </a> -->

<!-- Current .cover CSS (the bug per 05-RESEARCH §2.2): -->
<!--   position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; z-index: 0; opacity: 0.55; -->

<!-- Target composition (05-UI-SPEC §"Visual Contract — Gallery Tile"): -->
<!--   .b-piece { display: grid; grid-template-columns: 60% 40%; aspect-ratio: 4/5; border-radius: 8px; overflow: hidden; position: relative; } -->
<!--   .b-piece .cover { grid-column: 1; width: 100%; height: 100%; object-fit: cover; object-position: center; opacity: 1; } -->
<!--   .b-piece .meta  { grid-column: 2; display: flex; flex-direction: column; justify-content: end; padding: var(--sp-4); gap: var(--sp-2); background: var(--accent-bg); } -->
<!--   @media (max-width: 540px) { .b-piece { grid-template-columns: 1fr; grid-template-rows: 60% 40%; } } -->

<!-- Schema reference: piece.data.hero is image()-typed (src/content.config.ts:11-29) — Astro <Image> handles it. -->
<!-- Per-discipline accent: bucket components reference --accent-bg / --accent var set by [category].astro:88. Preserved per Q3. -->

<!-- LCP contract (05-UI-SPEC §"LCP element contract per route"): -->
<!--   /  (splash):           carousel[0] priority={i === 0}, sizes="(max-width: 900px) 100vw, 280px" -->
<!--   /design + /marketing:  gallery p1 tile priority={slot === 1}, sizes="(max-width: 900px) 50vw, 240px" -->
<!--   /[category]/[slug]:    hero priority, sizes="(max-width: 960px) 100vw, 960px" -->
<!-- (Astro 5.18.1 supports priority since 5.10 per 05-RESEARCH §5.9.) -->

<!-- Existing per-gallery reduced-motion blocks to remove (conflict with D-08 hover-exempt rule per 05-UI-SPEC §"Implementation contract" step 4): -->
<!--   GalleryA12.astro:139-142 — @media (prefers-reduced-motion) .b-piece:hover { transform: none } -->
<!--   GalleryB35.astro:183-186 — same -->
<!--   GalleryC68.astro:171-175 — same -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite GalleryA12.astro tile composition + drop .deco</name>
  <files>src/components/GalleryA12.astro</files>
  <behavior>
    - On a built `/design` page with ≥1 piece, the tile's hero renders at full opacity, taking the left 60% of the tile (or top 60% below 540px).
    - Title + role text column renders at the right 40% with its existing per-slot accent background.
    - The `.deco` element no longer exists in rendered HTML.
    - The p1 tile's `<img>` carries `loading="eager"` and `fetchpriority="high"` (Astro priority prop emits these).
    - Tile aspect-ratio is 4:5 portrait at all viewports >540px.
    - `grep -c '<img' dist/design/index.html` ≥ count of non-draft design pieces (Gate 24 GREEN for /design).
  </behavior>
  <action>
    Open `src/components/GalleryA12.astro`. Three regions to edit:

    **(A) Markup (`.b-piece` anchor):**
    1. Update the `<Image>` props: add `priority={slot === 1}`, `widths={[280, 560]}`, `sizes="(max-width: 900px) 50vw, 240px"`. Keep `src={piece.data.hero}`, `alt={piece.data.title}`, `class="cover"`.
    2. Remove the `<span class="deco" aria-hidden="true"></span>` element entirely (Q1 resolution).
    3. Move the `<span class="tag">` element INSIDE the `<div class="meta">` (its first child), so the entire text column is one flex column in grid-column 2.

    **(B) CSS — `.b-piece` rule:** Find the existing `.b-piece` rule and replace its layout properties with:
    ```
    .b-piece {
      display: grid;
      grid-template-columns: 60% 40%;
      aspect-ratio: 4 / 5;
      border-radius: 8px;
      overflow: hidden;
      position: relative;
      /* keep existing color/transition/transform from the hover rules */
    }
    ```
    Keep the existing `:hover` rule (`transform: scale(1.02) rotate(-0.3deg)` etc.) untouched — Plan 05-07 wraps it in `@media (hover: hover)`. **Remove the existing `@media (prefers-reduced-motion: reduce) .b-piece:hover { transform: none }` block** at GalleryA12.astro:139-142 — D-08 makes hover-feedback exempt; this conflicts. Plan 05-06 owns the global reduced-motion architecture; this plan removes the per-component hammer.

    **(C) CSS — `.cover` + `.meta` + `.tag` + `.deco`:** Replace the existing `.b-piece .cover` rule with:
    ```
    .b-piece .cover {
      grid-column: 1;
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center;
      opacity: 1;          /* was 0.55 — the bug */
    }
    ```
    Remove `position: absolute; inset: 0; z-index: 0` (no longer needed as a grid child).

    Replace `.b-piece .meta`:
    ```
    .b-piece .meta {
      grid-column: 2;
      display: flex;
      flex-direction: column;
      justify-content: end;
      padding: var(--sp-4);
      gap: var(--sp-2);
      background: var(--accent-bg);  /* preserves per-slot accent — Q3 */
      /* keep existing color, typography rules on .ttl / .role children */
    }
    ```

    Drop ALL `.deco` selectors — the base `.b-piece .deco` rule AND the slot-specific variants `.p1 .deco { font-size: 240px ... }`, `.p2 .deco`, etc. That deletion also kills two raw `font-size` literals (`240px` and `90px`) per 05-RESEARCH §2.5 inventory — free Gate 25 contribution.

    Ensure `.tag` no longer has `position: absolute`; it flows naturally as the first child of `.meta`. Keep its typography (`font-size: var(--fs-card-no)`, color, letter-spacing).

    Add a `@media (max-width: 540px)` block:
    ```
    @media (max-width: 540px) {
      .b-piece {
        grid-template-columns: 1fr;
        grid-template-rows: 60% 40%;
      }
      .b-piece .cover { grid-column: 1; grid-row: 1; }
      .b-piece .meta  { grid-column: 1; grid-row: 2; }
    }
    ```

    **Do NOT touch the OUTER gallery grid** — A12 has its own outer layout (1-2 piece bucket). Phase 3 magazine asymmetry preserved; only per-tile internals change.

    Per 05-RESEARCH §5.8: after editing run `npm run build` — Astro will recompile scoped CSS hashes (expected).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/05-04-t1.log &amp;&amp; grep -F 'OK: Gate 24' /tmp/05-04-t1.log &amp;&amp; ! grep -F 'class="deco"' dist/design/index.html &amp;&amp; grep -F 'fetchpriority="high"' dist/design/index.html</automated>
  </verify>
  <done>
    GalleryA12 tile composition is rewritten: grid 60/40, hero opacity 1, deco gone, p1 has priority, mobile vertical stack works ≤540px. `dist/design/index.html` emits ≥1 `<img>` with `fetchpriority="high"`. Gates 1–22 still pass. Advances SC5 + SC2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror the rewrite into GalleryB35.astro + GalleryC68.astro</name>
  <files>src/components/GalleryB35.astro, src/components/GalleryC68.astro</files>
  <behavior>
    - Both files match A12's internal tile composition exactly (60/40 grid, opacity 1 cover, deco removed, tag moved into meta).
    - Their bucket-specific OUTER grid (B35's 5-tile composition; C68's 6-8 piece flow) is preserved.
    - The per-component `@media (prefers-reduced-motion) .b-piece:hover { transform: none }` blocks at B35:183-186 and C68:171-175 are removed.
    - `priority={slot === 1}` applied in both (covers any /marketing piece that lands as p1).
    - Gate 22 (gallery-order parity with detail pager) remains GREEN.
  </behavior>
  <action>
    Apply the same three-region edit pattern from Task 1 to BOTH `src/components/GalleryB35.astro` AND `src/components/GalleryC68.astro`. Differences to mind:

    1. **Outer grid spans differ per bucket.** B35 has a sketch-locked 5-tile layout (p1+p2 grow, p3–p5 smaller); C68 has a 6–8 piece flow. The OUTER `grid-template-areas` / `grid-column: span N` rules on each `.b-piece.p{slot}` are PRESERVED. You're rewriting the INSIDE of each `.b-piece`, not its placement within the gallery grid.

    2. **B35 has 5 `<a class="b-piece">` instances; C68 has 6-8.** Apply the markup change (`<Image priority={slot === 1} widths sizes>`, drop `.deco`, move `.tag` into `.meta`) to EACH instance.

    3. **Each component has its own `.b-piece .deco` style with per-slot variants** (e.g. `.p3 .deco`, `.p5 .deco` per 05-RESEARCH §2.5 — same 90px/240px literals). Drop ALL `.deco` style blocks from both files. Two more raw `font-size: Npx` literals disappear per file — Gate 25 contributions.

    4. **Remove the per-component reduced-motion hover-disable blocks.** B35:183-186 and C68:171-175. (A12's was removed in Task 1.) Per 05-UI-SPEC §"Implementation contract" step 4.

    5. **Bucket selection logic** lives in `src/pages/[category].astro` — do NOT modify it. Run `grep -n 'Gallery[ABC]' src/pages/\[category\].astro` first to confirm which bucket renders for /design vs /marketing at the current piece counts.

    After editing run `npm run build && bash scripts/verify-build.sh`. Gate 24 MUST be GREEN on both `/design` and `/marketing` HTMLs. Gate 22 MUST remain GREEN. If marketing renders A12 (1 piece) and design renders A12 (1 piece) at current content state, Task 2's gates on B35/C68 won't fire visibly until more pieces land — but the rewrite still locks future safety.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/05-04-t2.log &amp;&amp; grep -F 'OK: Gate 24' /tmp/05-04-t2.log &amp;&amp; grep -F 'OK: Gate 22' /tmp/05-04-t2.log &amp;&amp; ! grep -rF 'class="deco"' src/components/Gallery</automated>
  </verify>
  <done>
    Both B35 and C68 have identical internal tile composition to A12. Their outer bucket layouts are untouched. The three per-component reduced-motion `transform: none` blocks are gone. Gate 22 (pager parity) and Gate 24 (gallery img count) are GREEN. Advances SC5 + SC2 (gallery side).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add priority/sizes to splash carousel + detail hero</name>
  <files>src/pages/index.astro, src/pages/[category]/[slug].astro</files>
  <behavior>
    - `src/pages/index.astro` portrait carousel: the first `<Image>` (index 0 in the slides array) has `priority={i === 0}` and `sizes="(max-width: 900px) 100vw, 280px"`. Subsequent slides do NOT get `priority` (Astro emits `loading="lazy"` for them by default).
    - `src/pages/[category]/[slug].astro` detail hero: the hero `<Image>` (the one at the top of the detail body, NOT the paginated PDF slides below) has `priority` (no conditional — it's always above the fold) and `sizes="(max-width: 960px) 100vw, 960px"`.
    - Built HTML for `/` contains `fetchpriority="high"` exactly once (for the first carousel slide).
    - Built HTML for `/design/<one-slug>` contains `fetchpriority="high"` exactly once (for the hero) — the paginated slide images stay lazy.
    - No regression to Gate 7 (PDF rasterization), Gate 10 (paginated slides), or Gate 11 (full-PDF link).
  </behavior>
  <action>
    Two surgical edits, one per file:

    **(A) `src/pages/index.astro`:** Locate the portrait carousel `<Image>` element (per 05-RESEARCH §3.7 step 1, around index.astro:78 — `<Image src={p.src} ... />` inside the slides loop). Add `priority={i === 0}` and `sizes="(max-width: 900px) 100vw, 280px"` to its props. Keep all existing props (`src`, `alt`, `widths`, any existing `class`) verbatim. Only the carousel slide loop is touched — do NOT touch the bio card, role-links, discipline cards, question bar, footer, or any of the other carousel scripts.

    **(B) `src/pages/[category]/[slug].astro`:** Locate the detail hero `<Image>` (per 05-CONTEXT §code_context — around [slug].astro:95, the `<Image src={piece.data.hero} ... class="hero">` element OR however the component currently styles it; grep for the first `<Image>` in the file). Add `priority` (bare, no condition) and `sizes="(max-width: 960px) 100vw, 960px"`. Keep all existing props. Do NOT touch the paginated PDF block below (`piece.data.pdfPaginate` slides at [slug].astro:~120 — those stay lazy per FOUND-02). Do NOT touch the pager (Plan 04-02 territory, Phase 5 Plan 05-07 will gate its hover).

    Per 05-RESEARCH §5.9: Astro 5.18.1 supports `priority` since 5.10 — no version bump needed.

    After editing run `npm run build && bash scripts/verify-build.sh`. All gates must remain GREEN. Then verify with the `grep -c 'fetchpriority="high"' dist/index.html` check — splash should report exactly 1.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/05-04-t3.log &amp;&amp; ! grep -F 'FAIL' /tmp/05-04-t3.log &amp;&amp; test "$(grep -c 'fetchpriority="high"' dist/index.html)" = "1" &amp;&amp; grep -lF 'fetchpriority="high"' dist/design/*/index.html | head -1</automated>
  </verify>
  <done>
    Splash carousel first slide has priority + correct sizes. Detail hero has priority + correct sizes. Paginated PDF slides remain lazy. Gates 7/10/11 unchanged. Advances SC2 (LCP on splash, detail).
  </done>
</task>

</tasks>

<verification>
- `npm run build && bash scripts/verify-build.sh` — Gates 1–22 + Gate 24 all OK; Gate 23 OK (Plan 05-03 also landed); Gate 25 remains FAIL until Plan 05-05
- Visual check at desktop on `/design` and `/marketing` — gallery tile shows hero image, not olive slab; per-slot accent visible on right 40% column
- Visual check at 375px on `/design` — tile stacks vertical (hero top, text bottom); no overflow
- `grep -c 'fetchpriority="high"' dist/index.html` returns `1` (splash)
- `grep -c 'fetchpriority="high"' dist/design/<slug>/index.html` returns ≥`1` (detail hero) — exact count depends on whether the gallery on the detail page renders other priority candidates (it doesn't — detail page just shows hero + CRO + paginated slides + pager)
</verification>

<success_criteria>
SC5 (gallery tiles render hero) is closed. Half of SC2 (LCP-critical images on splash, gallery, detail are pre-loaded with `priority` + `fetchpriority="high"`) is ready; the actual SC2 Lighthouse measurement happens in Plan 05-08. Phase 4 BLOCKER-2 is closed; the carry-over note in ROADMAP §Phase 5 SC5 can be re-classified as "closed by Plan 05-04."
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-performance-accessibility/05-04-SUMMARY.md`. Note:
- BLOCKER-2 was a CSS bug (opacity 0.55), not a wiring bug — research §1 finding #1 confirmed pre-execution
- `.deco` element drop incidentally killed 6 raw font-size literals (240px ×3, 90px ×3) — partial Gate 25 contribution; Plan 05-05 picks up the remaining ~21
- The three per-component reduced-motion hover-disable blocks were removed in this plan, not Plan 05-06 — surface to Plan 05-06 so it doesn't duplicate the work
</output>
