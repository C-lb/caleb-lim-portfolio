---
phase: 5
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/layouts/Base.astro
  - src/styles/tokens.css
autonomous: true
requirements:
  - FOUND-01
must_haves:
  truths:
    - "On a viewport ≤700px wide, the topbar shows the brand `caleb lim` left + three 44×44 px icon glyphs (✉, in, ⤓) right. The three text labels `caleb.lim.2024@smu.edu.sg`, `linkedin`, `resume` are visually hidden but remain in source for SEO/AT"
    - "Above 700px, the existing desktop text-link layout is unchanged (the Phase 4 chrome still ships)"
    - "Each icon link is exactly 44×44 px, with the SVG glyph centered via flex; the entire 44×44 area is the click target"
    - "Each icon link carries an `aria-label` per 05-UI-SPEC: `Email Caleb` / `Caleb on LinkedIn` / `Download Caleb's resume`. The SVG itself is `aria-hidden=\"true\"`"
    - "Desktop `.nav-link` tap-target floor is raised to ≥24×24 px (WCAG 2.5.8 AA) by bumping padding from `4px 0` to ≥`12px 8px`"
    - "Gate 23 in `scripts/verify-build.sh` (built dist/{index,design,about}/index.html contains `@media (max-width: 700px)` AND `aria-label=\"Email Caleb\"` family) reports OK"
  artifacts:
    - path: "src/layouts/Base.astro"
      provides: "Topbar markup gains three inline SVG glyphs, paired with `.nav-text` spans for >700px and `.visually-hidden` fallback below 700px. CSS gains the @media (max-width: 700px) block + bumped desktop padding."
    - path: "src/styles/tokens.css"
      provides: "Adds `.visually-hidden` utility class (clip-path-based screen-reader hider) per 05-UI-SPEC §Accessibility ARIA contract additions."
  key_links:
    - from: "src/layouts/Base.astro `.nav-link`"
      to: "icon glyph swap at ≤700px"
      via: "`@media (max-width: 700px) { .nav-text { display: none } .nav-icon { display: block } .nav-link { width: 44px; height: 44px; padding: 0; display: inline-flex; align-items: center; justify-content: center } }`"
      pattern: '@media \(max-width: 700px\)'
    - from: "every built page (Base.astro is universal layout)"
      to: "topbar collapse"
      via: "Astro scoped CSS inlines into every dist/*.html — Gate 23 verifies this"
      pattern: "aria-label=\"Email Caleb\""
---

<objective>
Wave 1 — close BLOCKER-1 from Phase 4 UI-REVIEW. Implement the ≤700px topbar collapse per D-01/D-02/D-03 and raise the desktop tap-target floor to WCAG 2.5.8 AA per the Pillar-5 carry-over.

Purpose: SC1 (mobile critical path + ≤700px topbar collapse) — the single largest BLOCKER currently shipping to production. Recruiters on iPhone Safari today see `resume` clipped off the right edge.

Output: One `Base.astro` edit (markup + CSS), one `tokens.css` edit (adds `.visually-hidden` utility). Gate 23 turns green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mobile-performance-accessibility/05-CONTEXT.md
@.planning/phases/05-mobile-performance-accessibility/05-RESEARCH.md
@.planning/phases/05-mobile-performance-accessibility/05-UI-SPEC.md
@.planning/phases/04-navigation-secondary-surfaces/04-UI-REVIEW.md
@src/layouts/Base.astro
@src/styles/tokens.css

<interfaces>
<!-- Current topbar markup (Base.astro:31-39, verbatim per 05-RESEARCH §2.1): -->
<!-- <header class="topbar"> -->
<!--   <a href="/" class="brand">caleb lim</a> -->
<!--   <StatusPill />        ← position:fixed, OWN coordinate space, ignore in collapse -->
<!--   <nav aria-label="primary"> -->
<!--     <a class="nav-link" href="mailto:caleb.lim.2024@smu.edu.sg">caleb.lim.2024@smu.edu.sg</a> -->
<!--     <a class="nav-link" target="_blank" rel="noopener noreferrer" href="https://www.linkedin.com/in/caleblim/">linkedin</a> -->
<!--     <a class="nav-link" href="/caleb-lim-resume.pdf" download>resume</a> -->
<!--   </nav> -->
<!-- </header> -->

<!-- Current .nav-link CSS (Base.astro:88-100ish, padding: 4px 0 → ~19px effective tap height, below WCAG AA): -->
<!-- .nav-link { padding: 4px 0; ... } -->

<!-- Phase 5 UI-SPEC §"Topbar Collapse Behavior" locks the contract: -->
<!--   > 700px:  .nav-text visible, .nav-icon display:none, .nav-link inline w/ padding -->
<!--   ≤ 700px:  .nav-text display:none + .visually-hidden for AT, .nav-icon 44x44 inline-flex centered -->

<!-- visually-hidden utility (canonical clip-path pattern per WAI-ARIA Authoring Practices, 05-UI-SPEC §ARIA contract additions): -->
<!--   position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; -->
<!--   overflow: hidden; clip-path: inset(50%); white-space: nowrap; border: 0; -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add .visually-hidden utility to tokens.css</name>
  <files>src/styles/tokens.css</files>
  <behavior>
    - After Plan 05-03 Task 1 ships, `grep -c 'visually-hidden' src/styles/tokens.css` returns ≥1.
    - The class declaration matches the canonical clip-path pattern exactly: `position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip-path: inset(50%); white-space: nowrap; border: 0`.
    - The utility lives in `tokens.css` (NOT a new file) to avoid an extra import chain — Base.astro already imports tokens.css.
  </behavior>
  <action>
    Open `src/styles/tokens.css`. After the last existing rule (current end of file ~line 70), append a clearly-commented `/* Phase 5: a11y utility — visually-hidden (clip-path screen-reader hider) */` block followed by the `.visually-hidden { ... }` class with the exact properties listed in `<behavior>`. Do NOT modify any other rule in this file — Plan 05-05 owns the rest of the `tokens.css` work (D-17 amendments, --sp-3 add, etc.). One isolated append, ~10 lines including the comment.

    Per 05-UI-SPEC §"ARIA contract additions": this utility class is the substrate for hiding the `.nav-text` spans below 700px while keeping them in the accessibility tree.
  </action>
  <verify>
    <automated>grep -F 'visually-hidden' src/styles/tokens.css &amp;&amp; grep -F 'clip-path: inset(50%)' src/styles/tokens.css</automated>
  </verify>
  <done>
    `.visually-hidden` utility exists in `tokens.css` and follows the canonical pattern. No other rule modified. Advances SC1 (a11y substrate for Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Topbar mobile collapse + desktop tap-target floor in Base.astro</name>
  <files>src/layouts/Base.astro</files>
  <behavior>
    - On a desktop (>700px) viewport, the topbar renders the existing three text links (`caleb.lim.2024@smu.edu.sg`, `linkedin`, `resume`) unchanged in look. New: each `.nav-link` is ≥24px tall (WCAG 2.5.8 AA).
    - On a ≤700px viewport, the three text labels visually disappear and are replaced by three centered 44×44 px icon glyphs (envelope, "in", download arrow). The hover-color and focus-outline behaviors that already work on desktop still work on the icons.
    - Each icon glyph link has an `aria-label` exactly matching 05-UI-SPEC §"Accessible naming for icon glyphs": "Email Caleb" / "Caleb on LinkedIn" / "Download Caleb's resume". The SVG element itself is `aria-hidden="true"`.
    - The brand `caleb lim` left + `<nav>` right layout is preserved (flex space-between) at every viewport. No hamburger drawer, no stacked-row treatment.
    - The StatusPill is left alone — it is `position: fixed` per 05-CONTEXT §code_context and lives in its own coordinate space.
    - `npm run build && bash scripts/verify-build.sh` reports `OK: Gate 23` (Plan 05-01 Task 2's gate turns green).
    - All Phase 1–4 gates still pass (specifically: Gate 19a–f and Gate 20 verify the email/LinkedIn/resume href contract, the target=_blank-with-noopener contract, and the resume-in-header contract — none must regress).
  </behavior>
  <action>
    Open `src/layouts/Base.astro`. Two regions to edit:

    **(A) Markup — each `.nav-link` becomes a span+svg pair.** Inside the `<nav aria-label="primary">` block (around Base.astro:31–39), rewrite each of the three `.nav-link` anchors per the 05-UI-SPEC §"Topbar Collapse Behavior" table. The new structure for each:

    ```
    <a class="nav-link" href="mailto:caleb.lim.2024@smu.edu.sg" aria-label="Email Caleb">
      <span class="nav-text">caleb.lim.2024@smu.edu.sg</span>
      <svg class="nav-icon" aria-hidden="true" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
        <!-- envelope path -->
      </svg>
    </a>
    ```

    Repeat for LinkedIn (keep `target="_blank" rel="noopener noreferrer"` — Gate 20 verifies it) and resume (keep `download` attribute — Gate 19d verifies it). For the SVG glyphs (per Claude's Discretion #1 in 05-CONTEXT.md + Q5 resolution in 05-UI-SPEC):
    - **Envelope**: 24x24 viewBox, monoline. `M 3 5 h 18 v 14 h -18 z` + a `M 3 5 l 9 7 l 9 -7` chevron for the flap.
    - **"in"**: 24x24 viewBox, hand-author the `i` (small square dot top + vertical bar) + `n` (mirrored arch). Roughly: `M 4 9 v 11 M 4 5 v 0.5` for the i, `M 10 20 v -11 M 10 14 c 0 -3 4 -5 8 -5 v 11` for the n. Tune to look like the LinkedIn `in` mark without being a 1:1 trademark copy. Stroke-based, no fill.
    - **Download arrow**: 24x24 viewBox. `M 12 4 v 12 M 6 11 l 6 6 l 6 -6 M 4 21 h 16` — downward chevron over a baseline.

    Each `.nav-link` MUST also gain the `aria-label` matching the table above. The visible text strings stay in source via `<span class="nav-text">` — at ≤700px they hide behind the `.visually-hidden` utility OR a `display: none` (pick one and document — `display: none` is simpler and yields the same screen-reader behavior because `aria-label` on the parent already provides the accessible name; `visually-hidden` on the span is only needed if we want screen readers to read BOTH the aria-label and the text, which is double-announcement and bad).

    **Decision (apply this):** Use `display: none` on `.nav-text` at ≤700px. The `aria-label` on the parent `<a>` is the screen-reader accessible name. The `.visually-hidden` utility from Task 1 is reserved for any future use; it's not consumed here. (This is the cleanest pattern per WAI-ARIA — the parent's `aria-label` overrides child text content for the AT name, so hiding the text via `display: none` is correct and avoids the double-announce.)

    **(B) CSS — add the @media (max-width: 700px) block and bump desktop padding.** In the `<style>` block at the bottom of `Base.astro`:

    1. Modify the existing `.nav-link` rule (currently around line 88-100): change `padding: 4px 0` to `padding: 12px 8px` (clears WCAG AA 24×24 at all widths — Phase 5 desktop floor per 05-UI-SPEC §"Site-wide tap-target floor"). Keep all other properties as-is (font-family, color, text-transform).
    2. Add a new rule for `.nav-icon` at the top of the same style scope: `display: none; stroke: currentColor; fill: none;` (hidden by default; shown only ≤700px).
    3. Add a new rule for `.nav-text`: nothing special at desktop (it's the default-visible label).
    4. Add a new `@media (max-width: 700px) { ... }` block. Inside:
       - `.nav-text { display: none; }`
       - `.nav-icon { display: block; }`
       - `.nav-link { width: 44px; height: 44px; padding: 0; display: inline-flex; align-items: center; justify-content: center; }`
       - `.topbar nav { gap: var(--sp-2); }` (tighter gap fits three icons in the row)
       - **Do NOT** touch `.brand` or `.topbar` flex container — the brand stays left, the nav stays right (justify-content: space-between is already correct).
       - **Do NOT** touch `StatusPill` or `.statuspill` — its `position: fixed; top: 12px` is out-of-flow and Plan 05-07 will tune its padding separately (D-04).

    Do NOT remove or alter Gates 19a/19b/19c/19d/19f/20 contract surfaces:
    - `href="mailto:..."` stays on the email link (Gate 19a)
    - `href="https://www.linkedin.com/in/caleblim/"` stays (Gate 19b)
    - `target="_blank" rel="noopener noreferrer"` stays on LinkedIn (Gate 20)
    - `href="/"` on `.brand` stays (Gate 19c)
    - `href="/caleb-lim-resume.pdf"` + `download` stays on the resume link (Gate 19d)
    - `<header>` + `<main>` landmarks + `aria-current="page"` logic stay (Gate 19f)

    After editing run `npm run build && bash scripts/verify-build.sh`. Gate 23 MUST flip GREEN. Gates 1–22 MUST remain GREEN. If Gate 22 (gallery prev/next parity) regresses, that's unrelated; surface it. If Gate 19d regresses, your `<a download>` attribute didn't survive the rewrite — fix.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; bash scripts/verify-build.sh 2>&amp;1 | tee /tmp/05-03-verify.log &amp;&amp; grep -F 'OK: Gate 23' /tmp/05-03-verify.log &amp;&amp; ! grep -F 'FAIL: Gate 19' /tmp/05-03-verify.log &amp;&amp; ! grep -F 'FAIL: Gate 20' /tmp/05-03-verify.log</automated>
  </verify>
  <done>
    `dist/index.html`, `dist/design/index.html`, `dist/about/index.html` all contain the `@media (max-width: 700px)` block AND `aria-label="Email Caleb"` / `aria-label="Caleb on LinkedIn"` / `aria-label="Download Caleb's resume"`. Desktop `.nav-link` padding is ≥`12px 8px`. No regression in Phase 1–4 gates (especially 19a–f, 20). Closes BLOCKER-1. Advances SC1.
  </done>
</task>

</tasks>

<verification>
- `bash scripts/verify-build.sh` — Gate 23 OK; Gates 1–22 unchanged
- Manual visual check at 375×812 (Safari devtools or Firefox responsive design mode): brand left, three icons right, no overflow, no hamburger
- Manual visual check at 1280px: existing text-link topbar, unchanged
- Screen-reader smoke: macOS VoiceOver Cmd+F5 walks the topbar and reads "Email Caleb, link" / "Caleb on LinkedIn, link" / "Download Caleb's resume, link" at ≤700px
- Real-device verification deferred to Plan 05-08
</verification>

<success_criteria>
SC1's primary symptom (mobile topbar overflow at 375px) is closed: every page Base.astro renders has a working tap-safe icon row below 700px. Recruiters on iPhone can tap all four affordances (brand back-to-splash, email, linkedin, resume) without horizontal scroll or clipped text. Gate 23 is GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-performance-accessibility/05-03-SUMMARY.md`. Note explicitly:
- Three inline SVG paths (envelope/in/download) were hand-authored — Claude's Discretion #1 acted on
- `.visually-hidden` utility added to tokens.css is currently unused (the `display: none` + `aria-label` pattern won the design call); leaving it in for future use
- Phase 4 BLOCKER-1 is closed; the Phase-4 carry-over note in ROADMAP §Phase 5 SC1 can be re-classified as "closed by Plan 05-03"
</output>
